/*
 * Copyright (c) Microsoft Corporation.
 * Licensed under the MIT License.
 *
 * Code generated by Microsoft (R) AutoRest Code Generator.
 * Changes may cause incorrect behavior and will be lost if the code is regenerated.
 */

import { PagedAsyncIterableIterator, PageSettings } from "@azure/core-paging";
import { setContinuationToken } from "../pagingHelper.js";
import { Endpoint } from "../operationsInterfaces/index.js";
import * as coreClient from "@azure/core-client";
import * as Mappers from "../models/mappers.js";
import * as Parameters from "../models/parameters.js";
import { MissionClient } from "../missionClient.js";
import {
  SimplePollerLike,
  OperationState,
  createHttpPoller,
} from "@azure/core-lro";
import { createLroSpec } from "../lroImpl.js";
import {
  EndpointResource,
  EndpointListBySubscriptionNextOptionalParams,
  EndpointListBySubscriptionOptionalParams,
  EndpointListBySubscriptionResponse,
  EndpointListByEnclaveResourceNextOptionalParams,
  EndpointListByEnclaveResourceOptionalParams,
  EndpointListByEnclaveResourceResponse,
  EndpointGetOptionalParams,
  EndpointGetResponse,
  EndpointCreateOrUpdateOptionalParams,
  EndpointCreateOrUpdateResponse,
  EndpointResourceUpdate,
  EndpointUpdateOptionalParams,
  EndpointUpdateResponse,
  EndpointDeleteOptionalParams,
  EndpointListBySubscriptionNextResponse,
  EndpointListByEnclaveResourceNextResponse,
} from "../models/index.js";

/// <reference lib="esnext.asynciterable" />
/** Class containing Endpoint operations. */
export class EndpointImpl implements Endpoint {
  private readonly client: MissionClient;

  /**
   * Initialize a new instance of the class Endpoint class.
   * @param client Reference to the service client
   */
  constructor(client: MissionClient) {
    this.client = client;
  }

  /**
   * List EndpointResource resources by subscription ID
   * @param virtualEnclaveName The name of the enclaveResource Resource
   * @param options The options parameters.
   */
  public listBySubscription(
    virtualEnclaveName: string,
    options?: EndpointListBySubscriptionOptionalParams,
  ): PagedAsyncIterableIterator<EndpointResource> {
    const iter = this.listBySubscriptionPagingAll(virtualEnclaveName, options);
    return {
      next() {
        return iter.next();
      },
      [Symbol.asyncIterator]() {
        return this;
      },
      byPage: (settings?: PageSettings) => {
        if (settings?.maxPageSize) {
          throw new Error("maxPageSize is not supported by this operation.");
        }
        return this.listBySubscriptionPagingPage(
          virtualEnclaveName,
          options,
          settings,
        );
      },
    };
  }

  private async *listBySubscriptionPagingPage(
    virtualEnclaveName: string,
    options?: EndpointListBySubscriptionOptionalParams,
    settings?: PageSettings,
  ): AsyncIterableIterator<EndpointResource[]> {
    let result: EndpointListBySubscriptionResponse;
    let continuationToken = settings?.continuationToken;
    if (!continuationToken) {
      result = await this._listBySubscription(virtualEnclaveName, options);
      let page = result.value || [];
      continuationToken = result.nextLink;
      setContinuationToken(page, continuationToken);
      yield page;
    }
    while (continuationToken) {
      result = await this._listBySubscriptionNext(
        virtualEnclaveName,
        continuationToken,
        options,
      );
      continuationToken = result.nextLink;
      let page = result.value || [];
      setContinuationToken(page, continuationToken);
      yield page;
    }
  }

  private async *listBySubscriptionPagingAll(
    virtualEnclaveName: string,
    options?: EndpointListBySubscriptionOptionalParams,
  ): AsyncIterableIterator<EndpointResource> {
    for await (const page of this.listBySubscriptionPagingPage(
      virtualEnclaveName,
      options,
    )) {
      yield* page;
    }
  }

  /**
   * List EndpointResource resources by EnclaveResource
   * @param resourceGroupName The name of the resource group. The name is case insensitive.
   * @param virtualEnclaveName The name of the enclaveResource Resource
   * @param options The options parameters.
   */
  public listByEnclaveResource(
    resourceGroupName: string,
    virtualEnclaveName: string,
    options?: EndpointListByEnclaveResourceOptionalParams,
  ): PagedAsyncIterableIterator<EndpointResource> {
    const iter = this.listByEnclaveResourcePagingAll(
      resourceGroupName,
      virtualEnclaveName,
      options,
    );
    return {
      next() {
        return iter.next();
      },
      [Symbol.asyncIterator]() {
        return this;
      },
      byPage: (settings?: PageSettings) => {
        if (settings?.maxPageSize) {
          throw new Error("maxPageSize is not supported by this operation.");
        }
        return this.listByEnclaveResourcePagingPage(
          resourceGroupName,
          virtualEnclaveName,
          options,
          settings,
        );
      },
    };
  }

  private async *listByEnclaveResourcePagingPage(
    resourceGroupName: string,
    virtualEnclaveName: string,
    options?: EndpointListByEnclaveResourceOptionalParams,
    settings?: PageSettings,
  ): AsyncIterableIterator<EndpointResource[]> {
    let result: EndpointListByEnclaveResourceResponse;
    let continuationToken = settings?.continuationToken;
    if (!continuationToken) {
      result = await this._listByEnclaveResource(
        resourceGroupName,
        virtualEnclaveName,
        options,
      );
      let page = result.value || [];
      continuationToken = result.nextLink;
      setContinuationToken(page, continuationToken);
      yield page;
    }
    while (continuationToken) {
      result = await this._listByEnclaveResourceNext(
        resourceGroupName,
        virtualEnclaveName,
        continuationToken,
        options,
      );
      continuationToken = result.nextLink;
      let page = result.value || [];
      setContinuationToken(page, continuationToken);
      yield page;
    }
  }

  private async *listByEnclaveResourcePagingAll(
    resourceGroupName: string,
    virtualEnclaveName: string,
    options?: EndpointListByEnclaveResourceOptionalParams,
  ): AsyncIterableIterator<EndpointResource> {
    for await (const page of this.listByEnclaveResourcePagingPage(
      resourceGroupName,
      virtualEnclaveName,
      options,
    )) {
      yield* page;
    }
  }

  /**
   * List EndpointResource resources by subscription ID
   * @param virtualEnclaveName The name of the enclaveResource Resource
   * @param options The options parameters.
   */
  private _listBySubscription(
    virtualEnclaveName: string,
    options?: EndpointListBySubscriptionOptionalParams,
  ): Promise<EndpointListBySubscriptionResponse> {
    return this.client.sendOperationRequest(
      { virtualEnclaveName, options },
      listBySubscriptionOperationSpec,
    );
  }

  /**
   * List EndpointResource resources by EnclaveResource
   * @param resourceGroupName The name of the resource group. The name is case insensitive.
   * @param virtualEnclaveName The name of the enclaveResource Resource
   * @param options The options parameters.
   */
  private _listByEnclaveResource(
    resourceGroupName: string,
    virtualEnclaveName: string,
    options?: EndpointListByEnclaveResourceOptionalParams,
  ): Promise<EndpointListByEnclaveResourceResponse> {
    return this.client.sendOperationRequest(
      { resourceGroupName, virtualEnclaveName, options },
      listByEnclaveResourceOperationSpec,
    );
  }

  /**
   * Get a EndpointResource
   * @param resourceGroupName The name of the resource group. The name is case insensitive.
   * @param virtualEnclaveName The name of the enclaveResource Resource
   * @param endpointName The name of the endpointResource Resource
   * @param options The options parameters.
   */
  get(
    resourceGroupName: string,
    virtualEnclaveName: string,
    endpointName: string,
    options?: EndpointGetOptionalParams,
  ): Promise<EndpointGetResponse> {
    return this.client.sendOperationRequest(
      { resourceGroupName, virtualEnclaveName, endpointName, options },
      getOperationSpec,
    );
  }

  /**
   * Create a EndpointResource
   * @param resourceGroupName The name of the resource group. The name is case insensitive.
   * @param virtualEnclaveName The name of the enclaveResource Resource
   * @param endpointName The name of the endpointResource Resource
   * @param resource Resource create parameters.
   * @param options The options parameters.
   */
  async beginCreateOrUpdate(
    resourceGroupName: string,
    virtualEnclaveName: string,
    endpointName: string,
    resource: EndpointResource,
    options?: EndpointCreateOrUpdateOptionalParams,
  ): Promise<
    SimplePollerLike<
      OperationState<EndpointCreateOrUpdateResponse>,
      EndpointCreateOrUpdateResponse
    >
  > {
    const directSendOperation = async (
      args: coreClient.OperationArguments,
      spec: coreClient.OperationSpec,
    ): Promise<EndpointCreateOrUpdateResponse> => {
      return this.client.sendOperationRequest(args, spec);
    };
    const sendOperationFn = async (
      args: coreClient.OperationArguments,
      spec: coreClient.OperationSpec,
    ) => {
      let currentRawResponse: coreClient.FullOperationResponse | undefined =
        undefined;
      const providedCallback = args.options?.onResponse;
      const callback: coreClient.RawResponseCallback = (
        rawResponse: coreClient.FullOperationResponse,
        flatResponse: unknown,
      ) => {
        currentRawResponse = rawResponse;
        providedCallback?.(rawResponse, flatResponse);
      };
      const updatedArgs = {
        ...args,
        options: {
          ...args.options,
          onResponse: callback,
        },
      };
      const flatResponse = await directSendOperation(updatedArgs, spec);
      return {
        flatResponse,
        rawResponse: {
          statusCode: currentRawResponse!.status,
          body: currentRawResponse!.parsedBody,
          headers: currentRawResponse!.headers.toJSON(),
        },
      };
    };

    const lro = createLroSpec({
      sendOperationFn,
      args: {
        resourceGroupName,
        virtualEnclaveName,
        endpointName,
        resource,
        options,
      },
      spec: createOrUpdateOperationSpec,
    });
    const poller = await createHttpPoller<
      EndpointCreateOrUpdateResponse,
      OperationState<EndpointCreateOrUpdateResponse>
    >(lro, {
      restoreFrom: options?.resumeFrom,
      intervalInMs: options?.updateIntervalInMs,
      resourceLocationConfig: "azure-async-operation",
    });
    await poller.poll();
    return poller;
  }

  /**
   * Create a EndpointResource
   * @param resourceGroupName The name of the resource group. The name is case insensitive.
   * @param virtualEnclaveName The name of the enclaveResource Resource
   * @param endpointName The name of the endpointResource Resource
   * @param resource Resource create parameters.
   * @param options The options parameters.
   */
  async beginCreateOrUpdateAndWait(
    resourceGroupName: string,
    virtualEnclaveName: string,
    endpointName: string,
    resource: EndpointResource,
    options?: EndpointCreateOrUpdateOptionalParams,
  ): Promise<EndpointCreateOrUpdateResponse> {
    const poller = await this.beginCreateOrUpdate(
      resourceGroupName,
      virtualEnclaveName,
      endpointName,
      resource,
      options,
    );
    return poller.pollUntilDone();
  }

  /**
   * Update a EndpointResource
   * @param resourceGroupName The name of the resource group. The name is case insensitive.
   * @param virtualEnclaveName The name of the enclaveResource Resource
   * @param endpointName The name of the endpointResource Resource
   * @param properties The resource properties to be updated.
   * @param options The options parameters.
   */
  async beginUpdate(
    resourceGroupName: string,
    virtualEnclaveName: string,
    endpointName: string,
    properties: EndpointResourceUpdate,
    options?: EndpointUpdateOptionalParams,
  ): Promise<
    SimplePollerLike<
      OperationState<EndpointUpdateResponse>,
      EndpointUpdateResponse
    >
  > {
    const directSendOperation = async (
      args: coreClient.OperationArguments,
      spec: coreClient.OperationSpec,
    ): Promise<EndpointUpdateResponse> => {
      return this.client.sendOperationRequest(args, spec);
    };
    const sendOperationFn = async (
      args: coreClient.OperationArguments,
      spec: coreClient.OperationSpec,
    ) => {
      let currentRawResponse: coreClient.FullOperationResponse | undefined =
        undefined;
      const providedCallback = args.options?.onResponse;
      const callback: coreClient.RawResponseCallback = (
        rawResponse: coreClient.FullOperationResponse,
        flatResponse: unknown,
      ) => {
        currentRawResponse = rawResponse;
        providedCallback?.(rawResponse, flatResponse);
      };
      const updatedArgs = {
        ...args,
        options: {
          ...args.options,
          onResponse: callback,
        },
      };
      const flatResponse = await directSendOperation(updatedArgs, spec);
      return {
        flatResponse,
        rawResponse: {
          statusCode: currentRawResponse!.status,
          body: currentRawResponse!.parsedBody,
          headers: currentRawResponse!.headers.toJSON(),
        },
      };
    };

    const lro = createLroSpec({
      sendOperationFn,
      args: {
        resourceGroupName,
        virtualEnclaveName,
        endpointName,
        properties,
        options,
      },
      spec: updateOperationSpec,
    });
    const poller = await createHttpPoller<
      EndpointUpdateResponse,
      OperationState<EndpointUpdateResponse>
    >(lro, {
      restoreFrom: options?.resumeFrom,
      intervalInMs: options?.updateIntervalInMs,
      resourceLocationConfig: "location",
    });
    await poller.poll();
    return poller;
  }

  /**
   * Update a EndpointResource
   * @param resourceGroupName The name of the resource group. The name is case insensitive.
   * @param virtualEnclaveName The name of the enclaveResource Resource
   * @param endpointName The name of the endpointResource Resource
   * @param properties The resource properties to be updated.
   * @param options The options parameters.
   */
  async beginUpdateAndWait(
    resourceGroupName: string,
    virtualEnclaveName: string,
    endpointName: string,
    properties: EndpointResourceUpdate,
    options?: EndpointUpdateOptionalParams,
  ): Promise<EndpointUpdateResponse> {
    const poller = await this.beginUpdate(
      resourceGroupName,
      virtualEnclaveName,
      endpointName,
      properties,
      options,
    );
    return poller.pollUntilDone();
  }

  /**
   * Delete a EndpointResource
   * @param resourceGroupName The name of the resource group. The name is case insensitive.
   * @param virtualEnclaveName The name of the enclaveResource Resource
   * @param endpointName The name of the endpointResource Resource
   * @param options The options parameters.
   */
  async beginDelete(
    resourceGroupName: string,
    virtualEnclaveName: string,
    endpointName: string,
    options?: EndpointDeleteOptionalParams,
  ): Promise<SimplePollerLike<OperationState<void>, void>> {
    const directSendOperation = async (
      args: coreClient.OperationArguments,
      spec: coreClient.OperationSpec,
    ): Promise<void> => {
      return this.client.sendOperationRequest(args, spec);
    };
    const sendOperationFn = async (
      args: coreClient.OperationArguments,
      spec: coreClient.OperationSpec,
    ) => {
      let currentRawResponse: coreClient.FullOperationResponse | undefined =
        undefined;
      const providedCallback = args.options?.onResponse;
      const callback: coreClient.RawResponseCallback = (
        rawResponse: coreClient.FullOperationResponse,
        flatResponse: unknown,
      ) => {
        currentRawResponse = rawResponse;
        providedCallback?.(rawResponse, flatResponse);
      };
      const updatedArgs = {
        ...args,
        options: {
          ...args.options,
          onResponse: callback,
        },
      };
      const flatResponse = await directSendOperation(updatedArgs, spec);
      return {
        flatResponse,
        rawResponse: {
          statusCode: currentRawResponse!.status,
          body: currentRawResponse!.parsedBody,
          headers: currentRawResponse!.headers.toJSON(),
        },
      };
    };

    const lro = createLroSpec({
      sendOperationFn,
      args: { resourceGroupName, virtualEnclaveName, endpointName, options },
      spec: deleteOperationSpec,
    });
    const poller = await createHttpPoller<void, OperationState<void>>(lro, {
      restoreFrom: options?.resumeFrom,
      intervalInMs: options?.updateIntervalInMs,
      resourceLocationConfig: "location",
    });
    await poller.poll();
    return poller;
  }

  /**
   * Delete a EndpointResource
   * @param resourceGroupName The name of the resource group. The name is case insensitive.
   * @param virtualEnclaveName The name of the enclaveResource Resource
   * @param endpointName The name of the endpointResource Resource
   * @param options The options parameters.
   */
  async beginDeleteAndWait(
    resourceGroupName: string,
    virtualEnclaveName: string,
    endpointName: string,
    options?: EndpointDeleteOptionalParams,
  ): Promise<void> {
    const poller = await this.beginDelete(
      resourceGroupName,
      virtualEnclaveName,
      endpointName,
      options,
    );
    return poller.pollUntilDone();
  }

  /**
   * ListBySubscriptionNext
   * @param virtualEnclaveName The name of the enclaveResource Resource
   * @param nextLink The nextLink from the previous successful call to the ListBySubscription method.
   * @param options The options parameters.
   */
  private _listBySubscriptionNext(
    virtualEnclaveName: string,
    nextLink: string,
    options?: EndpointListBySubscriptionNextOptionalParams,
  ): Promise<EndpointListBySubscriptionNextResponse> {
    return this.client.sendOperationRequest(
      { virtualEnclaveName, nextLink, options },
      listBySubscriptionNextOperationSpec,
    );
  }

  /**
   * ListByEnclaveResourceNext
   * @param resourceGroupName The name of the resource group. The name is case insensitive.
   * @param virtualEnclaveName The name of the enclaveResource Resource
   * @param nextLink The nextLink from the previous successful call to the ListByEnclaveResource method.
   * @param options The options parameters.
   */
  private _listByEnclaveResourceNext(
    resourceGroupName: string,
    virtualEnclaveName: string,
    nextLink: string,
    options?: EndpointListByEnclaveResourceNextOptionalParams,
  ): Promise<EndpointListByEnclaveResourceNextResponse> {
    return this.client.sendOperationRequest(
      { resourceGroupName, virtualEnclaveName, nextLink, options },
      listByEnclaveResourceNextOperationSpec,
    );
  }
}
// Operation Specifications
const serializer = coreClient.createSerializer(Mappers, /* isXml */ false);

const listBySubscriptionOperationSpec: coreClient.OperationSpec = {
  path: "/subscriptions/{subscriptionId}/providers/Microsoft.Mission/virtualEnclaves/{virtualEnclaveName}/endpoints",
  httpMethod: "GET",
  responses: {
    200: {
      bodyMapper: Mappers.EndpointResourceListResult,
    },
    default: {
      bodyMapper: Mappers.ErrorResponse,
    },
  },
  queryParameters: [Parameters.apiVersion],
  urlParameters: [
    Parameters.$host,
    Parameters.subscriptionId,
    Parameters.virtualEnclaveName,
  ],
  headerParameters: [Parameters.accept],
  serializer,
};
const listByEnclaveResourceOperationSpec: coreClient.OperationSpec = {
  path: "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Mission/virtualEnclaves/{virtualEnclaveName}/endpoints",
  httpMethod: "GET",
  responses: {
    200: {
      bodyMapper: Mappers.EndpointResourceListResult,
    },
    default: {
      bodyMapper: Mappers.ErrorResponse,
    },
  },
  queryParameters: [Parameters.apiVersion],
  urlParameters: [
    Parameters.$host,
    Parameters.subscriptionId,
    Parameters.resourceGroupName,
    Parameters.virtualEnclaveName,
  ],
  headerParameters: [Parameters.accept],
  serializer,
};
const getOperationSpec: coreClient.OperationSpec = {
  path: "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Mission/virtualEnclaves/{virtualEnclaveName}/endpoints/{endpointName}",
  httpMethod: "GET",
  responses: {
    200: {
      bodyMapper: Mappers.EndpointResource,
    },
    default: {
      bodyMapper: Mappers.ErrorResponse,
    },
  },
  queryParameters: [Parameters.apiVersion],
  urlParameters: [
    Parameters.$host,
    Parameters.subscriptionId,
    Parameters.resourceGroupName,
    Parameters.virtualEnclaveName,
    Parameters.endpointName,
  ],
  headerParameters: [Parameters.accept],
  serializer,
};
const createOrUpdateOperationSpec: coreClient.OperationSpec = {
  path: "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Mission/virtualEnclaves/{virtualEnclaveName}/endpoints/{endpointName}",
  httpMethod: "PUT",
  responses: {
    200: {
      bodyMapper: Mappers.EndpointResource,
    },
    201: {
      bodyMapper: Mappers.EndpointResource,
    },
    202: {
      bodyMapper: Mappers.EndpointResource,
    },
    204: {
      bodyMapper: Mappers.EndpointResource,
    },
    default: {
      bodyMapper: Mappers.ErrorResponse,
    },
  },
  requestBody: Parameters.resource9,
  queryParameters: [Parameters.apiVersion],
  urlParameters: [
    Parameters.$host,
    Parameters.subscriptionId,
    Parameters.resourceGroupName,
    Parameters.virtualEnclaveName,
    Parameters.endpointName,
  ],
  headerParameters: [Parameters.accept, Parameters.contentType],
  mediaType: "json",
  serializer,
};
const updateOperationSpec: coreClient.OperationSpec = {
  path: "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Mission/virtualEnclaves/{virtualEnclaveName}/endpoints/{endpointName}",
  httpMethod: "PATCH",
  responses: {
    200: {
      bodyMapper: Mappers.EndpointResource,
    },
    201: {
      bodyMapper: Mappers.EndpointResource,
    },
    202: {
      bodyMapper: Mappers.EndpointResource,
    },
    204: {
      bodyMapper: Mappers.EndpointResource,
    },
    default: {
      bodyMapper: Mappers.ErrorResponse,
    },
  },
  requestBody: Parameters.properties9,
  queryParameters: [Parameters.apiVersion],
  urlParameters: [
    Parameters.$host,
    Parameters.subscriptionId,
    Parameters.resourceGroupName,
    Parameters.virtualEnclaveName,
    Parameters.endpointName,
  ],
  headerParameters: [Parameters.accept, Parameters.contentType],
  mediaType: "json",
  serializer,
};
const deleteOperationSpec: coreClient.OperationSpec = {
  path: "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Mission/virtualEnclaves/{virtualEnclaveName}/endpoints/{endpointName}",
  httpMethod: "DELETE",
  responses: {
    200: {},
    201: {},
    202: {},
    204: {},
    default: {
      bodyMapper: Mappers.ErrorResponse,
    },
  },
  queryParameters: [Parameters.apiVersion],
  urlParameters: [
    Parameters.$host,
    Parameters.subscriptionId,
    Parameters.resourceGroupName,
    Parameters.virtualEnclaveName,
    Parameters.endpointName,
  ],
  headerParameters: [Parameters.accept],
  serializer,
};
const listBySubscriptionNextOperationSpec: coreClient.OperationSpec = {
  path: "{nextLink}",
  httpMethod: "GET",
  responses: {
    200: {
      bodyMapper: Mappers.EndpointResourceListResult,
    },
    default: {
      bodyMapper: Mappers.ErrorResponse,
    },
  },
  urlParameters: [
    Parameters.$host,
    Parameters.nextLink,
    Parameters.subscriptionId,
    Parameters.virtualEnclaveName,
  ],
  headerParameters: [Parameters.accept],
  serializer,
};
const listByEnclaveResourceNextOperationSpec: coreClient.OperationSpec = {
  path: "{nextLink}",
  httpMethod: "GET",
  responses: {
    200: {
      bodyMapper: Mappers.EndpointResourceListResult,
    },
    default: {
      bodyMapper: Mappers.ErrorResponse,
    },
  },
  urlParameters: [
    Parameters.$host,
    Parameters.nextLink,
    Parameters.subscriptionId,
    Parameters.resourceGroupName,
    Parameters.virtualEnclaveName,
  ],
  headerParameters: [Parameters.accept],
  serializer,
};
