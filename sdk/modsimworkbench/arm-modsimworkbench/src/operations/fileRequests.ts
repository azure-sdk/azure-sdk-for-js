/*
 * Copyright (c) Microsoft Corporation.
 * Licensed under the MIT License.
 *
 * Code generated by Microsoft (R) AutoRest Code Generator.
 * Changes may cause incorrect behavior and will be lost if the code is regenerated.
 */

import { PagedAsyncIterableIterator, PageSettings } from "@azure/core-paging";
import { setContinuationToken } from "../pagingHelper";
import { FileRequests } from "../operationsInterfaces";
import * as coreClient from "@azure/core-client";
import * as Mappers from "../models/mappers";
import * as Parameters from "../models/parameters";
import { MicrosoftModSimWorkbench } from "../microsoftModSimWorkbench";
import {
  SimplePollerLike,
  OperationState,
  createHttpPoller
} from "@azure/core-lro";
import { createLroSpec } from "../lroImpl";
import {
  Request,
  FileRequestsListNextOptionalParams,
  FileRequestsListOptionalParams,
  FileRequestsListResponse,
  FileRequestsGetOptionalParams,
  FileRequestsGetResponse,
  FileRequestsManageOptionalParams,
  FileRequestsManageResponse,
  FileRequestsListNextResponse
} from "../models";

/// <reference lib="esnext.asynciterable" />
/** Class containing FileRequests operations. */
export class FileRequestsImpl implements FileRequests {
  private readonly client: MicrosoftModSimWorkbench;

  /**
   * Initialize a new instance of the class FileRequests class.
   * @param client Reference to the service client
   */
  constructor(client: MicrosoftModSimWorkbench) {
    this.client = client;
  }

  /**
   * Get ModSim Workbench chamber data pipeline file request resource collection.
   * @param resourceGroupName The name of the resource group. The name is case insensitive.
   * @param workbenchName The name of the workbench.
   * @param chamberName The name of the chamber.
   * @param options The options parameters.
   */
  public list(
    resourceGroupName: string,
    workbenchName: string,
    chamberName: string,
    options?: FileRequestsListOptionalParams
  ): PagedAsyncIterableIterator<Request> {
    const iter = this.listPagingAll(
      resourceGroupName,
      workbenchName,
      chamberName,
      options
    );
    return {
      next() {
        return iter.next();
      },
      [Symbol.asyncIterator]() {
        return this;
      },
      byPage: (settings?: PageSettings) => {
        if (settings?.maxPageSize) {
          throw new Error("maxPageSize is not supported by this operation.");
        }
        return this.listPagingPage(
          resourceGroupName,
          workbenchName,
          chamberName,
          options,
          settings
        );
      }
    };
  }

  private async *listPagingPage(
    resourceGroupName: string,
    workbenchName: string,
    chamberName: string,
    options?: FileRequestsListOptionalParams,
    settings?: PageSettings
  ): AsyncIterableIterator<Request[]> {
    let result: FileRequestsListResponse;
    let continuationToken = settings?.continuationToken;
    if (!continuationToken) {
      result = await this._list(
        resourceGroupName,
        workbenchName,
        chamberName,
        options
      );
      let page = result.value || [];
      continuationToken = result.nextLink;
      setContinuationToken(page, continuationToken);
      yield page;
    }
    while (continuationToken) {
      result = await this._listNext(
        resourceGroupName,
        workbenchName,
        chamberName,
        continuationToken,
        options
      );
      continuationToken = result.nextLink;
      let page = result.value || [];
      setContinuationToken(page, continuationToken);
      yield page;
    }
  }

  private async *listPagingAll(
    resourceGroupName: string,
    workbenchName: string,
    chamberName: string,
    options?: FileRequestsListOptionalParams
  ): AsyncIterableIterator<Request> {
    for await (const page of this.listPagingPage(
      resourceGroupName,
      workbenchName,
      chamberName,
      options
    )) {
      yield* page;
    }
  }

  /**
   * Get a specific ModSim Workbench chamber data pipeline file request resource.
   * @param resourceGroupName The name of the resource group. The name is case insensitive.
   * @param workbenchName the workbench name.
   * @param chamberName the chamber name.
   * @param requestName the request name.
   * @param options The options parameters.
   */
  get(
    resourceGroupName: string,
    workbenchName: string,
    chamberName: string,
    requestName: string,
    options?: FileRequestsGetOptionalParams
  ): Promise<FileRequestsGetResponse> {
    return this.client.sendOperationRequest(
      { resourceGroupName, workbenchName, chamberName, requestName, options },
      getOperationSpec
    );
  }

  /**
   * Manage download file requests in an ModSim Workbench chamber Data pipeline.
   * @param resourceGroupName The name of the resource group. The name is case insensitive.
   * @param workbenchName the workbench name.
   * @param chamberName the chamber name.
   * @param requestName the file request name.
   * @param options The options parameters.
   */
  async beginManage(
    resourceGroupName: string,
    workbenchName: string,
    chamberName: string,
    requestName: string,
    options?: FileRequestsManageOptionalParams
  ): Promise<
    SimplePollerLike<
      OperationState<FileRequestsManageResponse>,
      FileRequestsManageResponse
    >
  > {
    const directSendOperation = async (
      args: coreClient.OperationArguments,
      spec: coreClient.OperationSpec
    ): Promise<FileRequestsManageResponse> => {
      return this.client.sendOperationRequest(args, spec);
    };
    const sendOperationFn = async (
      args: coreClient.OperationArguments,
      spec: coreClient.OperationSpec
    ) => {
      let currentRawResponse:
        | coreClient.FullOperationResponse
        | undefined = undefined;
      const providedCallback = args.options?.onResponse;
      const callback: coreClient.RawResponseCallback = (
        rawResponse: coreClient.FullOperationResponse,
        flatResponse: unknown
      ) => {
        currentRawResponse = rawResponse;
        providedCallback?.(rawResponse, flatResponse);
      };
      const updatedArgs = {
        ...args,
        options: {
          ...args.options,
          onResponse: callback
        }
      };
      const flatResponse = await directSendOperation(updatedArgs, spec);
      return {
        flatResponse,
        rawResponse: {
          statusCode: currentRawResponse!.status,
          body: currentRawResponse!.parsedBody,
          headers: currentRawResponse!.headers.toJSON()
        }
      };
    };

    const lro = createLroSpec({
      sendOperationFn,
      args: {
        resourceGroupName,
        workbenchName,
        chamberName,
        requestName,
        options
      },
      spec: manageOperationSpec
    });
    const poller = await createHttpPoller<
      FileRequestsManageResponse,
      OperationState<FileRequestsManageResponse>
    >(lro, {
      restoreFrom: options?.resumeFrom,
      intervalInMs: options?.updateIntervalInMs,
      resourceLocationConfig: "location"
    });
    await poller.poll();
    return poller;
  }

  /**
   * Manage download file requests in an ModSim Workbench chamber Data pipeline.
   * @param resourceGroupName The name of the resource group. The name is case insensitive.
   * @param workbenchName the workbench name.
   * @param chamberName the chamber name.
   * @param requestName the file request name.
   * @param options The options parameters.
   */
  async beginManageAndWait(
    resourceGroupName: string,
    workbenchName: string,
    chamberName: string,
    requestName: string,
    options?: FileRequestsManageOptionalParams
  ): Promise<FileRequestsManageResponse> {
    const poller = await this.beginManage(
      resourceGroupName,
      workbenchName,
      chamberName,
      requestName,
      options
    );
    return poller.pollUntilDone();
  }

  /**
   * Get ModSim Workbench chamber data pipeline file request resource collection.
   * @param resourceGroupName The name of the resource group. The name is case insensitive.
   * @param workbenchName The name of the workbench.
   * @param chamberName The name of the chamber.
   * @param options The options parameters.
   */
  private _list(
    resourceGroupName: string,
    workbenchName: string,
    chamberName: string,
    options?: FileRequestsListOptionalParams
  ): Promise<FileRequestsListResponse> {
    return this.client.sendOperationRequest(
      { resourceGroupName, workbenchName, chamberName, options },
      listOperationSpec
    );
  }

  /**
   * ListNext
   * @param resourceGroupName The name of the resource group. The name is case insensitive.
   * @param workbenchName The name of the workbench.
   * @param chamberName The name of the chamber.
   * @param nextLink The nextLink from the previous successful call to the List method.
   * @param options The options parameters.
   */
  private _listNext(
    resourceGroupName: string,
    workbenchName: string,
    chamberName: string,
    nextLink: string,
    options?: FileRequestsListNextOptionalParams
  ): Promise<FileRequestsListNextResponse> {
    return this.client.sendOperationRequest(
      { resourceGroupName, workbenchName, chamberName, nextLink, options },
      listNextOperationSpec
    );
  }
}
// Operation Specifications
const serializer = coreClient.createSerializer(Mappers, /* isXml */ false);

const getOperationSpec: coreClient.OperationSpec = {
  path:
    "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.ModSimWorkbench/workbenches/{workbenchName}/chambers/{chamberName}/fileRequests/{requestName}",
  httpMethod: "GET",
  responses: {
    200: {
      bodyMapper: Mappers.Request
    },
    default: {
      bodyMapper: Mappers.ErrorResponse
    }
  },
  queryParameters: [Parameters.apiVersion],
  urlParameters: [
    Parameters.$host,
    Parameters.subscriptionId,
    Parameters.workbenchName,
    Parameters.chamberName,
    Parameters.resourceGroupName,
    Parameters.requestName
  ],
  headerParameters: [Parameters.accept],
  serializer
};
const manageOperationSpec: coreClient.OperationSpec = {
  path:
    "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.ModSimWorkbench/workbenches/{workbenchName}/chambers/{chamberName}/fileRequests/{requestName}/manage",
  httpMethod: "POST",
  responses: {
    200: {
      bodyMapper: Mappers.FileRequestsManage
    },
    201: {
      bodyMapper: Mappers.FileRequestsManage
    },
    202: {
      bodyMapper: Mappers.FileRequestsManage
    },
    204: {
      bodyMapper: Mappers.FileRequestsManage
    },
    default: {
      bodyMapper: Mappers.ErrorResponse
    }
  },
  requestBody: Parameters.body7,
  queryParameters: [Parameters.apiVersion],
  urlParameters: [
    Parameters.$host,
    Parameters.subscriptionId,
    Parameters.workbenchName,
    Parameters.chamberName,
    Parameters.resourceGroupName,
    Parameters.requestName
  ],
  headerParameters: [Parameters.accept, Parameters.contentType],
  mediaType: "json",
  serializer
};
const listOperationSpec: coreClient.OperationSpec = {
  path:
    "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.ModSimWorkbench/workbenches/{workbenchName}/chambers/{chamberName}/fileRequests",
  httpMethod: "GET",
  responses: {
    200: {
      bodyMapper: Mappers.RequestPageableList
    },
    default: {
      bodyMapper: Mappers.ErrorResponse
    }
  },
  queryParameters: [Parameters.apiVersion],
  urlParameters: [
    Parameters.$host,
    Parameters.subscriptionId,
    Parameters.workbenchName,
    Parameters.chamberName,
    Parameters.resourceGroupName
  ],
  headerParameters: [Parameters.accept],
  serializer
};
const listNextOperationSpec: coreClient.OperationSpec = {
  path: "{nextLink}",
  httpMethod: "GET",
  responses: {
    200: {
      bodyMapper: Mappers.RequestPageableList
    },
    default: {
      bodyMapper: Mappers.ErrorResponse
    }
  },
  urlParameters: [
    Parameters.$host,
    Parameters.subscriptionId,
    Parameters.nextLink,
    Parameters.workbenchName,
    Parameters.chamberName,
    Parameters.resourceGroupName
  ],
  headerParameters: [Parameters.accept],
  serializer
};
