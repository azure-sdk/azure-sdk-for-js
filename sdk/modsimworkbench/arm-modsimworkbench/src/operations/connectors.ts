/*
 * Copyright (c) Microsoft Corporation.
 * Licensed under the MIT License.
 *
 * Code generated by Microsoft (R) AutoRest Code Generator.
 * Changes may cause incorrect behavior and will be lost if the code is regenerated.
 */

import { PagedAsyncIterableIterator, PageSettings } from "@azure/core-paging";
import { setContinuationToken } from "../pagingHelper";
import { Connectors } from "../operationsInterfaces";
import * as coreClient from "@azure/core-client";
import * as Mappers from "../models/mappers";
import * as Parameters from "../models/parameters";
import { MicrosoftModSimWorkbench } from "../microsoftModSimWorkbench";
import {
  SimplePollerLike,
  OperationState,
  createHttpPoller
} from "@azure/core-lro";
import { createLroSpec } from "../lroImpl";
import {
  Connector,
  ConnectorsListNextOptionalParams,
  ConnectorsListOptionalParams,
  ConnectorsListResponse,
  ConnectorsStartOptionalParams,
  ConnectorsStopOptionalParams,
  ConnectorsRestartOptionalParams,
  ConnectorsCreateOrUpdateOptionalParams,
  ConnectorsCreateOrUpdateResponse,
  ConnectorsUpdateOptionalParams,
  ConnectorsUpdateResponse,
  ConnectorsGetOptionalParams,
  ConnectorsGetResponse,
  ConnectorsDeleteOptionalParams,
  ConnectorsDeleteResponse,
  LicenseKeyParameters,
  ConnectorsLicenseAddOptionalParams,
  ConnectorsListNextResponse
} from "../models";

/// <reference lib="esnext.asynciterable" />
/** Class containing Connectors operations. */
export class ConnectorsImpl implements Connectors {
  private readonly client: MicrosoftModSimWorkbench;

  /**
   * Initialize a new instance of the class Connectors class.
   * @param client Reference to the service client
   */
  constructor(client: MicrosoftModSimWorkbench) {
    this.client = client;
  }

  /**
   * Lists all connectors.
   * @param resourceGroup The name of the resource group.
   * @param workbenchName The name of the workbench.
   * @param chamberName The name of the chamber.
   * @param options The options parameters.
   */
  public list(
    resourceGroup: string,
    workbenchName: string,
    chamberName: string,
    options?: ConnectorsListOptionalParams
  ): PagedAsyncIterableIterator<Connector> {
    const iter = this.listPagingAll(
      resourceGroup,
      workbenchName,
      chamberName,
      options
    );
    return {
      next() {
        return iter.next();
      },
      [Symbol.asyncIterator]() {
        return this;
      },
      byPage: (settings?: PageSettings) => {
        if (settings?.maxPageSize) {
          throw new Error("maxPageSize is not supported by this operation.");
        }
        return this.listPagingPage(
          resourceGroup,
          workbenchName,
          chamberName,
          options,
          settings
        );
      }
    };
  }

  private async *listPagingPage(
    resourceGroup: string,
    workbenchName: string,
    chamberName: string,
    options?: ConnectorsListOptionalParams,
    settings?: PageSettings
  ): AsyncIterableIterator<Connector[]> {
    let result: ConnectorsListResponse;
    let continuationToken = settings?.continuationToken;
    if (!continuationToken) {
      result = await this._list(
        resourceGroup,
        workbenchName,
        chamberName,
        options
      );
      let page = result.value || [];
      continuationToken = result.nextLink;
      setContinuationToken(page, continuationToken);
      yield page;
    }
    while (continuationToken) {
      result = await this._listNext(
        resourceGroup,
        workbenchName,
        chamberName,
        continuationToken,
        options
      );
      continuationToken = result.nextLink;
      let page = result.value || [];
      setContinuationToken(page, continuationToken);
      yield page;
    }
  }

  private async *listPagingAll(
    resourceGroup: string,
    workbenchName: string,
    chamberName: string,
    options?: ConnectorsListOptionalParams
  ): AsyncIterableIterator<Connector> {
    for await (const page of this.listPagingPage(
      resourceGroup,
      workbenchName,
      chamberName,
      options
    )) {
      yield* page;
    }
  }

  /**
   * Perform Start operation.
   * @param resourceGroup Name of the resource group
   * @param workbenchName Name of the workbench
   * @param chamberName the chamber name.
   * @param name Name of the connector
   * @param options The options parameters.
   */
  async beginStart(
    resourceGroup: string,
    workbenchName: string,
    chamberName: string,
    name: string,
    options?: ConnectorsStartOptionalParams
  ): Promise<SimplePollerLike<OperationState<void>, void>> {
    const directSendOperation = async (
      args: coreClient.OperationArguments,
      spec: coreClient.OperationSpec
    ): Promise<void> => {
      return this.client.sendOperationRequest(args, spec);
    };
    const sendOperationFn = async (
      args: coreClient.OperationArguments,
      spec: coreClient.OperationSpec
    ) => {
      let currentRawResponse:
        | coreClient.FullOperationResponse
        | undefined = undefined;
      const providedCallback = args.options?.onResponse;
      const callback: coreClient.RawResponseCallback = (
        rawResponse: coreClient.FullOperationResponse,
        flatResponse: unknown
      ) => {
        currentRawResponse = rawResponse;
        providedCallback?.(rawResponse, flatResponse);
      };
      const updatedArgs = {
        ...args,
        options: {
          ...args.options,
          onResponse: callback
        }
      };
      const flatResponse = await directSendOperation(updatedArgs, spec);
      return {
        flatResponse,
        rawResponse: {
          statusCode: currentRawResponse!.status,
          body: currentRawResponse!.parsedBody,
          headers: currentRawResponse!.headers.toJSON()
        }
      };
    };

    const lro = createLroSpec({
      sendOperationFn,
      args: { resourceGroup, workbenchName, chamberName, name, options },
      spec: startOperationSpec
    });
    const poller = await createHttpPoller<void, OperationState<void>>(lro, {
      restoreFrom: options?.resumeFrom,
      intervalInMs: options?.updateIntervalInMs,
      resourceLocationConfig: "location"
    });
    await poller.poll();
    return poller;
  }

  /**
   * Perform Start operation.
   * @param resourceGroup Name of the resource group
   * @param workbenchName Name of the workbench
   * @param chamberName the chamber name.
   * @param name Name of the connector
   * @param options The options parameters.
   */
  async beginStartAndWait(
    resourceGroup: string,
    workbenchName: string,
    chamberName: string,
    name: string,
    options?: ConnectorsStartOptionalParams
  ): Promise<void> {
    const poller = await this.beginStart(
      resourceGroup,
      workbenchName,
      chamberName,
      name,
      options
    );
    return poller.pollUntilDone();
  }

  /**
   * Perform Stop operation.
   * @param resourceGroup the resource group.
   * @param workbenchName the workbench name.
   * @param chamberName the chamber name.
   * @param name the connector name.
   * @param options The options parameters.
   */
  async beginStop(
    resourceGroup: string,
    workbenchName: string,
    chamberName: string,
    name: string,
    options?: ConnectorsStopOptionalParams
  ): Promise<SimplePollerLike<OperationState<void>, void>> {
    const directSendOperation = async (
      args: coreClient.OperationArguments,
      spec: coreClient.OperationSpec
    ): Promise<void> => {
      return this.client.sendOperationRequest(args, spec);
    };
    const sendOperationFn = async (
      args: coreClient.OperationArguments,
      spec: coreClient.OperationSpec
    ) => {
      let currentRawResponse:
        | coreClient.FullOperationResponse
        | undefined = undefined;
      const providedCallback = args.options?.onResponse;
      const callback: coreClient.RawResponseCallback = (
        rawResponse: coreClient.FullOperationResponse,
        flatResponse: unknown
      ) => {
        currentRawResponse = rawResponse;
        providedCallback?.(rawResponse, flatResponse);
      };
      const updatedArgs = {
        ...args,
        options: {
          ...args.options,
          onResponse: callback
        }
      };
      const flatResponse = await directSendOperation(updatedArgs, spec);
      return {
        flatResponse,
        rawResponse: {
          statusCode: currentRawResponse!.status,
          body: currentRawResponse!.parsedBody,
          headers: currentRawResponse!.headers.toJSON()
        }
      };
    };

    const lro = createLroSpec({
      sendOperationFn,
      args: { resourceGroup, workbenchName, chamberName, name, options },
      spec: stopOperationSpec
    });
    const poller = await createHttpPoller<void, OperationState<void>>(lro, {
      restoreFrom: options?.resumeFrom,
      intervalInMs: options?.updateIntervalInMs,
      resourceLocationConfig: "location"
    });
    await poller.poll();
    return poller;
  }

  /**
   * Perform Stop operation.
   * @param resourceGroup the resource group.
   * @param workbenchName the workbench name.
   * @param chamberName the chamber name.
   * @param name the connector name.
   * @param options The options parameters.
   */
  async beginStopAndWait(
    resourceGroup: string,
    workbenchName: string,
    chamberName: string,
    name: string,
    options?: ConnectorsStopOptionalParams
  ): Promise<void> {
    const poller = await this.beginStop(
      resourceGroup,
      workbenchName,
      chamberName,
      name,
      options
    );
    return poller.pollUntilDone();
  }

  /**
   * Perform Restart operation.
   * @param resourceGroup the resource group.
   * @param workbenchName the workbench name.
   * @param chamberName the chamber name.
   * @param name the connector name.
   * @param options The options parameters.
   */
  async beginRestart(
    resourceGroup: string,
    workbenchName: string,
    chamberName: string,
    name: string,
    options?: ConnectorsRestartOptionalParams
  ): Promise<SimplePollerLike<OperationState<void>, void>> {
    const directSendOperation = async (
      args: coreClient.OperationArguments,
      spec: coreClient.OperationSpec
    ): Promise<void> => {
      return this.client.sendOperationRequest(args, spec);
    };
    const sendOperationFn = async (
      args: coreClient.OperationArguments,
      spec: coreClient.OperationSpec
    ) => {
      let currentRawResponse:
        | coreClient.FullOperationResponse
        | undefined = undefined;
      const providedCallback = args.options?.onResponse;
      const callback: coreClient.RawResponseCallback = (
        rawResponse: coreClient.FullOperationResponse,
        flatResponse: unknown
      ) => {
        currentRawResponse = rawResponse;
        providedCallback?.(rawResponse, flatResponse);
      };
      const updatedArgs = {
        ...args,
        options: {
          ...args.options,
          onResponse: callback
        }
      };
      const flatResponse = await directSendOperation(updatedArgs, spec);
      return {
        flatResponse,
        rawResponse: {
          statusCode: currentRawResponse!.status,
          body: currentRawResponse!.parsedBody,
          headers: currentRawResponse!.headers.toJSON()
        }
      };
    };

    const lro = createLroSpec({
      sendOperationFn,
      args: { resourceGroup, workbenchName, chamberName, name, options },
      spec: restartOperationSpec
    });
    const poller = await createHttpPoller<void, OperationState<void>>(lro, {
      restoreFrom: options?.resumeFrom,
      intervalInMs: options?.updateIntervalInMs,
      resourceLocationConfig: "location"
    });
    await poller.poll();
    return poller;
  }

  /**
   * Perform Restart operation.
   * @param resourceGroup the resource group.
   * @param workbenchName the workbench name.
   * @param chamberName the chamber name.
   * @param name the connector name.
   * @param options The options parameters.
   */
  async beginRestartAndWait(
    resourceGroup: string,
    workbenchName: string,
    chamberName: string,
    name: string,
    options?: ConnectorsRestartOptionalParams
  ): Promise<void> {
    const poller = await this.beginRestart(
      resourceGroup,
      workbenchName,
      chamberName,
      name,
      options
    );
    return poller.pollUntilDone();
  }

  /**
   * Creates or updates the specified connector.
   * @param resourceGroup The name of the resource group.
   * @param workbenchName The name of the workbench.
   * @param chamberName The name of the chamber.
   * @param connectorName The name of the connector.
   * @param options The options parameters.
   */
  async beginCreateOrUpdate(
    resourceGroup: string,
    workbenchName: string,
    chamberName: string,
    connectorName: string,
    options?: ConnectorsCreateOrUpdateOptionalParams
  ): Promise<
    SimplePollerLike<
      OperationState<ConnectorsCreateOrUpdateResponse>,
      ConnectorsCreateOrUpdateResponse
    >
  > {
    const directSendOperation = async (
      args: coreClient.OperationArguments,
      spec: coreClient.OperationSpec
    ): Promise<ConnectorsCreateOrUpdateResponse> => {
      return this.client.sendOperationRequest(args, spec);
    };
    const sendOperationFn = async (
      args: coreClient.OperationArguments,
      spec: coreClient.OperationSpec
    ) => {
      let currentRawResponse:
        | coreClient.FullOperationResponse
        | undefined = undefined;
      const providedCallback = args.options?.onResponse;
      const callback: coreClient.RawResponseCallback = (
        rawResponse: coreClient.FullOperationResponse,
        flatResponse: unknown
      ) => {
        currentRawResponse = rawResponse;
        providedCallback?.(rawResponse, flatResponse);
      };
      const updatedArgs = {
        ...args,
        options: {
          ...args.options,
          onResponse: callback
        }
      };
      const flatResponse = await directSendOperation(updatedArgs, spec);
      return {
        flatResponse,
        rawResponse: {
          statusCode: currentRawResponse!.status,
          body: currentRawResponse!.parsedBody,
          headers: currentRawResponse!.headers.toJSON()
        }
      };
    };

    const lro = createLroSpec({
      sendOperationFn,
      args: {
        resourceGroup,
        workbenchName,
        chamberName,
        connectorName,
        options
      },
      spec: createOrUpdateOperationSpec
    });
    const poller = await createHttpPoller<
      ConnectorsCreateOrUpdateResponse,
      OperationState<ConnectorsCreateOrUpdateResponse>
    >(lro, {
      restoreFrom: options?.resumeFrom,
      intervalInMs: options?.updateIntervalInMs,
      resourceLocationConfig: "azure-async-operation"
    });
    await poller.poll();
    return poller;
  }

  /**
   * Creates or updates the specified connector.
   * @param resourceGroup The name of the resource group.
   * @param workbenchName The name of the workbench.
   * @param chamberName The name of the chamber.
   * @param connectorName The name of the connector.
   * @param options The options parameters.
   */
  async beginCreateOrUpdateAndWait(
    resourceGroup: string,
    workbenchName: string,
    chamberName: string,
    connectorName: string,
    options?: ConnectorsCreateOrUpdateOptionalParams
  ): Promise<ConnectorsCreateOrUpdateResponse> {
    const poller = await this.beginCreateOrUpdate(
      resourceGroup,
      workbenchName,
      chamberName,
      connectorName,
      options
    );
    return poller.pollUntilDone();
  }

  /**
   * Updates the specified connectors.
   * @param resourceGroup The name of the resource group.
   * @param workbenchName The name of the workbench.
   * @param chamberName The name of the chamber.
   * @param connectorName The name of the connector.
   * @param options The options parameters.
   */
  update(
    resourceGroup: string,
    workbenchName: string,
    chamberName: string,
    connectorName: string,
    options?: ConnectorsUpdateOptionalParams
  ): Promise<ConnectorsUpdateResponse> {
    return this.client.sendOperationRequest(
      { resourceGroup, workbenchName, chamberName, connectorName, options },
      updateOperationSpec
    );
  }

  /**
   * Gets information about the specified connector.
   * @param resourceGroup The name of the resource group.
   * @param workbenchName The name of the workbench.
   * @param chamberName The name of the chamber.
   * @param connectorName The name of the connector.
   * @param options The options parameters.
   */
  get(
    resourceGroup: string,
    workbenchName: string,
    chamberName: string,
    connectorName: string,
    options?: ConnectorsGetOptionalParams
  ): Promise<ConnectorsGetResponse> {
    return this.client.sendOperationRequest(
      { resourceGroup, workbenchName, chamberName, connectorName, options },
      getOperationSpec
    );
  }

  /**
   * Deletes the specified connector.
   * @param resourceGroup The name of the resource group.
   * @param workbenchName The name of the workbench.
   * @param chamberName The name of the chamber.
   * @param connectorName The name of the connector.
   * @param options The options parameters.
   */
  async beginDelete(
    resourceGroup: string,
    workbenchName: string,
    chamberName: string,
    connectorName: string,
    options?: ConnectorsDeleteOptionalParams
  ): Promise<
    SimplePollerLike<
      OperationState<ConnectorsDeleteResponse>,
      ConnectorsDeleteResponse
    >
  > {
    const directSendOperation = async (
      args: coreClient.OperationArguments,
      spec: coreClient.OperationSpec
    ): Promise<ConnectorsDeleteResponse> => {
      return this.client.sendOperationRequest(args, spec);
    };
    const sendOperationFn = async (
      args: coreClient.OperationArguments,
      spec: coreClient.OperationSpec
    ) => {
      let currentRawResponse:
        | coreClient.FullOperationResponse
        | undefined = undefined;
      const providedCallback = args.options?.onResponse;
      const callback: coreClient.RawResponseCallback = (
        rawResponse: coreClient.FullOperationResponse,
        flatResponse: unknown
      ) => {
        currentRawResponse = rawResponse;
        providedCallback?.(rawResponse, flatResponse);
      };
      const updatedArgs = {
        ...args,
        options: {
          ...args.options,
          onResponse: callback
        }
      };
      const flatResponse = await directSendOperation(updatedArgs, spec);
      return {
        flatResponse,
        rawResponse: {
          statusCode: currentRawResponse!.status,
          body: currentRawResponse!.parsedBody,
          headers: currentRawResponse!.headers.toJSON()
        }
      };
    };

    const lro = createLroSpec({
      sendOperationFn,
      args: {
        resourceGroup,
        workbenchName,
        chamberName,
        connectorName,
        options
      },
      spec: deleteOperationSpec
    });
    const poller = await createHttpPoller<
      ConnectorsDeleteResponse,
      OperationState<ConnectorsDeleteResponse>
    >(lro, {
      restoreFrom: options?.resumeFrom,
      intervalInMs: options?.updateIntervalInMs,
      resourceLocationConfig: "location"
    });
    await poller.poll();
    return poller;
  }

  /**
   * Deletes the specified connector.
   * @param resourceGroup The name of the resource group.
   * @param workbenchName The name of the workbench.
   * @param chamberName The name of the chamber.
   * @param connectorName The name of the connector.
   * @param options The options parameters.
   */
  async beginDeleteAndWait(
    resourceGroup: string,
    workbenchName: string,
    chamberName: string,
    connectorName: string,
    options?: ConnectorsDeleteOptionalParams
  ): Promise<ConnectorsDeleteResponse> {
    const poller = await this.beginDelete(
      resourceGroup,
      workbenchName,
      chamberName,
      connectorName,
      options
    );
    return poller.pollUntilDone();
  }

  /**
   * Lists all connectors.
   * @param resourceGroup The name of the resource group.
   * @param workbenchName The name of the workbench.
   * @param chamberName The name of the chamber.
   * @param options The options parameters.
   */
  private _list(
    resourceGroup: string,
    workbenchName: string,
    chamberName: string,
    options?: ConnectorsListOptionalParams
  ): Promise<ConnectorsListResponse> {
    return this.client.sendOperationRequest(
      { resourceGroup, workbenchName, chamberName, options },
      listOperationSpec
    );
  }

  /**
   * Add license to an ModSim Workbench connector.
   * @param resourceGroup The name of the resource group
   * @param workbenchName The name of the workbench
   * @param chamberName The name of the chamber.
   * @param name The name of the connector.
   * @param body License key parameters
   * @param options The options parameters.
   */
  async beginLicenseAdd(
    resourceGroup: string,
    workbenchName: string,
    chamberName: string,
    name: string,
    body: LicenseKeyParameters,
    options?: ConnectorsLicenseAddOptionalParams
  ): Promise<SimplePollerLike<OperationState<void>, void>> {
    const directSendOperation = async (
      args: coreClient.OperationArguments,
      spec: coreClient.OperationSpec
    ): Promise<void> => {
      return this.client.sendOperationRequest(args, spec);
    };
    const sendOperationFn = async (
      args: coreClient.OperationArguments,
      spec: coreClient.OperationSpec
    ) => {
      let currentRawResponse:
        | coreClient.FullOperationResponse
        | undefined = undefined;
      const providedCallback = args.options?.onResponse;
      const callback: coreClient.RawResponseCallback = (
        rawResponse: coreClient.FullOperationResponse,
        flatResponse: unknown
      ) => {
        currentRawResponse = rawResponse;
        providedCallback?.(rawResponse, flatResponse);
      };
      const updatedArgs = {
        ...args,
        options: {
          ...args.options,
          onResponse: callback
        }
      };
      const flatResponse = await directSendOperation(updatedArgs, spec);
      return {
        flatResponse,
        rawResponse: {
          statusCode: currentRawResponse!.status,
          body: currentRawResponse!.parsedBody,
          headers: currentRawResponse!.headers.toJSON()
        }
      };
    };

    const lro = createLroSpec({
      sendOperationFn,
      args: { resourceGroup, workbenchName, chamberName, name, body, options },
      spec: licenseAddOperationSpec
    });
    const poller = await createHttpPoller<void, OperationState<void>>(lro, {
      restoreFrom: options?.resumeFrom,
      intervalInMs: options?.updateIntervalInMs,
      resourceLocationConfig: "location"
    });
    await poller.poll();
    return poller;
  }

  /**
   * Add license to an ModSim Workbench connector.
   * @param resourceGroup The name of the resource group
   * @param workbenchName The name of the workbench
   * @param chamberName The name of the chamber.
   * @param name The name of the connector.
   * @param body License key parameters
   * @param options The options parameters.
   */
  async beginLicenseAddAndWait(
    resourceGroup: string,
    workbenchName: string,
    chamberName: string,
    name: string,
    body: LicenseKeyParameters,
    options?: ConnectorsLicenseAddOptionalParams
  ): Promise<void> {
    const poller = await this.beginLicenseAdd(
      resourceGroup,
      workbenchName,
      chamberName,
      name,
      body,
      options
    );
    return poller.pollUntilDone();
  }

  /**
   * ListNext
   * @param resourceGroup The name of the resource group.
   * @param workbenchName The name of the workbench.
   * @param chamberName The name of the chamber.
   * @param nextLink The nextLink from the previous successful call to the List method.
   * @param options The options parameters.
   */
  private _listNext(
    resourceGroup: string,
    workbenchName: string,
    chamberName: string,
    nextLink: string,
    options?: ConnectorsListNextOptionalParams
  ): Promise<ConnectorsListNextResponse> {
    return this.client.sendOperationRequest(
      { resourceGroup, workbenchName, chamberName, nextLink, options },
      listNextOperationSpec
    );
  }
}
// Operation Specifications
const serializer = coreClient.createSerializer(Mappers, /* isXml */ false);

const startOperationSpec: coreClient.OperationSpec = {
  path:
    "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroup}/providers/Microsoft.ModSimWorkbench/workbenches/{workbenchName}/chambers/{chamberName}/connectors/{name}/start",
  httpMethod: "POST",
  responses: {
    200: {},
    201: {},
    202: {},
    204: {},
    default: {
      bodyMapper: Mappers.ErrorResponse
    }
  },
  queryParameters: [Parameters.apiVersion],
  urlParameters: [
    Parameters.$host,
    Parameters.subscriptionId,
    Parameters.resourceGroup,
    Parameters.name,
    Parameters.workbenchName,
    Parameters.chamberName
  ],
  headerParameters: [Parameters.accept],
  serializer
};
const stopOperationSpec: coreClient.OperationSpec = {
  path:
    "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroup}/providers/Microsoft.ModSimWorkbench/workbenches/{workbenchName}/chambers/{chamberName}/connectors/{name}/stop",
  httpMethod: "POST",
  responses: {
    200: {},
    201: {},
    202: {},
    204: {},
    default: {
      bodyMapper: Mappers.ErrorResponse
    }
  },
  queryParameters: [Parameters.apiVersion],
  urlParameters: [
    Parameters.$host,
    Parameters.subscriptionId,
    Parameters.resourceGroup,
    Parameters.name,
    Parameters.workbenchName,
    Parameters.chamberName
  ],
  headerParameters: [Parameters.accept],
  serializer
};
const restartOperationSpec: coreClient.OperationSpec = {
  path:
    "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroup}/providers/Microsoft.ModSimWorkbench/workbenches/{workbenchName}/chambers/{chamberName}/connectors/{name}/restart",
  httpMethod: "POST",
  responses: {
    200: {},
    201: {},
    202: {},
    204: {},
    default: {
      bodyMapper: Mappers.ErrorResponse
    }
  },
  queryParameters: [Parameters.apiVersion],
  urlParameters: [
    Parameters.$host,
    Parameters.subscriptionId,
    Parameters.resourceGroup,
    Parameters.name,
    Parameters.workbenchName,
    Parameters.chamberName
  ],
  headerParameters: [Parameters.accept],
  serializer
};
const createOrUpdateOperationSpec: coreClient.OperationSpec = {
  path:
    "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroup}/providers/Microsoft.ModSimWorkbench/workbenches/{workbenchName}/chambers/{chamberName}/connectors/{connectorName}",
  httpMethod: "PUT",
  responses: {
    200: {
      bodyMapper: Mappers.Connector
    },
    201: {
      bodyMapper: Mappers.Connector
    },
    202: {
      bodyMapper: Mappers.Connector
    },
    204: {
      bodyMapper: Mappers.Connector
    },
    default: {
      bodyMapper: Mappers.ErrorResponse
    }
  },
  requestBody: Parameters.parameters,
  queryParameters: [Parameters.apiVersion],
  urlParameters: [
    Parameters.$host,
    Parameters.subscriptionId,
    Parameters.resourceGroup,
    Parameters.workbenchName,
    Parameters.chamberName,
    Parameters.connectorName
  ],
  headerParameters: [Parameters.accept, Parameters.contentType],
  mediaType: "json",
  serializer
};
const updateOperationSpec: coreClient.OperationSpec = {
  path:
    "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroup}/providers/Microsoft.ModSimWorkbench/workbenches/{workbenchName}/chambers/{chamberName}/connectors/{connectorName}",
  httpMethod: "PATCH",
  responses: {
    200: {
      bodyMapper: Mappers.Connector
    },
    201: {
      bodyMapper: Mappers.Connector
    },
    default: {
      bodyMapper: Mappers.ErrorResponse
    }
  },
  requestBody: Parameters.parameters,
  queryParameters: [Parameters.apiVersion],
  urlParameters: [
    Parameters.$host,
    Parameters.subscriptionId,
    Parameters.resourceGroup,
    Parameters.workbenchName,
    Parameters.chamberName,
    Parameters.connectorName
  ],
  headerParameters: [Parameters.accept, Parameters.contentType],
  mediaType: "json",
  serializer
};
const getOperationSpec: coreClient.OperationSpec = {
  path:
    "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroup}/providers/Microsoft.ModSimWorkbench/workbenches/{workbenchName}/chambers/{chamberName}/connectors/{connectorName}",
  httpMethod: "GET",
  responses: {
    200: {
      bodyMapper: Mappers.Connector
    },
    default: {
      bodyMapper: Mappers.ErrorResponse
    }
  },
  queryParameters: [Parameters.apiVersion],
  urlParameters: [
    Parameters.$host,
    Parameters.subscriptionId,
    Parameters.resourceGroup,
    Parameters.workbenchName,
    Parameters.chamberName,
    Parameters.connectorName
  ],
  headerParameters: [Parameters.accept],
  serializer
};
const deleteOperationSpec: coreClient.OperationSpec = {
  path:
    "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroup}/providers/Microsoft.ModSimWorkbench/workbenches/{workbenchName}/chambers/{chamberName}/connectors/{connectorName}",
  httpMethod: "DELETE",
  responses: {
    200: {
      bodyMapper: Mappers.Connector
    },
    201: {
      bodyMapper: Mappers.Connector
    },
    202: {
      bodyMapper: Mappers.Connector
    },
    204: {
      bodyMapper: Mappers.Connector
    },
    default: {
      bodyMapper: Mappers.ErrorResponse
    }
  },
  queryParameters: [Parameters.apiVersion],
  urlParameters: [
    Parameters.$host,
    Parameters.subscriptionId,
    Parameters.resourceGroup,
    Parameters.workbenchName,
    Parameters.chamberName,
    Parameters.connectorName
  ],
  headerParameters: [Parameters.accept],
  serializer
};
const listOperationSpec: coreClient.OperationSpec = {
  path:
    "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroup}/providers/Microsoft.ModSimWorkbench/workbenches/{workbenchName}/chambers/{chamberName}/connectors",
  httpMethod: "GET",
  responses: {
    200: {
      bodyMapper: Mappers.ConnectorPageableList
    },
    default: {
      bodyMapper: Mappers.ErrorResponse
    }
  },
  queryParameters: [Parameters.apiVersion],
  urlParameters: [
    Parameters.$host,
    Parameters.subscriptionId,
    Parameters.resourceGroup,
    Parameters.workbenchName,
    Parameters.chamberName
  ],
  headerParameters: [Parameters.accept],
  serializer
};
const licenseAddOperationSpec: coreClient.OperationSpec = {
  path:
    "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroup}/providers/Microsoft.ModSimWorkbench/workbenches/{workbenchName}/chambers/{chamberName}/connectors/{name}/licenseAdd",
  httpMethod: "POST",
  responses: {
    200: {},
    201: {},
    202: {},
    204: {},
    default: {
      bodyMapper: Mappers.ErrorResponse
    }
  },
  requestBody: Parameters.body1,
  queryParameters: [Parameters.apiVersion],
  urlParameters: [
    Parameters.$host,
    Parameters.subscriptionId,
    Parameters.resourceGroup,
    Parameters.name,
    Parameters.workbenchName,
    Parameters.chamberName
  ],
  headerParameters: [Parameters.accept, Parameters.contentType],
  mediaType: "json",
  serializer
};
const listNextOperationSpec: coreClient.OperationSpec = {
  path: "{nextLink}",
  httpMethod: "GET",
  responses: {
    200: {
      bodyMapper: Mappers.ConnectorPageableList
    },
    default: {
      bodyMapper: Mappers.ErrorResponse
    }
  },
  urlParameters: [
    Parameters.$host,
    Parameters.subscriptionId,
    Parameters.resourceGroup,
    Parameters.nextLink,
    Parameters.workbenchName,
    Parameters.chamberName
  ],
  headerParameters: [Parameters.accept],
  serializer
};
