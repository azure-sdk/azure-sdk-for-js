/*
 * Copyright (c) Microsoft Corporation.
 * Licensed under the MIT License.
 *
 * Code generated by Microsoft (R) AutoRest Code Generator.
 * Changes may cause incorrect behavior and will be lost if the code is regenerated.
 */

import { PagedAsyncIterableIterator, PageSettings } from "@azure/core-paging";
import { setContinuationToken } from "../pagingHelper";
import { Files } from "../operationsInterfaces";
import * as coreClient from "@azure/core-client";
import * as Mappers from "../models/mappers";
import * as Parameters from "../models/parameters";
import { MicrosoftModSimWorkbench } from "../microsoftModSimWorkbench";
import {
  SimplePollerLike,
  OperationState,
  createHttpPoller
} from "@azure/core-lro";
import { createLroSpec } from "../lroImpl";
import {
  File,
  FilesListNextOptionalParams,
  FilesListOptionalParams,
  FilesListResponse,
  FilesGetOptionalParams,
  FilesGetResponse,
  FilesDownloadRequestOptionalParams,
  FilesDownloadRequestResponse,
  FilesListNextResponse
} from "../models";

/// <reference lib="esnext.asynciterable" />
/** Class containing Files operations. */
export class FilesImpl implements Files {
  private readonly client: MicrosoftModSimWorkbench;

  /**
   * Initialize a new instance of the class Files class.
   * @param client Reference to the service client
   */
  constructor(client: MicrosoftModSimWorkbench) {
    this.client = client;
  }

  /**
   * Lists all files.
   * @param resourceGroupName The name of the resource group. The name is case insensitive.
   * @param workbenchName The name of the workbench.
   * @param chamberName The name of the chamber.
   * @param options The options parameters.
   */
  public list(
    resourceGroupName: string,
    workbenchName: string,
    chamberName: string,
    options?: FilesListOptionalParams
  ): PagedAsyncIterableIterator<File> {
    const iter = this.listPagingAll(
      resourceGroupName,
      workbenchName,
      chamberName,
      options
    );
    return {
      next() {
        return iter.next();
      },
      [Symbol.asyncIterator]() {
        return this;
      },
      byPage: (settings?: PageSettings) => {
        if (settings?.maxPageSize) {
          throw new Error("maxPageSize is not supported by this operation.");
        }
        return this.listPagingPage(
          resourceGroupName,
          workbenchName,
          chamberName,
          options,
          settings
        );
      }
    };
  }

  private async *listPagingPage(
    resourceGroupName: string,
    workbenchName: string,
    chamberName: string,
    options?: FilesListOptionalParams,
    settings?: PageSettings
  ): AsyncIterableIterator<File[]> {
    let result: FilesListResponse;
    let continuationToken = settings?.continuationToken;
    if (!continuationToken) {
      result = await this._list(
        resourceGroupName,
        workbenchName,
        chamberName,
        options
      );
      let page = result.value || [];
      continuationToken = result.nextLink;
      setContinuationToken(page, continuationToken);
      yield page;
    }
    while (continuationToken) {
      result = await this._listNext(
        resourceGroupName,
        workbenchName,
        chamberName,
        continuationToken,
        options
      );
      continuationToken = result.nextLink;
      let page = result.value || [];
      setContinuationToken(page, continuationToken);
      yield page;
    }
  }

  private async *listPagingAll(
    resourceGroupName: string,
    workbenchName: string,
    chamberName: string,
    options?: FilesListOptionalParams
  ): AsyncIterableIterator<File> {
    for await (const page of this.listPagingPage(
      resourceGroupName,
      workbenchName,
      chamberName,
      options
    )) {
      yield* page;
    }
  }

  /**
   * Get a specific ModSim Workbench chamber data pipeline file resource.
   * @param resourceGroupName The name of the resource group. The name is case insensitive.
   * @param workbenchName the workbench name.
   * @param chamberName the chamber name.
   * @param fileName the file name.
   * @param options The options parameters.
   */
  get(
    resourceGroupName: string,
    workbenchName: string,
    chamberName: string,
    fileName: string,
    options?: FilesGetOptionalParams
  ): Promise<FilesGetResponse> {
    return this.client.sendOperationRequest(
      { resourceGroupName, workbenchName, chamberName, fileName, options },
      getOperationSpec
    );
  }

  /**
   * Request to download files from an ModSim Workbench chamber data pipeline.
   * @param resourceGroupName The name of the resource group. The name is case insensitive.
   * @param workbenchName the workbench name.
   * @param chamberName the chamber name.
   * @param fileName the file name.
   * @param options The options parameters.
   */
  async beginDownloadRequest(
    resourceGroupName: string,
    workbenchName: string,
    chamberName: string,
    fileName: string,
    options?: FilesDownloadRequestOptionalParams
  ): Promise<
    SimplePollerLike<
      OperationState<FilesDownloadRequestResponse>,
      FilesDownloadRequestResponse
    >
  > {
    const directSendOperation = async (
      args: coreClient.OperationArguments,
      spec: coreClient.OperationSpec
    ): Promise<FilesDownloadRequestResponse> => {
      return this.client.sendOperationRequest(args, spec);
    };
    const sendOperationFn = async (
      args: coreClient.OperationArguments,
      spec: coreClient.OperationSpec
    ) => {
      let currentRawResponse:
        | coreClient.FullOperationResponse
        | undefined = undefined;
      const providedCallback = args.options?.onResponse;
      const callback: coreClient.RawResponseCallback = (
        rawResponse: coreClient.FullOperationResponse,
        flatResponse: unknown
      ) => {
        currentRawResponse = rawResponse;
        providedCallback?.(rawResponse, flatResponse);
      };
      const updatedArgs = {
        ...args,
        options: {
          ...args.options,
          onResponse: callback
        }
      };
      const flatResponse = await directSendOperation(updatedArgs, spec);
      return {
        flatResponse,
        rawResponse: {
          statusCode: currentRawResponse!.status,
          body: currentRawResponse!.parsedBody,
          headers: currentRawResponse!.headers.toJSON()
        }
      };
    };

    const lro = createLroSpec({
      sendOperationFn,
      args: {
        resourceGroupName,
        workbenchName,
        chamberName,
        fileName,
        options
      },
      spec: downloadRequestOperationSpec
    });
    const poller = await createHttpPoller<
      FilesDownloadRequestResponse,
      OperationState<FilesDownloadRequestResponse>
    >(lro, {
      restoreFrom: options?.resumeFrom,
      intervalInMs: options?.updateIntervalInMs,
      resourceLocationConfig: "location"
    });
    await poller.poll();
    return poller;
  }

  /**
   * Request to download files from an ModSim Workbench chamber data pipeline.
   * @param resourceGroupName The name of the resource group. The name is case insensitive.
   * @param workbenchName the workbench name.
   * @param chamberName the chamber name.
   * @param fileName the file name.
   * @param options The options parameters.
   */
  async beginDownloadRequestAndWait(
    resourceGroupName: string,
    workbenchName: string,
    chamberName: string,
    fileName: string,
    options?: FilesDownloadRequestOptionalParams
  ): Promise<FilesDownloadRequestResponse> {
    const poller = await this.beginDownloadRequest(
      resourceGroupName,
      workbenchName,
      chamberName,
      fileName,
      options
    );
    return poller.pollUntilDone();
  }

  /**
   * Lists all files.
   * @param resourceGroupName The name of the resource group. The name is case insensitive.
   * @param workbenchName The name of the workbench.
   * @param chamberName The name of the chamber.
   * @param options The options parameters.
   */
  private _list(
    resourceGroupName: string,
    workbenchName: string,
    chamberName: string,
    options?: FilesListOptionalParams
  ): Promise<FilesListResponse> {
    return this.client.sendOperationRequest(
      { resourceGroupName, workbenchName, chamberName, options },
      listOperationSpec
    );
  }

  /**
   * ListNext
   * @param resourceGroupName The name of the resource group. The name is case insensitive.
   * @param workbenchName The name of the workbench.
   * @param chamberName The name of the chamber.
   * @param nextLink The nextLink from the previous successful call to the List method.
   * @param options The options parameters.
   */
  private _listNext(
    resourceGroupName: string,
    workbenchName: string,
    chamberName: string,
    nextLink: string,
    options?: FilesListNextOptionalParams
  ): Promise<FilesListNextResponse> {
    return this.client.sendOperationRequest(
      { resourceGroupName, workbenchName, chamberName, nextLink, options },
      listNextOperationSpec
    );
  }
}
// Operation Specifications
const serializer = coreClient.createSerializer(Mappers, /* isXml */ false);

const getOperationSpec: coreClient.OperationSpec = {
  path:
    "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.ModSimWorkbench/workbenches/{workbenchName}/chambers/{chamberName}/files/{fileName}",
  httpMethod: "GET",
  responses: {
    200: {
      bodyMapper: Mappers.File
    },
    default: {
      bodyMapper: Mappers.ErrorResponse
    }
  },
  queryParameters: [Parameters.apiVersion],
  urlParameters: [
    Parameters.$host,
    Parameters.subscriptionId,
    Parameters.workbenchName,
    Parameters.chamberName,
    Parameters.resourceGroupName,
    Parameters.fileName
  ],
  headerParameters: [Parameters.accept],
  serializer
};
const downloadRequestOperationSpec: coreClient.OperationSpec = {
  path:
    "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.ModSimWorkbench/workbenches/{workbenchName}/chambers/{chamberName}/files/{fileName}/downloadRequest",
  httpMethod: "POST",
  responses: {
    200: {
      bodyMapper: Mappers.DownloadRequest
    },
    201: {
      bodyMapper: Mappers.DownloadRequest
    },
    202: {
      bodyMapper: Mappers.DownloadRequest
    },
    204: {
      bodyMapper: Mappers.DownloadRequest
    },
    default: {
      bodyMapper: Mappers.ErrorResponse
    }
  },
  requestBody: Parameters.body5,
  queryParameters: [Parameters.apiVersion],
  urlParameters: [
    Parameters.$host,
    Parameters.subscriptionId,
    Parameters.workbenchName,
    Parameters.chamberName,
    Parameters.resourceGroupName,
    Parameters.fileName
  ],
  headerParameters: [Parameters.accept, Parameters.contentType],
  mediaType: "json",
  serializer
};
const listOperationSpec: coreClient.OperationSpec = {
  path:
    "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.ModSimWorkbench/workbenches/{workbenchName}/chambers/{chamberName}/files",
  httpMethod: "GET",
  responses: {
    200: {
      bodyMapper: Mappers.FilePageableList
    },
    default: {
      bodyMapper: Mappers.ErrorResponse
    }
  },
  queryParameters: [Parameters.apiVersion],
  urlParameters: [
    Parameters.$host,
    Parameters.subscriptionId,
    Parameters.workbenchName,
    Parameters.chamberName,
    Parameters.resourceGroupName
  ],
  headerParameters: [Parameters.accept],
  serializer
};
const listNextOperationSpec: coreClient.OperationSpec = {
  path: "{nextLink}",
  httpMethod: "GET",
  responses: {
    200: {
      bodyMapper: Mappers.FilePageableList
    },
    default: {
      bodyMapper: Mappers.ErrorResponse
    }
  },
  urlParameters: [
    Parameters.$host,
    Parameters.subscriptionId,
    Parameters.nextLink,
    Parameters.workbenchName,
    Parameters.chamberName,
    Parameters.resourceGroupName
  ],
  headerParameters: [Parameters.accept],
  serializer
};
