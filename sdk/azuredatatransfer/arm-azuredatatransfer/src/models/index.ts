/*
 * Copyright (c) Microsoft Corporation.
 * Licensed under the MIT License.
 *
 * Code generated by Microsoft (R) AutoRest Code Generator.
 * Changes may cause incorrect behavior and will be lost if the code is regenerated.
 */

import * as coreClient from "@azure/core-client";

/** The request used to list approved schemas within the resource provider. */
export interface ListApprovedSchemasRequest {
  /** The name of the pipeline to filter approved schemas. */
  pipeline?: string;
  /** The direction pipeline to filter approved schemas. */
  direction?: Direction;
}

/** The schemas list result. */
export interface SchemasListResult {
  /** Schemas array. */
  value?: Schema[];
}

/** The schema object. */
export interface Schema {
  /** ID associated with this schema */
  id?: string;
  /** Connection ID associated with this schema */
  connectionId?: string;
  /** Status of the schema */
  status?: SchemaStatus;
  /** Name of the schema */
  name?: string;
  /** Content of the schema */
  content?: string;
  /** The direction of the schema. */
  direction?: Direction;
  /** Uri containing SAS token for the zipped schema */
  schemaUri?: string;
  /** The Schema Type */
  schemaType?: SchemaType;
}

/** Common error response for all Azure Resource Manager APIs to return error details for failed operations. (This also follows the OData error response format.). */
export interface ErrorResponse {
  /** The error object. */
  error?: ErrorDetail;
}

/** The error detail. */
export interface ErrorDetail {
  /**
   * The error code.
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly code?: string;
  /**
   * The error message.
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly message?: string;
  /**
   * The error target.
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly target?: string;
  /**
   * The error details.
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly details?: ErrorDetail[];
  /**
   * The error additional info.
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly additionalInfo?: ErrorAdditionalInfo[];
}

/** The resource management error additional info. */
export interface ErrorAdditionalInfo {
  /**
   * The additional info type.
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly type?: string;
  /**
   * The additional info.
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly info?: Record<string, unknown>;
}

/** A list of REST API operations supported by an Azure Resource Provider. It contains an URL link to get the next set of results. */
export interface OperationListResult {
  /**
   * List of operations supported by the resource provider
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly value?: Operation[];
  /**
   * URL to get the next set of operation list results (if there are any).
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly nextLink?: string;
}

/** Details of a REST API operation, returned from the Resource Provider Operations API */
export interface Operation {
  /**
   * The name of the operation, as per Resource-Based Access Control (RBAC). Examples: "Microsoft.Compute/virtualMachines/write", "Microsoft.Compute/virtualMachines/capture/action"
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly name?: string;
  /**
   * Whether the operation applies to data-plane. This is "true" for data-plane operations and "false" for ARM/control-plane operations.
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly isDataAction?: boolean;
  /** Localized display information for this particular operation. */
  display?: OperationDisplay;
  /**
   * The intended executor of the operation; as in Resource Based Access Control (RBAC) and audit logs UX. Default value is "user,system"
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly origin?: Origin;
  /**
   * Enum. Indicates the action type. "Internal" refers to actions that are for internal only APIs.
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly actionType?: ActionType;
}

/** Localized display information for this particular operation. */
export interface OperationDisplay {
  /**
   * The localized friendly form of the resource provider name, e.g. "Microsoft Monitoring Insights" or "Microsoft Compute".
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly provider?: string;
  /**
   * The localized friendly name of the resource type related to this operation. E.g. "Virtual Machines" or "Job Schedule Collections".
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly resource?: string;
  /**
   * The concise, localized friendly name for the operation; suitable for dropdowns. E.g. "Create or Update Virtual Machine", "Restart Virtual Machine".
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly operation?: string;
  /**
   * The short, localized friendly description of the operation; suitable for tool tips and detailed views.
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly description?: string;
}

/** Result of the schema validation. */
export interface ValidateSchemaResult {
  /** Validation status of the schema */
  status?: ValidateSchemaStatus;
  /** Message describing the schema validation */
  message?: string;
}

/** The response of a Connection list operation. */
export interface ConnectionListResult {
  /** The Connection items on this page */
  value: Connection[];
  /** The link to the next page of items */
  nextLink?: string;
}

/** Properties of connection */
export interface ConnectionProperties {
  /** Pipeline to use to transfer data */
  pipeline: string;
  /** Direction of data movement */
  direction?: Direction;
  /** Justification for the connection request */
  justification?: string;
  /**
   * Status of the connection
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly status?: ConnectionStatus;
  /**
   * Force disablement status of the current connection
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly forceDisabledStatus?: ForceDisabledStatus[];
  /**
   * Reason for status
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly statusReason?: string;
  /**
   * Link status of the current connection
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly linkStatus?: LinkStatus;
  /**
   * Resource ID of the linked connection
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly linkedConnectionId?: string;
  /** The flow types being requested for this connection */
  flowTypes?: FlowType[];
  /** Requirement ID of the connection */
  requirementId?: string;
  /** Subscription ID to link cloud subscriptions together */
  remoteSubscriptionId?: string;
  /**
   * Approver of this connection request
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly approver?: string;
  /** PIN to link requests together */
  pin?: string;
  /**
   * The timestamp that this connection request was submitted at
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly dateSubmitted?: Date;
  /** The primary contact for this connection request */
  primaryContact?: string;
  /** The secondary contacts for this connection request */
  secondaryContacts?: string[];
  /**
   * Provisioning state of the connection
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly provisioningState?: ProvisioningState;
  /** The policies for this connection */
  policies?: string[];
  /** The schemas for this connection */
  schemas?: Schema[];
  /** The schema URIs for this connection */
  schemaUris?: string[];
}

/** Managed service identity (system assigned and/or user assigned identities) */
export interface ManagedServiceIdentity {
  /**
   * The service principal ID of the system assigned identity. This property will only be provided for a system assigned identity.
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly principalId?: string;
  /**
   * The tenant ID of the system assigned identity. This property will only be provided for a system assigned identity.
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly tenantId?: string;
  /** Type of managed service identity (where both SystemAssigned and UserAssigned types are allowed). */
  type: ManagedServiceIdentityType;
  /** The set of user assigned identities associated with the resource. The userAssignedIdentities dictionary keys will be ARM resource ids in the form: '/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.ManagedIdentity/userAssignedIdentities/{identityName}. The dictionary values can be empty objects ({}) in requests. */
  userAssignedIdentities?: {
    [propertyName: string]: UserAssignedIdentity | null;
  };
}

/** User assigned identity properties */
export interface UserAssignedIdentity {
  /**
   * The principal ID of the assigned identity.
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly principalId?: string;
  /**
   * The client ID of the assigned identity.
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly clientId?: string;
}

/** Common fields that are returned in the response for all Azure Resource Manager resources */
export interface Resource {
  /**
   * Fully qualified resource ID for the resource. E.g. "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}"
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly id?: string;
  /**
   * The name of the resource
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly name?: string;
  /**
   * The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts"
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly type?: string;
  /**
   * Azure Resource Manager metadata containing createdBy and modifiedBy information.
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly systemData?: SystemData;
}

/** Metadata pertaining to creation and last modification of the resource. */
export interface SystemData {
  /** The identity that created the resource. */
  createdBy?: string;
  /** The type of identity that created the resource. */
  createdByType?: CreatedByType;
  /** The timestamp of resource creation (UTC). */
  createdAt?: Date;
  /** The identity that last modified the resource. */
  lastModifiedBy?: string;
  /** The type of identity that last modified the resource. */
  lastModifiedByType?: CreatedByType;
  /** The timestamp of resource last modification (UTC) */
  lastModifiedAt?: Date;
}

/** The response of a Pipeline list operation. */
export interface PipelineListResult {
  /** The Pipeline items on this page */
  value: Pipeline[];
  /** The link to the next page of items */
  nextLink?: string;
}

/** Properties of pipeline */
export interface PipelineProperties {
  /** Remote cloud of the data to be transferred or received */
  remoteCloud: string;
  /** Display name of this pipeline */
  displayName?: string;
  /**
   * Connections associated with pipeline
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly connections?: PipelineConnection[];
  /** Subscribers of this resource */
  subscribers?: Subscriber[];
  /**
   * Provisioning state of the pipeline
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly provisioningState?: ProvisioningState;
  /** The policies for this pipeline */
  policies?: string[];
  /** The flow types allowed for this pipeline */
  flowTypes?: FlowType[];
  /** The flow types that are disabled for this pipeline */
  disabledFlowTypes?: FlowType[];
  /** Quarantine Download Storage Account */
  quarantineDownloadStorageAccount?: string;
  /** Quarantine Download Storage Container */
  quarantineDownloadStorageContainer?: string;
  /** Status of the current pipeline */
  status?: PipelineStatus;
}

/** Connection body inside a pipeline */
export interface PipelineConnection {
  /** Describes unknown properties. The value of an unknown property can be of "any" type. */
  [property: string]: any;
  /** Connection id inside pipeline */
  id: string;
  /**
   * Connection name inside pipeline
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly name?: string;
  /**
   * Connection type inside pipeline
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly type?: string;
  /**
   * Connection location inside pipeline
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly location?: string;
  /**
   * Connection etag inside pipeline
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly etag?: string;
  /**
   * Metadata pertaining to creation and last modification of the resource.
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly systemData?: SystemData;
  /** Connection properties inside pipeline */
  properties?: PipelineConnectionProperties;
}

/** Connection properties inside pipeline */
export interface PipelineConnectionProperties {
  /** Describes unknown properties. The value of an unknown property can be of "any" type. */
  [property: string]: any;
  /** Internal metadata of the connection inside pipeline. */
  internalMetadata?: InternalMetadataProperties;
}

/** Internal metadata of the connection inside pipeline. */
export interface InternalMetadataProperties {
  /** Describes unknown properties. The value of an unknown property can be of "any" type. */
  [property: string]: any;
  /** Operation status associated with the last patch request */
  operationStatus?: OperationStatusProperties;
  /**
   * User that last set the approved status for this connection
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly statusSetBy?: string;
}

/** Operation status associated with the last patch request */
export interface OperationStatusProperties {
  /**
   * Operation status for the last patch request for this connection.
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly status?: OperationStatusEnum;
  /**
   * Operation status ID of the last patch request for this connection.
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly id?: string;
  /**
   * Message for the operation for the last patch request for this connection.
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly message?: string;
}

/** An individual that would like to subscribe to events that occur on a pipeline. */
export interface Subscriber {
  /** Email of the subscriber */
  email?: string;
  /** Number specifying what notifications to receive */
  notifications?: number;
}

/** The connections resource patch definition. */
export interface ConnectionsPatch {
  /** The managed service identities assigned to this resource. */
  identity?: ManagedServiceIdentity;
  /** Resource tags. */
  tags?: { [propertyName: string]: string };
}

/** The response of a Flow list operation. */
export interface FlowListResult {
  /** The Flow items on this page */
  value: Flow[];
  /** The link to the next page of items */
  nextLink?: string;
}

/** Properties of flow */
export interface FlowProperties {
  /** The connection associated with this flow */
  connection?: SelectedResource;
  /**
   * Dataflow GUID associated with this flow
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly flowId?: string;
  /** AME, PME, or TORUS only! AKV Chain Containing SAS Token */
  keyVaultUri?: string;
  /**
   * Link status of the current flow
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly linkStatus?: LinkStatus;
  /**
   * Resource ID of the linked flow
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly linkedFlowId?: string;
  /** Status of the current flow */
  status?: FlowStatus;
  /**
   * Force disablement status of the current flow
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly forceDisabledStatus?: ForceDisabledStatus[];
  /** Storage Account */
  storageAccountName?: string;
  /** Storage Account ID */
  storageAccountId?: string;
  /** Storage Container Name */
  storageContainerName?: string;
  /** Storage Table Name */
  storageTableName?: string;
  /** Service Bus Queue ID */
  serviceBusQueueId?: string;
  /** The flow type for this flow */
  flowType?: FlowType;
  /** Transfer Storage Blobs or Tables */
  dataType?: DataType;
  /**
   * Provisioning state of the flow
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly provisioningState?: ProvisioningState;
  /** The policies for this flow */
  policies?: string[];
  /** The selected schema for this flow */
  schema?: Schema;
  /** The messaging options for this flow */
  messagingOptions?: MessagingOptions;
  /** The API Flow configuration options for Azure Data Transfer API Flow type. */
  apiFlowOptions?: ApiFlowOptions;
  /** The URI to the customer managed key for this flow */
  customerManagedKeyVaultUri?: string;
  /** The flow stream identifier */
  streamId?: string;
  /** The protocol of the stream */
  streamProtocol?: StreamProtocol;
  /** The latency of the stream in milliseconds */
  streamLatency?: number;
  /** The passphrase used for SRT streams */
  passphrase?: string;
  /** The source IP address and CIDR ranges of the stream */
  sourceAddresses?: StreamSourceAddresses;
  /** The destination endpoints of the stream */
  destinationEndpoints?: string[];
  /** The destination endpoint ports of the stream */
  destinationEndpointPorts?: number[];
  /** Event Hub ID */
  eventHubId?: string;
  /** Event Hub Consumer Group */
  consumerGroup?: string;
}

/** A resource selected from ARM */
export interface SelectedResource {
  /** Name of the connection */
  name?: string;
  /** Id of the connection */
  id: string;
  /** Location of the connection */
  location?: string;
  /** Name of the subscription with the connection */
  subscriptionName?: string;
}

/** The option associated with messaging flows. */
export interface MessagingOptions {
  /** Billing tier for this messaging flow */
  billingTier?: FlowBillingTier;
}

/** Properties specific to API Flow Type */
export interface ApiFlowOptions {
  /** Remote host to which communication needs to be made */
  remoteEndpoint?: string;
  /** Unique CNAME to represent the Azure Data Transfer API Flow instance */
  cname?: string;
  /** Remote Calling Mode in the Azure Data Transfer API Flow, which describes how the API Flow will be invoked */
  apiMode?: ApiMode;
  /** Flag for if Azure Data Transfer API Flow should extract the user token */
  identityTranslation?: IdentityTranslation;
  /** Sender's app user assigned Manage Identity client ID */
  senderClientId?: string;
  /** Remote stub app registration Client ID */
  remoteCallingModeClientId?: string;
  /** Optional field to override the audience of the remote endpoint */
  audienceOverride?: string;
}

/** The source IP address and CIDR ranges of the stream */
export interface StreamSourceAddresses {
  /** A source IP address or CIDR range */
  sourceAddresses?: string[];
}

/** Plan for the resource. */
export interface Plan {
  /** A user defined name of the 3rd Party Artifact that is being procured. */
  name: string;
  /** The publisher of the 3rd Party Artifact that is being bought. E.g. NewRelic */
  publisher: string;
  /** The 3rd Party artifact that is being procured. E.g. NewRelic. Product maps to the OfferID specified for the artifact at the time of Data Market onboarding. */
  product: string;
  /** A publisher provided promotion code as provisioned in Data Market for the said product/artifact. */
  promotionCode?: string;
  /** The version of the desired product/artifact. */
  version?: string;
}

/** The flows resource patch definition. */
export interface FlowsPatch {
  /** The managed service identities assigned to this resource. */
  identity?: ManagedServiceIdentity;
  /** Resource tags. */
  tags?: { [propertyName: string]: string };
}

/** List of destination endpoint ports for the flow stream */
export interface GetDestinationEndpointPortsResult {
  /** The destination endpoint port for the flow stream */
  ports?: number[];
}

/** List of destination endpoints for the flow stream */
export interface GetDestinationEndpointsResult {
  /** The destination endpoints for the flow stream */
  endpoints?: string[];
}

/** The connection string for the specified streaming flow */
export interface GetStreamConnectionStringResult {
  /** The connection string for the specified streaming flow */
  connectionString?: string;
}

/** The resource to reference. */
export interface ResourceBody {
  /** ID of the resource. */
  id: string;
  /** Reason for resource operation. */
  statusReason?: string;
}

/** Set the destination endpoint ports for the specified stream ID */
export interface SetDestinationEndpointPorts {
  /** The specified stream ID destination endpoint ports */
  ports?: number[];
}

/** Set the destination endpoints for the specified stream ID */
export interface SetDestinationEndpoints {
  /** The specified stream ID destination endpoints */
  endpoints?: string[];
}

/** Set the passphrase used for SRT streams */
export interface SetStreamPassphrase {
  /** The passphrase used for SRT streams */
  value?: string;
}

/** Set the source addresses for the specified stream ID */
export interface SetSourceAddresses {
  /** Source addresses */
  values?: string[];
}

/** The response of a PendingConnection list operation. */
export interface PendingConnectionListResult {
  /** The PendingConnection items on this page */
  value: PendingConnection[];
  /** The link to the next page of items */
  nextLink?: string;
}

/** Pending connection object */
export interface PendingConnection {
  /** Pipeline to use to transfer data */
  pipeline: string;
  /** Direction of data movement */
  direction?: Direction;
  /** Justification for the connection request */
  justification?: string;
  /**
   * Status of the connection
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly status?: ConnectionStatus;
  /**
   * Force disablement status of the current connection
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly forceDisabledStatus?: ForceDisabledStatus[];
  /**
   * Reason for status
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly statusReason?: string;
  /**
   * Link status of the current connection
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly linkStatus?: LinkStatus;
  /**
   * Resource ID of the linked connection
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly linkedConnectionId?: string;
  /** The flow types being requested for this connection */
  flowTypes?: FlowType[];
  /** Requirement ID of the connection */
  requirementId?: string;
  /** Subscription ID to link cloud subscriptions together */
  remoteSubscriptionId?: string;
  /**
   * Approver of this connection request
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly approver?: string;
  /** PIN to link requests together */
  pin?: string;
  /**
   * The timestamp that this connection request was submitted at
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly dateSubmitted?: Date;
  /** The primary contact for this connection request */
  primaryContact?: string;
  /** The secondary contacts for this connection request */
  secondaryContacts?: string[];
  /**
   * Provisioning state of the connection
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly provisioningState?: ProvisioningState;
  /** The policies for this connection */
  policies?: string[];
  /** The schemas for this connection */
  schemas?: Schema[];
  /** The schema URIs for this connection */
  schemaUris?: string[];
  /** The resource-specific properties for this resource. */
  properties?: Connection;
  /** Resource tags. */
  tags?: { [propertyName: string]: string };
  /** The geo-location where the resource lives */
  location: string;
  /**
   * Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly id?: string;
  /**
   * The name of the resource
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly name?: string;
  /**
   * The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts"
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly type?: string;
  /**
   * Azure Resource Manager metadata containing createdBy and modifiedBy information.
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly systemData?: SystemData;
  /**
   * Subscription ID of the pending connection.
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly subscriptionId?: string;
}

/** The response of a PendingFlow list operation. */
export interface PendingFlowListResult {
  /** The PendingFlow items on this page */
  value: PendingFlow[];
  /** The link to the next page of items */
  nextLink?: string;
}

/** Pending flow object */
export interface PendingFlow {
  /** The connection associated with this flow */
  connection?: SelectedResource;
  /**
   * Dataflow GUID associated with this flow
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly flowId?: string;
  /** AME, PME, or TORUS only! AKV Chain Containing SAS Token */
  keyVaultUri?: string;
  /**
   * Link status of the current flow
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly linkStatus?: LinkStatus;
  /**
   * Resource ID of the linked flow
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly linkedFlowId?: string;
  /** Status of the current flow */
  status?: FlowStatus;
  /**
   * Force disablement status of the current flow
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly forceDisabledStatus?: ForceDisabledStatus[];
  /** Storage Account */
  storageAccountName?: string;
  /** Storage Account ID */
  storageAccountId?: string;
  /** Storage Container Name */
  storageContainerName?: string;
  /** Storage Table Name */
  storageTableName?: string;
  /** Service Bus Queue ID */
  serviceBusQueueId?: string;
  /** The flow type for this flow */
  flowType?: FlowType;
  /** Transfer Storage Blobs or Tables */
  dataType?: DataType;
  /**
   * Provisioning state of the flow
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly provisioningState?: ProvisioningState;
  /** The policies for this flow */
  policies?: string[];
  /** The selected schema for this flow */
  schema?: Schema;
  /** The messaging options for this flow */
  messagingOptions?: MessagingOptions;
  /** The API Flow configuration options for Azure Data Transfer API Flow type. */
  apiFlowOptions?: ApiFlowOptions;
  /** The URI to the customer managed key for this flow */
  customerManagedKeyVaultUri?: string;
  /** The flow stream identifier */
  streamId?: string;
  /** The protocol of the stream */
  streamProtocol?: StreamProtocol;
  /** The latency of the stream in milliseconds */
  streamLatency?: number;
  /** The passphrase used for SRT streams */
  passphrase?: string;
  /** The source IP address and CIDR ranges of the stream */
  sourceAddresses?: StreamSourceAddresses;
  /** The destination endpoints of the stream */
  destinationEndpoints?: string[];
  /** The destination endpoint ports of the stream */
  destinationEndpointPorts?: number[];
  /** Event Hub ID */
  eventHubId?: string;
  /** Event Hub Consumer Group */
  consumerGroup?: string;
  /** The resource-specific properties for this resource. */
  properties?: Flow;
  /** Resource tags. */
  tags?: { [propertyName: string]: string };
  /** The geo-location where the resource lives */
  location: string;
  /**
   * Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly id?: string;
  /**
   * The name of the resource
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly name?: string;
  /**
   * The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts"
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly type?: string;
  /**
   * Azure Resource Manager metadata containing createdBy and modifiedBy information.
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly systemData?: SystemData;
  /**
   * Subscription ID of the pending flow.
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly subscriptionId?: string;
  /**
   * Connection ID of the pending flow.
   * NOTE: This property will not be serialized. It can only be populated by the server.
   */
  readonly connectionId?: string;
}

/** The pipelines resource patch definition. */
export interface PipelinesPatch {
  /** Properties of pipelines patch body. */
  properties?: PipelinesPatchProperties;
  /** Resource tags. */
  tags?: { [propertyName: string]: string };
  /** The managed service identities assigned to this resource. */
  identity?: ManagedServiceIdentity;
}

/** Properties of pipelines patch body. */
export interface PipelinesPatchProperties {
  /** Connections associated with pipeline */
  connections?: PipelineConnection[];
  /** The flow types allowed for this pipeline */
  flowTypes?: FlowType[];
}

/** The action to be executed. */
export interface Action {
  /** Type of action to be executed */
  actionType: ActionType;
  /** Type of target to execute the action on */
  targetType: TargetType;
  /** Targets for the action */
  targets: string[];
  /** Business justification for the action */
  justification?: string;
}

/** An array of connection id resources. */
export interface ConnectionIdList {
  /** Connection ID to target */
  value?: string[];
}

/** An array of flow resources. */
export interface ListFlowsByPipelineResult {
  /** List flows by pipeline result by connection */
  value?: ListFlowsByPipelineConnection[];
}

/** A connection resource id in addition to all child flow resources under this connection. */
export interface ListFlowsByPipelineConnection {
  /** ID of the connection. */
  id?: string;
  /** List of flows associated with the connection. */
  flows?: Flow[];
}

/** Azure operation completed successfully. */
export interface AzureResourceManagerArmResponseConnection {
  /** The body type of the operation request or response. */
  body: Connection;
}

/** Azure operation completed successfully. */
export interface AzureResourceManagerArmResponseFlow {
  /** The body type of the operation request or response. */
  body: Flow;
}

/** Azure operation completed successfully. */
export interface AzureResourceManagerArmResponsePipeline {
  /** The body type of the operation request or response. */
  body: Pipeline;
}

/** The resource model definition for an Azure Resource Manager tracked top level resource which has 'tags' and a 'location' */
export interface TrackedResource extends Resource {
  /** Resource tags. */
  tags?: { [propertyName: string]: string };
  /** The geo-location where the resource lives */
  location: string;
}

/** The connection resource definition. */
export interface Connection extends TrackedResource {
  /** Properties of connection */
  properties?: ConnectionProperties;
  /** The managed service identities assigned to this resource. */
  identity?: ManagedServiceIdentity;
}

/** The pipeline resource definition. */
export interface Pipeline extends TrackedResource {
  /** Properties of pipeline */
  properties?: PipelineProperties;
  /** The managed service identities assigned to this resource. */
  identity?: ManagedServiceIdentity;
}

/** The flow resource definition. */
export interface Flow extends TrackedResource {
  /** Properties of flow */
  properties?: FlowProperties;
  /** Details of the resource plan. */
  plan?: Plan;
  /** The managed service identities assigned to this resource. */
  identity?: ManagedServiceIdentity;
}

/** Defines headers for Connections_update operation. */
export interface ConnectionsUpdateHeaders {
  /** The Location header contains the URL where the status of the long running operation can be checked. */
  location?: string;
  /** The Retry-After header can indicate how long the client should wait before polling the operation status. */
  retryAfter?: number;
}

/** Defines headers for Connections_delete operation. */
export interface ConnectionsDeleteHeaders {
  /** The Location header contains the URL where the status of the long running operation can be checked. */
  location?: string;
  /** The Retry-After header can indicate how long the client should wait before polling the operation status. */
  retryAfter?: number;
}

/** Defines headers for Connections_link operation. */
export interface ConnectionsLinkHeaders {
  /** The Location header contains the URL where the status of the long running operation can be checked. */
  location?: string;
  /** The Retry-After header can indicate how long the client should wait before polling the operation status. */
  retryAfter?: number;
}

/** Defines headers for Pipelines_createOrUpdate operation. */
export interface PipelinesCreateOrUpdateHeaders {
  /** A link to the status monitor */
  azureAsyncOperation?: string;
  /** The Retry-After header can indicate how long the client should wait before polling the operation status. */
  retryAfter?: number;
}

/** Defines headers for Pipelines_update operation. */
export interface PipelinesUpdateHeaders {
  /** The Location header contains the URL where the status of the long running operation can be checked. */
  location?: string;
  /** The Retry-After header can indicate how long the client should wait before polling the operation status. */
  retryAfter?: number;
}

/** Defines headers for Pipelines_delete operation. */
export interface PipelinesDeleteHeaders {
  /** The Location header contains the URL where the status of the long running operation can be checked. */
  location?: string;
  /** The Retry-After header can indicate how long the client should wait before polling the operation status. */
  retryAfter?: number;
}

/** Defines headers for Pipelines_approveConnection operation. */
export interface PipelinesApproveConnectionHeaders {
  /** The Location header contains the URL where the status of the long running operation can be checked. */
  location?: string;
  /** The Retry-After header can indicate how long the client should wait before polling the operation status. */
  retryAfter?: number;
}

/** Defines headers for Pipelines_executeAction operation. */
export interface PipelinesExecuteActionHeaders {
  /** The Location header contains the URL where the status of the long running operation can be checked. */
  location?: string;
  /** The Retry-After header can indicate how long the client should wait before polling the operation status. */
  retryAfter?: number;
}

/** Defines headers for Pipelines_rejectConnection operation. */
export interface PipelinesRejectConnectionHeaders {
  /** The Location header contains the URL where the status of the long running operation can be checked. */
  location?: string;
  /** The Retry-After header can indicate how long the client should wait before polling the operation status. */
  retryAfter?: number;
}

/** Defines headers for Flows_update operation. */
export interface FlowsUpdateHeaders {
  /** The Location header contains the URL where the status of the long running operation can be checked. */
  location?: string;
  /** The Retry-After header can indicate how long the client should wait before polling the operation status. */
  retryAfter?: number;
}

/** Defines headers for Flows_delete operation. */
export interface FlowsDeleteHeaders {
  /** The Location header contains the URL where the status of the long running operation can be checked. */
  location?: string;
  /** The Retry-After header can indicate how long the client should wait before polling the operation status. */
  retryAfter?: number;
}

/** Defines headers for Flows_disable operation. */
export interface FlowsDisableHeaders {
  /** The Location header contains the URL where the status of the long running operation can be checked. */
  location?: string;
  /** The Retry-After header can indicate how long the client should wait before polling the operation status. */
  retryAfter?: number;
}

/** Defines headers for Flows_enable operation. */
export interface FlowsEnableHeaders {
  /** The Location header contains the URL where the status of the long running operation can be checked. */
  location?: string;
  /** The Retry-After header can indicate how long the client should wait before polling the operation status. */
  retryAfter?: number;
}

/** Defines headers for Flows_generatePassphrase operation. */
export interface FlowsGeneratePassphraseHeaders {
  /** The Location header contains the URL where the status of the long running operation can be checked. */
  location?: string;
  /** The Retry-After header can indicate how long the client should wait before polling the operation status. */
  retryAfter?: number;
}

/** Defines headers for Flows_link operation. */
export interface FlowsLinkHeaders {
  /** The Location header contains the URL where the status of the long running operation can be checked. */
  location?: string;
  /** The Retry-After header can indicate how long the client should wait before polling the operation status. */
  retryAfter?: number;
}

/** Defines headers for Flows_setDestinationEndpointPorts operation. */
export interface FlowsSetDestinationEndpointPortsHeaders {
  /** The Location header contains the URL where the status of the long running operation can be checked. */
  location?: string;
  /** The Retry-After header can indicate how long the client should wait before polling the operation status. */
  retryAfter?: number;
}

/** Defines headers for Flows_setDestinationEndpoints operation. */
export interface FlowsSetDestinationEndpointsHeaders {
  /** The Location header contains the URL where the status of the long running operation can be checked. */
  location?: string;
  /** The Retry-After header can indicate how long the client should wait before polling the operation status. */
  retryAfter?: number;
}

/** Defines headers for Flows_setPassphrase operation. */
export interface FlowsSetPassphraseHeaders {
  /** The Location header contains the URL where the status of the long running operation can be checked. */
  location?: string;
  /** The Retry-After header can indicate how long the client should wait before polling the operation status. */
  retryAfter?: number;
}

/** Defines headers for Flows_setSourceAddresses operation. */
export interface FlowsSetSourceAddressesHeaders {
  /** The Location header contains the URL where the status of the long running operation can be checked. */
  location?: string;
  /** The Retry-After header can indicate how long the client should wait before polling the operation status. */
  retryAfter?: number;
}

/** Known values of {@link Direction} that the service accepts. */
export enum KnownDirection {
  /** The direction is on the sending side of a pipeline, connection, or flow. */
  Send = "Send",
  /** The direction is on the receiving side side of a pipeline, connection, or flow. */
  Receive = "Receive",
}

/**
 * Defines values for Direction. \
 * {@link KnownDirection} can be used interchangeably with Direction,
 *  this enum contains the known values that the service supports.
 * ### Known values supported by the service
 * **Send**: The direction is on the sending side of a pipeline, connection, or flow. \
 * **Receive**: The direction is on the receiving side side of a pipeline, connection, or flow.
 */
export type Direction = string;

/** Known values of {@link SchemaStatus} that the service accepts. */
export enum KnownSchemaStatus {
  /** A schema that has not been approved and is awaiting review. */
  New = "New",
  /** A schema that has been approved and is ready for use. */
  Approved = "Approved",
}

/**
 * Defines values for SchemaStatus. \
 * {@link KnownSchemaStatus} can be used interchangeably with SchemaStatus,
 *  this enum contains the known values that the service supports.
 * ### Known values supported by the service
 * **New**: A schema that has not been approved and is awaiting review. \
 * **Approved**: A schema that has been approved and is ready for use.
 */
export type SchemaStatus = string;

/** Known values of {@link SchemaType} that the service accepts. */
export enum KnownSchemaType {
  /** The schema represents a single XML schema definition file. */
  Xsd = "Xsd",
  /** The schema represents a ZIP file with multiple XML schema definition files within it. */
  Zip = "Zip",
}

/**
 * Defines values for SchemaType. \
 * {@link KnownSchemaType} can be used interchangeably with SchemaType,
 *  this enum contains the known values that the service supports.
 * ### Known values supported by the service
 * **Xsd**: The schema represents a single XML schema definition file. \
 * **Zip**: The schema represents a ZIP file with multiple XML schema definition files within it.
 */
export type SchemaType = string;

/** Known values of {@link Origin} that the service accepts. */
export enum KnownOrigin {
  /** User */
  User = "user",
  /** System */
  System = "system",
  /** UserSystem */
  UserSystem = "user,system",
}

/**
 * Defines values for Origin. \
 * {@link KnownOrigin} can be used interchangeably with Origin,
 *  this enum contains the known values that the service supports.
 * ### Known values supported by the service
 * **user** \
 * **system** \
 * **user,system**
 */
export type Origin = string;

/** Known values of {@link ActionType} that the service accepts. */
export enum KnownActionType {
  /** Internal */
  Internal = "Internal",
}

/**
 * Defines values for ActionType. \
 * {@link KnownActionType} can be used interchangeably with ActionType,
 *  this enum contains the known values that the service supports.
 * ### Known values supported by the service
 * **Internal**
 */
export type ActionType = string;

/** Known values of {@link ValidateSchemaStatus} that the service accepts. */
export enum KnownValidateSchemaStatus {
  /** Validation of the schema was successful. */
  Succeeded = "Succeeded",
  /** The schema failed validation, whether due to error or an invalid schema. */
  Failed = "Failed",
}

/**
 * Defines values for ValidateSchemaStatus. \
 * {@link KnownValidateSchemaStatus} can be used interchangeably with ValidateSchemaStatus,
 *  this enum contains the known values that the service supports.
 * ### Known values supported by the service
 * **Succeeded**: Validation of the schema was successful. \
 * **Failed**: The schema failed validation, whether due to error or an invalid schema.
 */
export type ValidateSchemaStatus = string;

/** Known values of {@link ConnectionStatus} that the service accepts. */
export enum KnownConnectionStatus {
  /** This connection has been created but is under review by the pipeline owner. */
  InReview = "InReview",
  /** This connection has been approved by the pipeline owner and is ready for use. */
  Approved = "Approved",
  /** This connection has been rejected by the pipeline owner and cannot be used. */
  Rejected = "Rejected",
  /** Creation of the connection has been accepted. */
  Accepted = "Accepted",
}

/**
 * Defines values for ConnectionStatus. \
 * {@link KnownConnectionStatus} can be used interchangeably with ConnectionStatus,
 *  this enum contains the known values that the service supports.
 * ### Known values supported by the service
 * **InReview**: This connection has been created but is under review by the pipeline owner. \
 * **Approved**: This connection has been approved by the pipeline owner and is ready for use. \
 * **Rejected**: This connection has been rejected by the pipeline owner and cannot be used. \
 * **Accepted**: Creation of the connection has been accepted.
 */
export type ConnectionStatus = string;

/** Known values of {@link ForceDisabledStatus} that the service accepts. */
export enum KnownForceDisabledStatus {
  /** This Flow has been disabled as the connection has been disabled. */
  ConnectionForceDisabled = "ConnectionForceDisabled",
  /** This Flow has been disabled as the flow type this flow uses has been disabled. */
  FlowTypeForceDisabled = "FlowTypeForceDisabled",
}

/**
 * Defines values for ForceDisabledStatus. \
 * {@link KnownForceDisabledStatus} can be used interchangeably with ForceDisabledStatus,
 *  this enum contains the known values that the service supports.
 * ### Known values supported by the service
 * **ConnectionForceDisabled**: This Flow has been disabled as the connection has been disabled. \
 * **FlowTypeForceDisabled**: This Flow has been disabled as the flow type this flow uses has been disabled.
 */
export type ForceDisabledStatus = string;

/** Known values of {@link LinkStatus} that the service accepts. */
export enum KnownLinkStatus {
  /**
   * The pipeline, connection, or flow is currently linked with another corresponding pipeline,
   * connection, or flow, and can be used.
   */
  Linked = "Linked",
  /**
   * The pipeline, connection, or flow is not linked with another corresponding pipeline,
   * connection, or flow, and cannot be used.
   */
  Unlinked = "Unlinked",
}

/**
 * Defines values for LinkStatus. \
 * {@link KnownLinkStatus} can be used interchangeably with LinkStatus,
 *  this enum contains the known values that the service supports.
 * ### Known values supported by the service
 * **Linked**: The pipeline, connection, or flow is currently linked with another corresponding pipeline,
 * connection, or flow, and can be used. \
 * **Unlinked**: The pipeline, connection, or flow is not linked with another corresponding pipeline,
 * connection, or flow, and cannot be used.
 */
export type LinkStatus = string;

/** Known values of {@link FlowType} that the service accepts. */
export enum KnownFlowType {
  /** This type of flow is not set. */
  Unknown = "Unknown",
  /** This flow sends complex documents, such as Microsoft Office documents of Portable document format files. */
  Complex = "Complex",
  /** This flow sends source code files and artifacts. */
  DevSecOps = "DevSecOps",
  /** This flow over the Azure Data Transfer low latency messaging subsystem. */
  Messaging = "Messaging",
  /** This flow sends data that will not be processed by any security system. */
  Mission = "Mission",
  /** This flow is assigned to internal Microsoft use cases. */
  MicrosoftInternal = "MicrosoftInternal",
  /** This flow sends blobs that can be used for multiple use cases. */
  BasicFiles = "BasicFiles",
  /** This flow sends blobs that can be used for multiple use cases. */
  Data = "Data",
  /** This flow sends blobs that can be used for multiple use cases. */
  Standard = "Standard",
  /** This flow sends video streams. */
  StreamingVideo = "StreamingVideo",
  /** This flow sends blobs that will not be processed by any security system. */
  Opaque = "Opaque",
  /** This flow sends Extensible Markup Language that will not be processed by any security system. */
  MissionOpaqueXML = "MissionOpaqueXML",
  /** This flow sends blobs that can be used for Virtual Machines. */
  DiskImages = "DiskImages",
  /** This flow sends data via an Application Programmer Interface. */
  API = "API",
}

/**
 * Defines values for FlowType. \
 * {@link KnownFlowType} can be used interchangeably with FlowType,
 *  this enum contains the known values that the service supports.
 * ### Known values supported by the service
 * **Unknown**: This type of flow is not set. \
 * **Complex**: This flow sends complex documents, such as Microsoft Office documents of Portable document format files. \
 * **DevSecOps**: This flow sends source code files and artifacts. \
 * **Messaging**: This flow over the Azure Data Transfer low latency messaging subsystem. \
 * **Mission**: This flow sends data that will not be processed by any security system. \
 * **MicrosoftInternal**: This flow is assigned to internal Microsoft use cases. \
 * **BasicFiles**: This flow sends blobs that can be used for multiple use cases. \
 * **Data**: This flow sends blobs that can be used for multiple use cases. \
 * **Standard**: This flow sends blobs that can be used for multiple use cases. \
 * **StreamingVideo**: This flow sends video streams. \
 * **Opaque**: This flow sends blobs that will not be processed by any security system. \
 * **MissionOpaqueXML**: This flow sends Extensible Markup Language that will not be processed by any security system. \
 * **DiskImages**: This flow sends blobs that can be used for Virtual Machines. \
 * **API**: This flow sends data via an Application Programmer Interface.
 */
export type FlowType = string;

/** Known values of {@link ProvisioningState} that the service accepts. */
export enum KnownProvisioningState {
  /** Resource has been created. */
  Succeeded = "Succeeded",
  /** Resource creation failed. */
  Failed = "Failed",
  /** Resource creation was canceled. */
  Canceled = "Canceled",
  /** Resource has been accepted for creation. */
  Accepted = "Accepted",
}

/**
 * Defines values for ProvisioningState. \
 * {@link KnownProvisioningState} can be used interchangeably with ProvisioningState,
 *  this enum contains the known values that the service supports.
 * ### Known values supported by the service
 * **Succeeded**: Resource has been created. \
 * **Failed**: Resource creation failed. \
 * **Canceled**: Resource creation was canceled. \
 * **Accepted**: Resource has been accepted for creation.
 */
export type ProvisioningState = string;

/** Known values of {@link ManagedServiceIdentityType} that the service accepts. */
export enum KnownManagedServiceIdentityType {
  /** None */
  None = "None",
  /** SystemAssigned */
  SystemAssigned = "SystemAssigned",
  /** UserAssigned */
  UserAssigned = "UserAssigned",
  /** SystemAssignedUserAssigned */
  SystemAssignedUserAssigned = "SystemAssigned,UserAssigned",
}

/**
 * Defines values for ManagedServiceIdentityType. \
 * {@link KnownManagedServiceIdentityType} can be used interchangeably with ManagedServiceIdentityType,
 *  this enum contains the known values that the service supports.
 * ### Known values supported by the service
 * **None** \
 * **SystemAssigned** \
 * **UserAssigned** \
 * **SystemAssigned,UserAssigned**
 */
export type ManagedServiceIdentityType = string;

/** Known values of {@link CreatedByType} that the service accepts. */
export enum KnownCreatedByType {
  /** User */
  User = "User",
  /** Application */
  Application = "Application",
  /** ManagedIdentity */
  ManagedIdentity = "ManagedIdentity",
  /** Key */
  Key = "Key",
}

/**
 * Defines values for CreatedByType. \
 * {@link KnownCreatedByType} can be used interchangeably with CreatedByType,
 *  this enum contains the known values that the service supports.
 * ### Known values supported by the service
 * **User** \
 * **Application** \
 * **ManagedIdentity** \
 * **Key**
 */
export type CreatedByType = string;

/** Known values of {@link OperationStatusEnum} that the service accepts. */
export enum KnownOperationStatusEnum {
  /** The last patch request for this connection completed with an error. */
  Failed = "Failed",
  /** The last patch request for this connection completed. */
  Succeeded = "Succeeded",
}

/**
 * Defines values for OperationStatusEnum. \
 * {@link KnownOperationStatusEnum} can be used interchangeably with OperationStatusEnum,
 *  this enum contains the known values that the service supports.
 * ### Known values supported by the service
 * **Failed**: The last patch request for this connection completed with an error. \
 * **Succeeded**: The last patch request for this connection completed.
 */
export type OperationStatusEnum = string;

/** Known values of {@link PipelineStatus} that the service accepts. */
export enum KnownPipelineStatus {
  /** The pipeline is currently enabled and all child connection and flow resources below able to send or receive data. */
  Enabled = "Enabled",
  /** The pipeline is currently disabled and all child connection and flow resources below are unable to send or receive data. */
  Disabled = "Disabled",
}

/**
 * Defines values for PipelineStatus. \
 * {@link KnownPipelineStatus} can be used interchangeably with PipelineStatus,
 *  this enum contains the known values that the service supports.
 * ### Known values supported by the service
 * **Enabled**: The pipeline is currently enabled and all child connection and flow resources below able to send or receive data. \
 * **Disabled**: The pipeline is currently disabled and all child connection and flow resources below are unable to send or receive data.
 */
export type PipelineStatus = string;

/** Known values of {@link FlowStatus} that the service accepts. */
export enum KnownFlowStatus {
  /** The flow is currently enabled and able to send or receive data. */
  Enabled = "Enabled",
  /** The flow is currently disabled and cannot send or receive data. */
  Disabled = "Disabled",
}

/**
 * Defines values for FlowStatus. \
 * {@link KnownFlowStatus} can be used interchangeably with FlowStatus,
 *  this enum contains the known values that the service supports.
 * ### Known values supported by the service
 * **Enabled**: The flow is currently enabled and able to send or receive data. \
 * **Disabled**: The flow is currently disabled and cannot send or receive data.
 */
export type FlowStatus = string;

/** Known values of {@link DataType} that the service accepts. */
export enum KnownDataType {
  /** The flow uses Azure Storage blobs to send or receive data. */
  Blob = "Blob",
  /** The flow uses Azure Storage tables to send or receive data. */
  Table = "Table",
}

/**
 * Defines values for DataType. \
 * {@link KnownDataType} can be used interchangeably with DataType,
 *  this enum contains the known values that the service supports.
 * ### Known values supported by the service
 * **Blob**: The flow uses Azure Storage blobs to send or receive data. \
 * **Table**: The flow uses Azure Storage tables to send or receive data.
 */
export type DataType = string;

/** Known values of {@link FlowBillingTier} that the service accepts. */
export enum KnownFlowBillingTier {
  /** Billing tier for messaging processing that charges based on how much data is transferred. */
  BlobTransport = "BlobTransport",
  /** Billing tier for standard messaging processing that charges based on having the messaging resource allocated. */
  Standard = "Standard",
  /** Billing tier for premium messaging processing that charges based on having the messaging resource allocated. */
  Premium = "Premium",
}

/**
 * Defines values for FlowBillingTier. \
 * {@link KnownFlowBillingTier} can be used interchangeably with FlowBillingTier,
 *  this enum contains the known values that the service supports.
 * ### Known values supported by the service
 * **BlobTransport**: Billing tier for messaging processing that charges based on how much data is transferred. \
 * **Standard**: Billing tier for standard messaging processing that charges based on having the messaging resource allocated. \
 * **Premium**: Billing tier for premium messaging processing that charges based on having the messaging resource allocated.
 */
export type FlowBillingTier = string;

/** Known values of {@link ApiMode} that the service accepts. */
export enum KnownApiMode {
  /** The API flow will forward calls to a custom Software Development Kit implementation. */
  SDK = "SDK",
  /** The API flow will forward calls to a specified endpoint. */
  Endpoint = "Endpoint",
}

/**
 * Defines values for ApiMode. \
 * {@link KnownApiMode} can be used interchangeably with ApiMode,
 *  this enum contains the known values that the service supports.
 * ### Known values supported by the service
 * **SDK**: The API flow will forward calls to a custom Software Development Kit implementation. \
 * **Endpoint**: The API flow will forward calls to a specified endpoint.
 */
export type ApiMode = string;

/** Known values of {@link IdentityTranslation} that the service accepts. */
export enum KnownIdentityTranslation {
  /**
   * The API flow will make use of the user identity that called this API flow, and pass that token
   * through the boundary.
   */
  UserIdentity = "UserIdentity",
  /** The API flow will make use of the Azure Data Transfer Flow managed identity on the other side of the boundary. */
  ServiceIdentity = "ServiceIdentity",
}

/**
 * Defines values for IdentityTranslation. \
 * {@link KnownIdentityTranslation} can be used interchangeably with IdentityTranslation,
 *  this enum contains the known values that the service supports.
 * ### Known values supported by the service
 * **UserIdentity**: The API flow will make use of the user identity that called this API flow, and pass that token
 * through the boundary. \
 * **ServiceIdentity**: The API flow will make use of the Azure Data Transfer Flow managed identity on the other side of the boundary.
 */
export type IdentityTranslation = string;

/** Known values of {@link StreamProtocol} that the service accepts. */
export enum KnownStreamProtocol {
  /** This Flow is using User Datagram Protocol for the video stream. */
  UDP = "UDP",
  /** This Flow is using Secure Reliable Transfer for the video stream. */
  SRT = "SRT",
  /** This Flow is using Real-time Transport Protocol for the video stream. */
  RTP = "RTP",
}

/**
 * Defines values for StreamProtocol. \
 * {@link KnownStreamProtocol} can be used interchangeably with StreamProtocol,
 *  this enum contains the known values that the service supports.
 * ### Known values supported by the service
 * **UDP**: This Flow is using User Datagram Protocol for the video stream. \
 * **SRT**: This Flow is using Secure Reliable Transfer for the video stream. \
 * **RTP**: This Flow is using Real-time Transport Protocol for the video stream.
 */
export type StreamProtocol = string;

/** Known values of {@link TargetType} that the service accepts. */
export enum KnownTargetType {
  /** The action executing is targeting a pipeline. */
  Pipeline = "Pipeline",
  /** The action executing is targeting a connection. */
  Connection = "Connection",
  /** The action executing is targeting a flow type. */
  FlowType = "FlowType",
}

/**
 * Defines values for TargetType. \
 * {@link KnownTargetType} can be used interchangeably with TargetType,
 *  this enum contains the known values that the service supports.
 * ### Known values supported by the service
 * **Pipeline**: The action executing is targeting a pipeline. \
 * **Connection**: The action executing is targeting a connection. \
 * **FlowType**: The action executing is targeting a flow type.
 */
export type TargetType = string;

/** Optional parameters. */
export interface AzureDataTransferListApprovedSchemasOptionalParams
  extends coreClient.OperationOptions {}

/** Contains response data for the listApprovedSchemas operation. */
export type AzureDataTransferListApprovedSchemasResponse = SchemasListResult;

/** Optional parameters. */
export interface AzureDataTransferValidateSchemaOptionalParams
  extends coreClient.OperationOptions {}

/** Contains response data for the validateSchema operation. */
export type AzureDataTransferValidateSchemaResponse = ValidateSchemaResult;

/** Optional parameters. */
export interface OperationsListOptionalParams extends coreClient.OperationOptions {}

/** Contains response data for the list operation. */
export type OperationsListResponse = OperationListResult;

/** Optional parameters. */
export interface OperationsListNextOptionalParams extends coreClient.OperationOptions {}

/** Contains response data for the listNext operation. */
export type OperationsListNextResponse = OperationListResult;

/** Optional parameters. */
export interface ConnectionsListBySubscriptionOptionalParams extends coreClient.OperationOptions {}

/** Contains response data for the listBySubscription operation. */
export type ConnectionsListBySubscriptionResponse = ConnectionListResult;

/** Optional parameters. */
export interface ConnectionsListByResourceGroupOptionalParams extends coreClient.OperationOptions {}

/** Contains response data for the listByResourceGroup operation. */
export type ConnectionsListByResourceGroupResponse = ConnectionListResult;

/** Optional parameters. */
export interface ConnectionsGetOptionalParams extends coreClient.OperationOptions {}

/** Contains response data for the get operation. */
export type ConnectionsGetResponse = Connection;

/** Optional parameters. */
export interface ConnectionsCreateOrUpdateOptionalParams extends coreClient.OperationOptions {}

/** Contains response data for the createOrUpdate operation. */
export type ConnectionsCreateOrUpdateResponse = Connection;

/** Optional parameters. */
export interface ConnectionsUpdateOptionalParams extends coreClient.OperationOptions {
  /** Delay to wait until next poll, in milliseconds. */
  updateIntervalInMs?: number;
  /** A serialized poller which can be used to resume an existing paused Long-Running-Operation. */
  resumeFrom?: string;
}

/** Contains response data for the update operation. */
export type ConnectionsUpdateResponse = Connection;

/** Optional parameters. */
export interface ConnectionsDeleteOptionalParams extends coreClient.OperationOptions {
  /** Delay to wait until next poll, in milliseconds. */
  updateIntervalInMs?: number;
  /** A serialized poller which can be used to resume an existing paused Long-Running-Operation. */
  resumeFrom?: string;
}

/** Contains response data for the delete operation. */
export type ConnectionsDeleteResponse = ConnectionsDeleteHeaders;

/** Optional parameters. */
export interface ConnectionsLinkOptionalParams extends coreClient.OperationOptions {
  /** Delay to wait until next poll, in milliseconds. */
  updateIntervalInMs?: number;
  /** A serialized poller which can be used to resume an existing paused Long-Running-Operation. */
  resumeFrom?: string;
}

/** Contains response data for the link operation. */
export type ConnectionsLinkResponse = Connection;

/** Optional parameters. */
export interface ConnectionsListOptionalParams extends coreClient.OperationOptions {}

/** Contains response data for the list operation. */
export type ConnectionsListResponse = PendingConnectionListResult;

/** Optional parameters. */
export interface ConnectionsListPendingFlowsListOptionalParams
  extends coreClient.OperationOptions {}

/** Contains response data for the listPendingFlowsList operation. */
export type ConnectionsListPendingFlowsListResponse = PendingFlowListResult;

/** Optional parameters. */
export interface ConnectionsListBySubscriptionNextOptionalParams
  extends coreClient.OperationOptions {}

/** Contains response data for the listBySubscriptionNext operation. */
export type ConnectionsListBySubscriptionNextResponse = ConnectionListResult;

/** Optional parameters. */
export interface ConnectionsListByResourceGroupNextOptionalParams
  extends coreClient.OperationOptions {}

/** Contains response data for the listByResourceGroupNext operation. */
export type ConnectionsListByResourceGroupNextResponse = ConnectionListResult;

/** Optional parameters. */
export interface ConnectionsListNextOptionalParams extends coreClient.OperationOptions {}

/** Contains response data for the listNext operation. */
export type ConnectionsListNextResponse = PendingConnectionListResult;

/** Optional parameters. */
export interface ConnectionsListPendingFlowsListNextOptionalParams
  extends coreClient.OperationOptions {}

/** Contains response data for the listPendingFlowsListNext operation. */
export type ConnectionsListPendingFlowsListNextResponse = PendingFlowListResult;

/** Optional parameters. */
export interface PipelinesListBySubscriptionOptionalParams extends coreClient.OperationOptions {}

/** Contains response data for the listBySubscription operation. */
export type PipelinesListBySubscriptionResponse = PipelineListResult;

/** Optional parameters. */
export interface PipelinesListByResourceGroupOptionalParams extends coreClient.OperationOptions {}

/** Contains response data for the listByResourceGroup operation. */
export type PipelinesListByResourceGroupResponse = PipelineListResult;

/** Optional parameters. */
export interface PipelinesGetOptionalParams extends coreClient.OperationOptions {}

/** Contains response data for the get operation. */
export type PipelinesGetResponse = Pipeline;

/** Optional parameters. */
export interface PipelinesCreateOrUpdateOptionalParams extends coreClient.OperationOptions {
  /** Delay to wait until next poll, in milliseconds. */
  updateIntervalInMs?: number;
  /** A serialized poller which can be used to resume an existing paused Long-Running-Operation. */
  resumeFrom?: string;
}

/** Contains response data for the createOrUpdate operation. */
export type PipelinesCreateOrUpdateResponse = Pipeline;

/** Optional parameters. */
export interface PipelinesUpdateOptionalParams extends coreClient.OperationOptions {
  /** Delay to wait until next poll, in milliseconds. */
  updateIntervalInMs?: number;
  /** A serialized poller which can be used to resume an existing paused Long-Running-Operation. */
  resumeFrom?: string;
}

/** Contains response data for the update operation. */
export type PipelinesUpdateResponse = Pipeline;

/** Optional parameters. */
export interface PipelinesDeleteOptionalParams extends coreClient.OperationOptions {
  /** Delay to wait until next poll, in milliseconds. */
  updateIntervalInMs?: number;
  /** A serialized poller which can be used to resume an existing paused Long-Running-Operation. */
  resumeFrom?: string;
}

/** Contains response data for the delete operation. */
export type PipelinesDeleteResponse = PipelinesDeleteHeaders;

/** Optional parameters. */
export interface PipelinesApproveConnectionOptionalParams extends coreClient.OperationOptions {
  /** Delay to wait until next poll, in milliseconds. */
  updateIntervalInMs?: number;
  /** A serialized poller which can be used to resume an existing paused Long-Running-Operation. */
  resumeFrom?: string;
}

/** Contains response data for the approveConnection operation. */
export type PipelinesApproveConnectionResponse = Connection;

/** Optional parameters. */
export interface PipelinesExecuteActionOptionalParams extends coreClient.OperationOptions {
  /** Delay to wait until next poll, in milliseconds. */
  updateIntervalInMs?: number;
  /** A serialized poller which can be used to resume an existing paused Long-Running-Operation. */
  resumeFrom?: string;
}

/** Contains response data for the executeAction operation. */
export type PipelinesExecuteActionResponse = Pipeline;

/** Optional parameters. */
export interface PipelinesListOptionalParams extends coreClient.OperationOptions {
  /** Connection(s) to retrieve flows of */
  connections?: ConnectionIdList;
}

/** Contains response data for the list operation. */
export type PipelinesListResponse = ListFlowsByPipelineResult;

/** Optional parameters. */
export interface PipelinesListSchemasListOptionalParams extends coreClient.OperationOptions {}

/** Contains response data for the listSchemasList operation. */
export type PipelinesListSchemasListResponse = SchemasListResult;

/** Optional parameters. */
export interface PipelinesRejectConnectionOptionalParams extends coreClient.OperationOptions {
  /** Delay to wait until next poll, in milliseconds. */
  updateIntervalInMs?: number;
  /** A serialized poller which can be used to resume an existing paused Long-Running-Operation. */
  resumeFrom?: string;
}

/** Contains response data for the rejectConnection operation. */
export type PipelinesRejectConnectionResponse = Connection;

/** Optional parameters. */
export interface PipelinesListBySubscriptionNextOptionalParams
  extends coreClient.OperationOptions {}

/** Contains response data for the listBySubscriptionNext operation. */
export type PipelinesListBySubscriptionNextResponse = PipelineListResult;

/** Optional parameters. */
export interface PipelinesListByResourceGroupNextOptionalParams
  extends coreClient.OperationOptions {}

/** Contains response data for the listByResourceGroupNext operation. */
export type PipelinesListByResourceGroupNextResponse = PipelineListResult;

/** Optional parameters. */
export interface FlowsListByConnectionOptionalParams extends coreClient.OperationOptions {}

/** Contains response data for the listByConnection operation. */
export type FlowsListByConnectionResponse = FlowListResult;

/** Optional parameters. */
export interface FlowsGetOptionalParams extends coreClient.OperationOptions {}

/** Contains response data for the get operation. */
export type FlowsGetResponse = Flow;

/** Optional parameters. */
export interface FlowsCreateOrUpdateOptionalParams extends coreClient.OperationOptions {}

/** Contains response data for the createOrUpdate operation. */
export type FlowsCreateOrUpdateResponse = Flow;

/** Optional parameters. */
export interface FlowsUpdateOptionalParams extends coreClient.OperationOptions {
  /** Delay to wait until next poll, in milliseconds. */
  updateIntervalInMs?: number;
  /** A serialized poller which can be used to resume an existing paused Long-Running-Operation. */
  resumeFrom?: string;
}

/** Contains response data for the update operation. */
export type FlowsUpdateResponse = Flow;

/** Optional parameters. */
export interface FlowsDeleteOptionalParams extends coreClient.OperationOptions {
  /** Delay to wait until next poll, in milliseconds. */
  updateIntervalInMs?: number;
  /** A serialized poller which can be used to resume an existing paused Long-Running-Operation. */
  resumeFrom?: string;
}

/** Contains response data for the delete operation. */
export type FlowsDeleteResponse = FlowsDeleteHeaders;

/** Optional parameters. */
export interface FlowsDisableOptionalParams extends coreClient.OperationOptions {
  /** Delay to wait until next poll, in milliseconds. */
  updateIntervalInMs?: number;
  /** A serialized poller which can be used to resume an existing paused Long-Running-Operation. */
  resumeFrom?: string;
}

/** Contains response data for the disable operation. */
export type FlowsDisableResponse = Flow;

/** Optional parameters. */
export interface FlowsEnableOptionalParams extends coreClient.OperationOptions {
  /** Delay to wait until next poll, in milliseconds. */
  updateIntervalInMs?: number;
  /** A serialized poller which can be used to resume an existing paused Long-Running-Operation. */
  resumeFrom?: string;
}

/** Contains response data for the enable operation. */
export type FlowsEnableResponse = Flow;

/** Optional parameters. */
export interface FlowsGeneratePassphraseOptionalParams extends coreClient.OperationOptions {
  /** Delay to wait until next poll, in milliseconds. */
  updateIntervalInMs?: number;
  /** A serialized poller which can be used to resume an existing paused Long-Running-Operation. */
  resumeFrom?: string;
}

/** Contains response data for the generatePassphrase operation. */
export type FlowsGeneratePassphraseResponse = Flow;

/** Optional parameters. */
export interface FlowsGetDestinationEndpointPortsOptionalParams
  extends coreClient.OperationOptions {}

/** Contains response data for the getDestinationEndpointPorts operation. */
export type FlowsGetDestinationEndpointPortsResponse = GetDestinationEndpointPortsResult;

/** Optional parameters. */
export interface FlowsGetDestinationEndpointsOptionalParams extends coreClient.OperationOptions {}

/** Contains response data for the getDestinationEndpoints operation. */
export type FlowsGetDestinationEndpointsResponse = GetDestinationEndpointsResult;

/** Optional parameters. */
export interface FlowsGetSourceAddressesOptionalParams extends coreClient.OperationOptions {}

/** Contains response data for the getSourceAddresses operation. */
export type FlowsGetSourceAddressesResponse = StreamSourceAddresses;

/** Optional parameters. */
export interface FlowsGetStreamConnectionStringOptionalParams extends coreClient.OperationOptions {}

/** Contains response data for the getStreamConnectionString operation. */
export type FlowsGetStreamConnectionStringResponse = GetStreamConnectionStringResult;

/** Optional parameters. */
export interface FlowsLinkOptionalParams extends coreClient.OperationOptions {
  /** Delay to wait until next poll, in milliseconds. */
  updateIntervalInMs?: number;
  /** A serialized poller which can be used to resume an existing paused Long-Running-Operation. */
  resumeFrom?: string;
}

/** Contains response data for the link operation. */
export type FlowsLinkResponse = Flow;

/** Optional parameters. */
export interface FlowsSetDestinationEndpointPortsOptionalParams
  extends coreClient.OperationOptions {
  /** Delay to wait until next poll, in milliseconds. */
  updateIntervalInMs?: number;
  /** A serialized poller which can be used to resume an existing paused Long-Running-Operation. */
  resumeFrom?: string;
}

/** Contains response data for the setDestinationEndpointPorts operation. */
export type FlowsSetDestinationEndpointPortsResponse = Flow;

/** Optional parameters. */
export interface FlowsSetDestinationEndpointsOptionalParams extends coreClient.OperationOptions {
  /** Delay to wait until next poll, in milliseconds. */
  updateIntervalInMs?: number;
  /** A serialized poller which can be used to resume an existing paused Long-Running-Operation. */
  resumeFrom?: string;
}

/** Contains response data for the setDestinationEndpoints operation. */
export type FlowsSetDestinationEndpointsResponse = Flow;

/** Optional parameters. */
export interface FlowsSetPassphraseOptionalParams extends coreClient.OperationOptions {
  /** Delay to wait until next poll, in milliseconds. */
  updateIntervalInMs?: number;
  /** A serialized poller which can be used to resume an existing paused Long-Running-Operation. */
  resumeFrom?: string;
}

/** Contains response data for the setPassphrase operation. */
export type FlowsSetPassphraseResponse = Flow;

/** Optional parameters. */
export interface FlowsSetSourceAddressesOptionalParams extends coreClient.OperationOptions {
  /** Delay to wait until next poll, in milliseconds. */
  updateIntervalInMs?: number;
  /** A serialized poller which can be used to resume an existing paused Long-Running-Operation. */
  resumeFrom?: string;
}

/** Contains response data for the setSourceAddresses operation. */
export type FlowsSetSourceAddressesResponse = Flow;

/** Optional parameters. */
export interface FlowsListByConnectionNextOptionalParams extends coreClient.OperationOptions {}

/** Contains response data for the listByConnectionNext operation. */
export type FlowsListByConnectionNextResponse = FlowListResult;

/** Optional parameters. */
export interface AzuredatatransferrpOptionalParams extends coreClient.ServiceClientOptions {
  /** server parameter */
  $host?: string;
  /** Api Version */
  apiVersion?: string;
  /** Overrides client endpoint. */
  endpoint?: string;
}
