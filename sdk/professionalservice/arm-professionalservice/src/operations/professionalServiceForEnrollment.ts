/*
 * Copyright (c) Microsoft Corporation.
 * Licensed under the MIT License.
 *
 * Code generated by Microsoft (R) AutoRest Code Generator.
 * Changes may cause incorrect behavior and will be lost if the code is regenerated.
 */

import { ProfessionalServiceForEnrollment } from "../operationsInterfaces";
import * as coreClient from "@azure/core-client";
import * as Mappers from "../models/mappers";
import * as Parameters from "../models/parameters";
import { ProfessionalService } from "../professionalService";
import { PollerLike, PollOperationState, LroEngine } from "@azure/core-lro";
import { LroImpl } from "../lroImpl";
import {
  ProfessionalServiceForEnrollmentGetOptionalParams,
  ProfessionalServiceForEnrollmentGetResponse,
  ProfessionalServiceResourceCreation,
  ProfessionalServiceForEnrollmentCreateOrUpdateOptionalParams,
  ProfessionalServiceForEnrollmentCreateOrUpdateResponse,
  ProfessionalServiceForEnrollmentDeleteOptionalParams
} from "../models";

/** Class containing ProfessionalServiceForEnrollment operations. */
export class ProfessionalServiceForEnrollmentImpl
  implements ProfessionalServiceForEnrollment {
  private readonly client: ProfessionalService;

  /**
   * Initialize a new instance of the class ProfessionalServiceForEnrollment class.
   * @param client Reference to the service client
   */
  constructor(client: ProfessionalService) {
    this.client = client;
  }

  /**
   * Gets information about the specified Professional Service for enrollment account.
   * @param resourceName The name of the resource.
   * @param options The options parameters.
   */
  get(
    resourceName: string,
    options?: ProfessionalServiceForEnrollmentGetOptionalParams
  ): Promise<ProfessionalServiceForEnrollmentGetResponse> {
    return this.client.sendOperationRequest(
      { resourceName, options },
      getOperationSpec
    );
  }

  /**
   * Creates or updates a ProfessionalService resource for enrollment account.
   * @param resourceName The name of the resource.
   * @param parameters Parameters supplied to the create or update ProfessionalService operation.
   * @param options The options parameters.
   */
  async beginCreateOrUpdate(
    resourceName: string,
    parameters: ProfessionalServiceResourceCreation,
    options?: ProfessionalServiceForEnrollmentCreateOrUpdateOptionalParams
  ): Promise<
    PollerLike<
      PollOperationState<
        ProfessionalServiceForEnrollmentCreateOrUpdateResponse
      >,
      ProfessionalServiceForEnrollmentCreateOrUpdateResponse
    >
  > {
    const directSendOperation = async (
      args: coreClient.OperationArguments,
      spec: coreClient.OperationSpec
    ): Promise<ProfessionalServiceForEnrollmentCreateOrUpdateResponse> => {
      return this.client.sendOperationRequest(args, spec);
    };
    const sendOperation = async (
      args: coreClient.OperationArguments,
      spec: coreClient.OperationSpec
    ) => {
      let currentRawResponse:
        | coreClient.FullOperationResponse
        | undefined = undefined;
      const providedCallback = args.options?.onResponse;
      const callback: coreClient.RawResponseCallback = (
        rawResponse: coreClient.FullOperationResponse,
        flatResponse: unknown
      ) => {
        currentRawResponse = rawResponse;
        providedCallback?.(rawResponse, flatResponse);
      };
      const updatedArgs = {
        ...args,
        options: {
          ...args.options,
          onResponse: callback
        }
      };
      const flatResponse = await directSendOperation(updatedArgs, spec);
      return {
        flatResponse,
        rawResponse: {
          statusCode: currentRawResponse!.status,
          body: currentRawResponse!.parsedBody,
          headers: currentRawResponse!.headers.toJSON()
        }
      };
    };

    const lro = new LroImpl(
      sendOperation,
      { resourceName, parameters, options },
      createOrUpdateOperationSpec
    );
    const poller = new LroEngine(lro, {
      resumeFrom: options?.resumeFrom,
      intervalInMs: options?.updateIntervalInMs,
      lroResourceLocationConfig: "azure-async-operation"
    });
    await poller.poll();
    return poller;
  }

  /**
   * Creates or updates a ProfessionalService resource for enrollment account.
   * @param resourceName The name of the resource.
   * @param parameters Parameters supplied to the create or update ProfessionalService operation.
   * @param options The options parameters.
   */
  async beginCreateOrUpdateAndWait(
    resourceName: string,
    parameters: ProfessionalServiceResourceCreation,
    options?: ProfessionalServiceForEnrollmentCreateOrUpdateOptionalParams
  ): Promise<ProfessionalServiceForEnrollmentCreateOrUpdateResponse> {
    const poller = await this.beginCreateOrUpdate(
      resourceName,
      parameters,
      options
    );
    return poller.pollUntilDone();
  }

  /**
   * Deletes the specified ProfessionalService for enrollment account.
   * @param resourceName The name of the resource.
   * @param options The options parameters.
   */
  async beginDelete(
    resourceName: string,
    options?: ProfessionalServiceForEnrollmentDeleteOptionalParams
  ): Promise<PollerLike<PollOperationState<void>, void>> {
    const directSendOperation = async (
      args: coreClient.OperationArguments,
      spec: coreClient.OperationSpec
    ): Promise<void> => {
      return this.client.sendOperationRequest(args, spec);
    };
    const sendOperation = async (
      args: coreClient.OperationArguments,
      spec: coreClient.OperationSpec
    ) => {
      let currentRawResponse:
        | coreClient.FullOperationResponse
        | undefined = undefined;
      const providedCallback = args.options?.onResponse;
      const callback: coreClient.RawResponseCallback = (
        rawResponse: coreClient.FullOperationResponse,
        flatResponse: unknown
      ) => {
        currentRawResponse = rawResponse;
        providedCallback?.(rawResponse, flatResponse);
      };
      const updatedArgs = {
        ...args,
        options: {
          ...args.options,
          onResponse: callback
        }
      };
      const flatResponse = await directSendOperation(updatedArgs, spec);
      return {
        flatResponse,
        rawResponse: {
          statusCode: currentRawResponse!.status,
          body: currentRawResponse!.parsedBody,
          headers: currentRawResponse!.headers.toJSON()
        }
      };
    };

    const lro = new LroImpl(
      sendOperation,
      { resourceName, options },
      deleteOperationSpec
    );
    const poller = new LroEngine(lro, {
      resumeFrom: options?.resumeFrom,
      intervalInMs: options?.updateIntervalInMs,
      lroResourceLocationConfig: "location"
    });
    await poller.poll();
    return poller;
  }

  /**
   * Deletes the specified ProfessionalService for enrollment account.
   * @param resourceName The name of the resource.
   * @param options The options parameters.
   */
  async beginDeleteAndWait(
    resourceName: string,
    options?: ProfessionalServiceForEnrollmentDeleteOptionalParams
  ): Promise<void> {
    const poller = await this.beginDelete(resourceName, options);
    return poller.pollUntilDone();
  }
}
// Operation Specifications
const serializer = coreClient.createSerializer(Mappers, /* isXml */ false);

const getOperationSpec: coreClient.OperationSpec = {
  path:
    "/providers/Microsoft.Billing/billingAccounts/{enrollmentBillingAccount}/providers/Microsoft.ProfessionalService/resources/{resourceName}",
  httpMethod: "GET",
  responses: {
    200: {
      bodyMapper: Mappers.ProfessionalServiceResource
    },
    default: {
      bodyMapper: Mappers.ErrorResponse
    }
  },
  queryParameters: [Parameters.apiVersion],
  urlParameters: [
    Parameters.$host,
    Parameters.resourceName,
    Parameters.enrollmentBillingAccount
  ],
  headerParameters: [Parameters.accept],
  serializer
};
const createOrUpdateOperationSpec: coreClient.OperationSpec = {
  path:
    "/providers/Microsoft.Billing/billingAccounts/{enrollmentBillingAccount}/providers/Microsoft.ProfessionalService/resources/{resourceName}",
  httpMethod: "PUT",
  responses: {
    200: {
      bodyMapper: Mappers.ProfessionalServiceResource
    },
    201: {
      bodyMapper: Mappers.ProfessionalServiceResource
    },
    202: {
      bodyMapper: Mappers.ProfessionalServiceResource
    },
    204: {
      bodyMapper: Mappers.ProfessionalServiceResource
    },
    default: {
      bodyMapper: Mappers.ErrorResponse
    }
  },
  requestBody: Parameters.parameters,
  queryParameters: [Parameters.apiVersion],
  urlParameters: [
    Parameters.$host,
    Parameters.resourceName,
    Parameters.enrollmentBillingAccount
  ],
  headerParameters: [Parameters.accept, Parameters.contentType],
  mediaType: "json",
  serializer
};
const deleteOperationSpec: coreClient.OperationSpec = {
  path:
    "/providers/Microsoft.Billing/billingAccounts/{enrollmentBillingAccount}/providers/Microsoft.ProfessionalService/resources/{resourceName}",
  httpMethod: "DELETE",
  responses: {
    200: {},
    201: {},
    202: {},
    204: {},
    default: {
      bodyMapper: Mappers.ErrorResponse
    }
  },
  queryParameters: [Parameters.apiVersion],
  urlParameters: [
    Parameters.$host,
    Parameters.resourceName,
    Parameters.enrollmentBillingAccount
  ],
  headerParameters: [Parameters.accept],
  serializer
};
