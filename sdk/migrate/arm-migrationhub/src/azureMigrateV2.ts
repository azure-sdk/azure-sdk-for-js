/*
 * Copyright (c) Microsoft Corporation.
 * Licensed under the MIT License.
 *
 * Code generated by Microsoft (R) AutoRest Code Generator.
 * Changes may cause incorrect behavior and will be lost if the code is regenerated.
 */

import * as coreClient from "@azure/core-client";
import * as coreRestPipeline from "@azure/core-rest-pipeline";
import {
  PipelineRequest,
  PipelineResponse,
  SendRequest,
} from "@azure/core-rest-pipeline";
import * as coreAuth from "@azure/core-auth";
import {
  DatabaseInstancesControllerImpl,
  DatabasesControllerImpl,
  EventsControllerImpl,
  MachinesControllerImpl,
  MigrateProjectsControllerImpl,
  SolutionsControllerImpl,
  VirtualDesktopUserControllerImpl,
  WebServersControllerImpl,
  WebSitesControllerImpl,
  ProjectsImpl,
  PrivateEndpointConnectionsControllerImpl,
  PrivateEndpointConnectionControllerImpl,
  PrivateEndpointConnectionProxyControllerImpl,
  PrivateLinkResourceControllerImpl,
  OperationsImpl,
} from "./operations/index.js";
import {
  DatabaseInstancesController,
  DatabasesController,
  EventsController,
  MachinesController,
  MigrateProjectsController,
  SolutionsController,
  VirtualDesktopUserController,
  WebServersController,
  WebSitesController,
  Projects,
  PrivateEndpointConnectionsController,
  PrivateEndpointConnectionController,
  PrivateEndpointConnectionProxyController,
  PrivateLinkResourceController,
  Operations,
} from "./operationsInterfaces/index.js";
import { AzureMigrateV2OptionalParams } from "./models/index.js";

export class AzureMigrateV2 extends coreClient.ServiceClient {
  $host: string;
  subscriptionId: string;
  apiVersion: string;

  /**
   * Initializes a new instance of the AzureMigrateV2 class.
   * @param credentials Subscription credentials which uniquely identify client subscription.
   * @param subscriptionId The ID of the target subscription.
   * @param options The parameter options
   */
  constructor(
    credentials: coreAuth.TokenCredential,
    subscriptionId: string,
    options?: AzureMigrateV2OptionalParams,
  ) {
    if (credentials === undefined) {
      throw new Error("'credentials' cannot be null");
    }
    if (subscriptionId === undefined) {
      throw new Error("'subscriptionId' cannot be null");
    }

    // Initializing default values for options
    if (!options) {
      options = {};
    }
    const defaults: AzureMigrateV2OptionalParams = {
      requestContentType: "application/json; charset=utf-8",
      credential: credentials,
    };

    const packageDetails = `azsdk-js-arm-migrationhub/1.0.0-beta.1`;
    const userAgentPrefix =
      options.userAgentOptions && options.userAgentOptions.userAgentPrefix
        ? `${options.userAgentOptions.userAgentPrefix} ${packageDetails}`
        : `${packageDetails}`;

    const optionsWithDefaults = {
      ...defaults,
      ...options,
      userAgentOptions: {
        userAgentPrefix,
      },
      endpoint:
        options.endpoint ?? options.baseUri ?? "https://management.azure.com",
    };
    super(optionsWithDefaults);

    let bearerTokenAuthenticationPolicyFound: boolean = false;
    if (options?.pipeline && options.pipeline.getOrderedPolicies().length > 0) {
      const pipelinePolicies: coreRestPipeline.PipelinePolicy[] =
        options.pipeline.getOrderedPolicies();
      bearerTokenAuthenticationPolicyFound = pipelinePolicies.some(
        (pipelinePolicy) =>
          pipelinePolicy.name ===
          coreRestPipeline.bearerTokenAuthenticationPolicyName,
      );
    }
    if (
      !options ||
      !options.pipeline ||
      options.pipeline.getOrderedPolicies().length == 0 ||
      !bearerTokenAuthenticationPolicyFound
    ) {
      this.pipeline.removePolicy({
        name: coreRestPipeline.bearerTokenAuthenticationPolicyName,
      });
      this.pipeline.addPolicy(
        coreRestPipeline.bearerTokenAuthenticationPolicy({
          credential: credentials,
          scopes:
            optionsWithDefaults.credentialScopes ??
            `${optionsWithDefaults.endpoint}/.default`,
          challengeCallbacks: {
            authorizeRequestOnChallenge:
              coreClient.authorizeRequestOnClaimChallenge,
          },
        }),
      );
    }
    // Parameter assignments
    this.subscriptionId = subscriptionId;

    // Assigning values to Constant parameters
    this.$host = options.$host || "https://management.azure.com";
    this.apiVersion = options.apiVersion || "2023-01-01";
    this.databaseInstancesController = new DatabaseInstancesControllerImpl(
      this,
    );
    this.databasesController = new DatabasesControllerImpl(this);
    this.eventsController = new EventsControllerImpl(this);
    this.machinesController = new MachinesControllerImpl(this);
    this.migrateProjectsController = new MigrateProjectsControllerImpl(this);
    this.solutionsController = new SolutionsControllerImpl(this);
    this.virtualDesktopUserController = new VirtualDesktopUserControllerImpl(
      this,
    );
    this.webServersController = new WebServersControllerImpl(this);
    this.webSitesController = new WebSitesControllerImpl(this);
    this.projects = new ProjectsImpl(this);
    this.privateEndpointConnectionsController =
      new PrivateEndpointConnectionsControllerImpl(this);
    this.privateEndpointConnectionController =
      new PrivateEndpointConnectionControllerImpl(this);
    this.privateEndpointConnectionProxyController =
      new PrivateEndpointConnectionProxyControllerImpl(this);
    this.privateLinkResourceController = new PrivateLinkResourceControllerImpl(
      this,
    );
    this.operations = new OperationsImpl(this);
    this.addCustomApiVersionPolicy(options.apiVersion);
  }

  /** A function that adds a policy that sets the api-version (or equivalent) to reflect the library version. */
  private addCustomApiVersionPolicy(apiVersion?: string) {
    if (!apiVersion) {
      return;
    }
    const apiVersionPolicy = {
      name: "CustomApiVersionPolicy",
      async sendRequest(
        request: PipelineRequest,
        next: SendRequest,
      ): Promise<PipelineResponse> {
        const param = request.url.split("?");
        if (param.length > 1) {
          const newParams = param[1].split("&").map((item) => {
            if (item.indexOf("api-version") > -1) {
              return "api-version=" + apiVersion;
            } else {
              return item;
            }
          });
          request.url = param[0] + "?" + newParams.join("&");
        }
        return next(request);
      },
    };
    this.pipeline.addPolicy(apiVersionPolicy);
  }

  databaseInstancesController: DatabaseInstancesController;
  databasesController: DatabasesController;
  eventsController: EventsController;
  machinesController: MachinesController;
  migrateProjectsController: MigrateProjectsController;
  solutionsController: SolutionsController;
  virtualDesktopUserController: VirtualDesktopUserController;
  webServersController: WebServersController;
  webSitesController: WebSitesController;
  projects: Projects;
  privateEndpointConnectionsController: PrivateEndpointConnectionsController;
  privateEndpointConnectionController: PrivateEndpointConnectionController;
  privateEndpointConnectionProxyController: PrivateEndpointConnectionProxyController;
  privateLinkResourceController: PrivateLinkResourceController;
  operations: Operations;
}
