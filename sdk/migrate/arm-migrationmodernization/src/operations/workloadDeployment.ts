/*
 * Copyright (c) Microsoft Corporation.
 * Licensed under the MIT License.
 *
 * Code generated by Microsoft (R) AutoRest Code Generator.
 * Changes may cause incorrect behavior and will be lost if the code is regenerated.
 */

import { PagedAsyncIterableIterator, PageSettings } from "@azure/core-paging";
import { setContinuationToken } from "../pagingHelper.js";
import { WorkloadDeployment } from "../operationsInterfaces/index.js";
import * as coreClient from "@azure/core-client";
import * as Mappers from "../models/mappers.js";
import * as Parameters from "../models/parameters.js";
import { AzureMigrateEngineAPIs } from "../azureMigrateEngineAPIs.js";
import {
  SimplePollerLike,
  OperationState,
  createHttpPoller,
} from "@azure/core-lro";
import { createLroSpec } from "../lroImpl.js";
import {
  WorkloadDeploymentModel,
  WorkloadDeploymentListNextOptionalParams,
  WorkloadDeploymentListOptionalParams,
  WorkloadDeploymentListResponse,
  WorkloadDeploymentGetOptionalParams,
  WorkloadDeploymentGetResponse,
  WorkloadDeploymentCreateOptionalParams,
  WorkloadDeploymentCreateResponse,
  WorkloadDeploymentDeleteOptionalParams,
  WorkloadDeploymentDeleteResponse,
  WorkloadDeploymentGetSecretConfigurationsOptionalParams,
  WorkloadDeploymentGetSecretConfigurationsResponse,
  WorkloadDeploymentBuildContainerImageOptionalParams,
  WorkloadDeploymentBuildContainerImageResponse,
  WorkloadDeploymentTestMigrateOptionalParams,
  WorkloadDeploymentTestMigrateResponse,
  WorkloadDeploymentTestMigrateCleanupOptionalParams,
  WorkloadDeploymentTestMigrateCleanupResponse,
  WorkloadDeploymentMigrateOptionalParams,
  WorkloadDeploymentMigrateResponse,
  WorkloadDeploymentListNextResponse,
} from "../models/index.js";

/// <reference lib="esnext.asynciterable" />
/** Class containing WorkloadDeployment operations. */
export class WorkloadDeploymentImpl implements WorkloadDeployment {
  private readonly client: AzureMigrateEngineAPIs;

  /**
   * Initialize a new instance of the class WorkloadDeployment class.
   * @param client Reference to the service client
   */
  constructor(client: AzureMigrateEngineAPIs) {
    this.client = client;
  }

  /**
   * Gets the list of workload deployments in the given modernizeProject.
   * @param subscriptionId Azure Subscription Id in which project was created.
   * @param resourceGroupName Name of the Azure Resource Group that project is part of.
   * @param modernizeProjectName ModernizeProject name.
   * @param options The options parameters.
   */
  public list(
    subscriptionId: string,
    resourceGroupName: string,
    modernizeProjectName: string,
    options?: WorkloadDeploymentListOptionalParams,
  ): PagedAsyncIterableIterator<WorkloadDeploymentModel> {
    const iter = this.listPagingAll(
      subscriptionId,
      resourceGroupName,
      modernizeProjectName,
      options,
    );
    return {
      next() {
        return iter.next();
      },
      [Symbol.asyncIterator]() {
        return this;
      },
      byPage: (settings?: PageSettings) => {
        if (settings?.maxPageSize) {
          throw new Error("maxPageSize is not supported by this operation.");
        }
        return this.listPagingPage(
          subscriptionId,
          resourceGroupName,
          modernizeProjectName,
          options,
          settings,
        );
      },
    };
  }

  private async *listPagingPage(
    subscriptionId: string,
    resourceGroupName: string,
    modernizeProjectName: string,
    options?: WorkloadDeploymentListOptionalParams,
    settings?: PageSettings,
  ): AsyncIterableIterator<WorkloadDeploymentModel[]> {
    let result: WorkloadDeploymentListResponse;
    let continuationToken = settings?.continuationToken;
    if (!continuationToken) {
      result = await this._list(
        subscriptionId,
        resourceGroupName,
        modernizeProjectName,
        options,
      );
      let page = result.value || [];
      continuationToken = result.nextLink;
      setContinuationToken(page, continuationToken);
      yield page;
    }
    while (continuationToken) {
      result = await this._listNext(
        subscriptionId,
        resourceGroupName,
        modernizeProjectName,
        continuationToken,
        options,
      );
      continuationToken = result.nextLink;
      let page = result.value || [];
      setContinuationToken(page, continuationToken);
      yield page;
    }
  }

  private async *listPagingAll(
    subscriptionId: string,
    resourceGroupName: string,
    modernizeProjectName: string,
    options?: WorkloadDeploymentListOptionalParams,
  ): AsyncIterableIterator<WorkloadDeploymentModel> {
    for await (const page of this.listPagingPage(
      subscriptionId,
      resourceGroupName,
      modernizeProjectName,
      options,
    )) {
      yield* page;
    }
  }

  /**
   * Gets the details of the workload deployment.
   * @param subscriptionId Azure Subscription Id in which project was created.
   * @param resourceGroupName Name of the Azure Resource Group that project is part of.
   * @param modernizeProjectName ModernizeProject name.
   * @param workloadDeploymentName Workload deployment name.
   * @param options The options parameters.
   */
  get(
    subscriptionId: string,
    resourceGroupName: string,
    modernizeProjectName: string,
    workloadDeploymentName: string,
    options?: WorkloadDeploymentGetOptionalParams,
  ): Promise<WorkloadDeploymentGetResponse> {
    return this.client.sendOperationRequest(
      {
        subscriptionId,
        resourceGroupName,
        modernizeProjectName,
        workloadDeploymentName,
        options,
      },
      getOperationSpec,
    );
  }

  /**
   * Creates the workload deployment.
   * @param subscriptionId Azure Subscription Id in which project was created.
   * @param resourceGroupName Name of the Azure Resource Group that project is part of.
   * @param modernizeProjectName ModernizeProject name.
   * @param workloadDeploymentName Workload deployment name.
   * @param options The options parameters.
   */
  create(
    subscriptionId: string,
    resourceGroupName: string,
    modernizeProjectName: string,
    workloadDeploymentName: string,
    options?: WorkloadDeploymentCreateOptionalParams,
  ): Promise<WorkloadDeploymentCreateResponse> {
    return this.client.sendOperationRequest(
      {
        subscriptionId,
        resourceGroupName,
        modernizeProjectName,
        workloadDeploymentName,
        options,
      },
      createOperationSpec,
    );
  }

  /**
   * Removes the workload deployment.
   * @param subscriptionId Azure Subscription Id in which project was created.
   * @param resourceGroupName Name of the Azure Resource Group that project is part of.
   * @param modernizeProjectName ModernizeProject name.
   * @param workloadDeploymentName Workload deployment name.
   * @param options The options parameters.
   */
  async beginDelete(
    subscriptionId: string,
    resourceGroupName: string,
    modernizeProjectName: string,
    workloadDeploymentName: string,
    options?: WorkloadDeploymentDeleteOptionalParams,
  ): Promise<
    SimplePollerLike<
      OperationState<WorkloadDeploymentDeleteResponse>,
      WorkloadDeploymentDeleteResponse
    >
  > {
    const directSendOperation = async (
      args: coreClient.OperationArguments,
      spec: coreClient.OperationSpec,
    ): Promise<WorkloadDeploymentDeleteResponse> => {
      return this.client.sendOperationRequest(args, spec);
    };
    const sendOperationFn = async (
      args: coreClient.OperationArguments,
      spec: coreClient.OperationSpec,
    ) => {
      let currentRawResponse: coreClient.FullOperationResponse | undefined =
        undefined;
      const providedCallback = args.options?.onResponse;
      const callback: coreClient.RawResponseCallback = (
        rawResponse: coreClient.FullOperationResponse,
        flatResponse: unknown,
      ) => {
        currentRawResponse = rawResponse;
        providedCallback?.(rawResponse, flatResponse);
      };
      const updatedArgs = {
        ...args,
        options: {
          ...args.options,
          onResponse: callback,
        },
      };
      const flatResponse = await directSendOperation(updatedArgs, spec);
      return {
        flatResponse,
        rawResponse: {
          statusCode: currentRawResponse!.status,
          body: currentRawResponse!.parsedBody,
          headers: currentRawResponse!.headers.toJSON(),
        },
      };
    };

    const lro = createLroSpec({
      sendOperationFn,
      args: {
        subscriptionId,
        resourceGroupName,
        modernizeProjectName,
        workloadDeploymentName,
        options,
      },
      spec: deleteOperationSpec,
    });
    const poller = await createHttpPoller<
      WorkloadDeploymentDeleteResponse,
      OperationState<WorkloadDeploymentDeleteResponse>
    >(lro, {
      restoreFrom: options?.resumeFrom,
      intervalInMs: options?.updateIntervalInMs,
      resourceLocationConfig: "location",
    });
    await poller.poll();
    return poller;
  }

  /**
   * Removes the workload deployment.
   * @param subscriptionId Azure Subscription Id in which project was created.
   * @param resourceGroupName Name of the Azure Resource Group that project is part of.
   * @param modernizeProjectName ModernizeProject name.
   * @param workloadDeploymentName Workload deployment name.
   * @param options The options parameters.
   */
  async beginDeleteAndWait(
    subscriptionId: string,
    resourceGroupName: string,
    modernizeProjectName: string,
    workloadDeploymentName: string,
    options?: WorkloadDeploymentDeleteOptionalParams,
  ): Promise<WorkloadDeploymentDeleteResponse> {
    const poller = await this.beginDelete(
      subscriptionId,
      resourceGroupName,
      modernizeProjectName,
      workloadDeploymentName,
      options,
    );
    return poller.pollUntilDone();
  }

  /**
   * Gets the list of workload deployments in the given modernizeProject.
   * @param subscriptionId Azure Subscription Id in which project was created.
   * @param resourceGroupName Name of the Azure Resource Group that project is part of.
   * @param modernizeProjectName ModernizeProject name.
   * @param options The options parameters.
   */
  private _list(
    subscriptionId: string,
    resourceGroupName: string,
    modernizeProjectName: string,
    options?: WorkloadDeploymentListOptionalParams,
  ): Promise<WorkloadDeploymentListResponse> {
    return this.client.sendOperationRequest(
      { subscriptionId, resourceGroupName, modernizeProjectName, options },
      listOperationSpec,
    );
  }

  /**
   * Gets the secrets of the workload deployment.
   * @param subscriptionId Azure Subscription Id in which project was created.
   * @param resourceGroupName Name of the Azure Resource Group that project is part of.
   * @param modernizeProjectName ModernizeProject name.
   * @param workloadDeploymentName Workload deployment name.
   * @param options The options parameters.
   */
  getSecretConfigurations(
    subscriptionId: string,
    resourceGroupName: string,
    modernizeProjectName: string,
    workloadDeploymentName: string,
    options?: WorkloadDeploymentGetSecretConfigurationsOptionalParams,
  ): Promise<WorkloadDeploymentGetSecretConfigurationsResponse> {
    return this.client.sendOperationRequest(
      {
        subscriptionId,
        resourceGroupName,
        modernizeProjectName,
        workloadDeploymentName,
        options,
      },
      getSecretConfigurationsOperationSpec,
    );
  }

  /**
   * Performs the build container image action on the workload deployment.
   * @param subscriptionId Azure Subscription Id in which project was created.
   * @param resourceGroupName Name of the Azure Resource Group that project is part of.
   * @param modernizeProjectName ModernizeProject name.
   * @param workloadDeploymentName Workload deployment name.
   * @param options The options parameters.
   */
  async beginBuildContainerImage(
    subscriptionId: string,
    resourceGroupName: string,
    modernizeProjectName: string,
    workloadDeploymentName: string,
    options?: WorkloadDeploymentBuildContainerImageOptionalParams,
  ): Promise<
    SimplePollerLike<
      OperationState<WorkloadDeploymentBuildContainerImageResponse>,
      WorkloadDeploymentBuildContainerImageResponse
    >
  > {
    const directSendOperation = async (
      args: coreClient.OperationArguments,
      spec: coreClient.OperationSpec,
    ): Promise<WorkloadDeploymentBuildContainerImageResponse> => {
      return this.client.sendOperationRequest(args, spec);
    };
    const sendOperationFn = async (
      args: coreClient.OperationArguments,
      spec: coreClient.OperationSpec,
    ) => {
      let currentRawResponse: coreClient.FullOperationResponse | undefined =
        undefined;
      const providedCallback = args.options?.onResponse;
      const callback: coreClient.RawResponseCallback = (
        rawResponse: coreClient.FullOperationResponse,
        flatResponse: unknown,
      ) => {
        currentRawResponse = rawResponse;
        providedCallback?.(rawResponse, flatResponse);
      };
      const updatedArgs = {
        ...args,
        options: {
          ...args.options,
          onResponse: callback,
        },
      };
      const flatResponse = await directSendOperation(updatedArgs, spec);
      return {
        flatResponse,
        rawResponse: {
          statusCode: currentRawResponse!.status,
          body: currentRawResponse!.parsedBody,
          headers: currentRawResponse!.headers.toJSON(),
        },
      };
    };

    const lro = createLroSpec({
      sendOperationFn,
      args: {
        subscriptionId,
        resourceGroupName,
        modernizeProjectName,
        workloadDeploymentName,
        options,
      },
      spec: buildContainerImageOperationSpec,
    });
    const poller = await createHttpPoller<
      WorkloadDeploymentBuildContainerImageResponse,
      OperationState<WorkloadDeploymentBuildContainerImageResponse>
    >(lro, {
      restoreFrom: options?.resumeFrom,
      intervalInMs: options?.updateIntervalInMs,
      resourceLocationConfig: "location",
    });
    await poller.poll();
    return poller;
  }

  /**
   * Performs the build container image action on the workload deployment.
   * @param subscriptionId Azure Subscription Id in which project was created.
   * @param resourceGroupName Name of the Azure Resource Group that project is part of.
   * @param modernizeProjectName ModernizeProject name.
   * @param workloadDeploymentName Workload deployment name.
   * @param options The options parameters.
   */
  async beginBuildContainerImageAndWait(
    subscriptionId: string,
    resourceGroupName: string,
    modernizeProjectName: string,
    workloadDeploymentName: string,
    options?: WorkloadDeploymentBuildContainerImageOptionalParams,
  ): Promise<WorkloadDeploymentBuildContainerImageResponse> {
    const poller = await this.beginBuildContainerImage(
      subscriptionId,
      resourceGroupName,
      modernizeProjectName,
      workloadDeploymentName,
      options,
    );
    return poller.pollUntilDone();
  }

  /**
   * Performs the test migrate on the workload deployment.
   * @param subscriptionId Azure Subscription Id in which project was created.
   * @param resourceGroupName Name of the Azure Resource Group that project is part of.
   * @param modernizeProjectName ModernizeProject name.
   * @param workloadDeploymentName Workload deployment name.
   * @param options The options parameters.
   */
  async beginTestMigrate(
    subscriptionId: string,
    resourceGroupName: string,
    modernizeProjectName: string,
    workloadDeploymentName: string,
    options?: WorkloadDeploymentTestMigrateOptionalParams,
  ): Promise<
    SimplePollerLike<
      OperationState<WorkloadDeploymentTestMigrateResponse>,
      WorkloadDeploymentTestMigrateResponse
    >
  > {
    const directSendOperation = async (
      args: coreClient.OperationArguments,
      spec: coreClient.OperationSpec,
    ): Promise<WorkloadDeploymentTestMigrateResponse> => {
      return this.client.sendOperationRequest(args, spec);
    };
    const sendOperationFn = async (
      args: coreClient.OperationArguments,
      spec: coreClient.OperationSpec,
    ) => {
      let currentRawResponse: coreClient.FullOperationResponse | undefined =
        undefined;
      const providedCallback = args.options?.onResponse;
      const callback: coreClient.RawResponseCallback = (
        rawResponse: coreClient.FullOperationResponse,
        flatResponse: unknown,
      ) => {
        currentRawResponse = rawResponse;
        providedCallback?.(rawResponse, flatResponse);
      };
      const updatedArgs = {
        ...args,
        options: {
          ...args.options,
          onResponse: callback,
        },
      };
      const flatResponse = await directSendOperation(updatedArgs, spec);
      return {
        flatResponse,
        rawResponse: {
          statusCode: currentRawResponse!.status,
          body: currentRawResponse!.parsedBody,
          headers: currentRawResponse!.headers.toJSON(),
        },
      };
    };

    const lro = createLroSpec({
      sendOperationFn,
      args: {
        subscriptionId,
        resourceGroupName,
        modernizeProjectName,
        workloadDeploymentName,
        options,
      },
      spec: testMigrateOperationSpec,
    });
    const poller = await createHttpPoller<
      WorkloadDeploymentTestMigrateResponse,
      OperationState<WorkloadDeploymentTestMigrateResponse>
    >(lro, {
      restoreFrom: options?.resumeFrom,
      intervalInMs: options?.updateIntervalInMs,
      resourceLocationConfig: "location",
    });
    await poller.poll();
    return poller;
  }

  /**
   * Performs the test migrate on the workload deployment.
   * @param subscriptionId Azure Subscription Id in which project was created.
   * @param resourceGroupName Name of the Azure Resource Group that project is part of.
   * @param modernizeProjectName ModernizeProject name.
   * @param workloadDeploymentName Workload deployment name.
   * @param options The options parameters.
   */
  async beginTestMigrateAndWait(
    subscriptionId: string,
    resourceGroupName: string,
    modernizeProjectName: string,
    workloadDeploymentName: string,
    options?: WorkloadDeploymentTestMigrateOptionalParams,
  ): Promise<WorkloadDeploymentTestMigrateResponse> {
    const poller = await this.beginTestMigrate(
      subscriptionId,
      resourceGroupName,
      modernizeProjectName,
      workloadDeploymentName,
      options,
    );
    return poller.pollUntilDone();
  }

  /**
   * Performs the test migrate cleanup on the workload deployment.
   * @param subscriptionId Azure Subscription Id in which project was created.
   * @param resourceGroupName Name of the Azure Resource Group that project is part of.
   * @param modernizeProjectName ModernizeProject name.
   * @param workloadDeploymentName Workload deployment name.
   * @param options The options parameters.
   */
  async beginTestMigrateCleanup(
    subscriptionId: string,
    resourceGroupName: string,
    modernizeProjectName: string,
    workloadDeploymentName: string,
    options?: WorkloadDeploymentTestMigrateCleanupOptionalParams,
  ): Promise<
    SimplePollerLike<
      OperationState<WorkloadDeploymentTestMigrateCleanupResponse>,
      WorkloadDeploymentTestMigrateCleanupResponse
    >
  > {
    const directSendOperation = async (
      args: coreClient.OperationArguments,
      spec: coreClient.OperationSpec,
    ): Promise<WorkloadDeploymentTestMigrateCleanupResponse> => {
      return this.client.sendOperationRequest(args, spec);
    };
    const sendOperationFn = async (
      args: coreClient.OperationArguments,
      spec: coreClient.OperationSpec,
    ) => {
      let currentRawResponse: coreClient.FullOperationResponse | undefined =
        undefined;
      const providedCallback = args.options?.onResponse;
      const callback: coreClient.RawResponseCallback = (
        rawResponse: coreClient.FullOperationResponse,
        flatResponse: unknown,
      ) => {
        currentRawResponse = rawResponse;
        providedCallback?.(rawResponse, flatResponse);
      };
      const updatedArgs = {
        ...args,
        options: {
          ...args.options,
          onResponse: callback,
        },
      };
      const flatResponse = await directSendOperation(updatedArgs, spec);
      return {
        flatResponse,
        rawResponse: {
          statusCode: currentRawResponse!.status,
          body: currentRawResponse!.parsedBody,
          headers: currentRawResponse!.headers.toJSON(),
        },
      };
    };

    const lro = createLroSpec({
      sendOperationFn,
      args: {
        subscriptionId,
        resourceGroupName,
        modernizeProjectName,
        workloadDeploymentName,
        options,
      },
      spec: testMigrateCleanupOperationSpec,
    });
    const poller = await createHttpPoller<
      WorkloadDeploymentTestMigrateCleanupResponse,
      OperationState<WorkloadDeploymentTestMigrateCleanupResponse>
    >(lro, {
      restoreFrom: options?.resumeFrom,
      intervalInMs: options?.updateIntervalInMs,
      resourceLocationConfig: "location",
    });
    await poller.poll();
    return poller;
  }

  /**
   * Performs the test migrate cleanup on the workload deployment.
   * @param subscriptionId Azure Subscription Id in which project was created.
   * @param resourceGroupName Name of the Azure Resource Group that project is part of.
   * @param modernizeProjectName ModernizeProject name.
   * @param workloadDeploymentName Workload deployment name.
   * @param options The options parameters.
   */
  async beginTestMigrateCleanupAndWait(
    subscriptionId: string,
    resourceGroupName: string,
    modernizeProjectName: string,
    workloadDeploymentName: string,
    options?: WorkloadDeploymentTestMigrateCleanupOptionalParams,
  ): Promise<WorkloadDeploymentTestMigrateCleanupResponse> {
    const poller = await this.beginTestMigrateCleanup(
      subscriptionId,
      resourceGroupName,
      modernizeProjectName,
      workloadDeploymentName,
      options,
    );
    return poller.pollUntilDone();
  }

  /**
   * Performs migrate on the workload deployment.
   * @param subscriptionId Azure Subscription Id in which project was created.
   * @param resourceGroupName Name of the Azure Resource Group that project is part of.
   * @param modernizeProjectName ModernizeProject name.
   * @param workloadDeploymentName Workload deployment name.
   * @param options The options parameters.
   */
  async beginMigrate(
    subscriptionId: string,
    resourceGroupName: string,
    modernizeProjectName: string,
    workloadDeploymentName: string,
    options?: WorkloadDeploymentMigrateOptionalParams,
  ): Promise<
    SimplePollerLike<
      OperationState<WorkloadDeploymentMigrateResponse>,
      WorkloadDeploymentMigrateResponse
    >
  > {
    const directSendOperation = async (
      args: coreClient.OperationArguments,
      spec: coreClient.OperationSpec,
    ): Promise<WorkloadDeploymentMigrateResponse> => {
      return this.client.sendOperationRequest(args, spec);
    };
    const sendOperationFn = async (
      args: coreClient.OperationArguments,
      spec: coreClient.OperationSpec,
    ) => {
      let currentRawResponse: coreClient.FullOperationResponse | undefined =
        undefined;
      const providedCallback = args.options?.onResponse;
      const callback: coreClient.RawResponseCallback = (
        rawResponse: coreClient.FullOperationResponse,
        flatResponse: unknown,
      ) => {
        currentRawResponse = rawResponse;
        providedCallback?.(rawResponse, flatResponse);
      };
      const updatedArgs = {
        ...args,
        options: {
          ...args.options,
          onResponse: callback,
        },
      };
      const flatResponse = await directSendOperation(updatedArgs, spec);
      return {
        flatResponse,
        rawResponse: {
          statusCode: currentRawResponse!.status,
          body: currentRawResponse!.parsedBody,
          headers: currentRawResponse!.headers.toJSON(),
        },
      };
    };

    const lro = createLroSpec({
      sendOperationFn,
      args: {
        subscriptionId,
        resourceGroupName,
        modernizeProjectName,
        workloadDeploymentName,
        options,
      },
      spec: migrateOperationSpec,
    });
    const poller = await createHttpPoller<
      WorkloadDeploymentMigrateResponse,
      OperationState<WorkloadDeploymentMigrateResponse>
    >(lro, {
      restoreFrom: options?.resumeFrom,
      intervalInMs: options?.updateIntervalInMs,
      resourceLocationConfig: "location",
    });
    await poller.poll();
    return poller;
  }

  /**
   * Performs migrate on the workload deployment.
   * @param subscriptionId Azure Subscription Id in which project was created.
   * @param resourceGroupName Name of the Azure Resource Group that project is part of.
   * @param modernizeProjectName ModernizeProject name.
   * @param workloadDeploymentName Workload deployment name.
   * @param options The options parameters.
   */
  async beginMigrateAndWait(
    subscriptionId: string,
    resourceGroupName: string,
    modernizeProjectName: string,
    workloadDeploymentName: string,
    options?: WorkloadDeploymentMigrateOptionalParams,
  ): Promise<WorkloadDeploymentMigrateResponse> {
    const poller = await this.beginMigrate(
      subscriptionId,
      resourceGroupName,
      modernizeProjectName,
      workloadDeploymentName,
      options,
    );
    return poller.pollUntilDone();
  }

  /**
   * ListNext
   * @param subscriptionId Azure Subscription Id in which project was created.
   * @param resourceGroupName Name of the Azure Resource Group that project is part of.
   * @param modernizeProjectName ModernizeProject name.
   * @param nextLink The nextLink from the previous successful call to the List method.
   * @param options The options parameters.
   */
  private _listNext(
    subscriptionId: string,
    resourceGroupName: string,
    modernizeProjectName: string,
    nextLink: string,
    options?: WorkloadDeploymentListNextOptionalParams,
  ): Promise<WorkloadDeploymentListNextResponse> {
    return this.client.sendOperationRequest(
      {
        subscriptionId,
        resourceGroupName,
        modernizeProjectName,
        nextLink,
        options,
      },
      listNextOperationSpec,
    );
  }
}
// Operation Specifications
const serializer = coreClient.createSerializer(Mappers, /* isXml */ false);

const getOperationSpec: coreClient.OperationSpec = {
  path: "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Migrate/modernizeProjects/{modernizeProjectName}/workloadDeployments/{workloadDeploymentName}",
  httpMethod: "GET",
  responses: {
    200: {
      bodyMapper: Mappers.WorkloadDeploymentModel,
    },
    default: {
      bodyMapper: Mappers.ArmError,
    },
  },
  queryParameters: [Parameters.apiVersion],
  urlParameters: [
    Parameters.$host,
    Parameters.subscriptionId,
    Parameters.resourceGroupName,
    Parameters.modernizeProjectName,
    Parameters.workloadDeploymentName,
  ],
  headerParameters: [Parameters.accept],
  serializer,
};
const createOperationSpec: coreClient.OperationSpec = {
  path: "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Migrate/modernizeProjects/{modernizeProjectName}/workloadDeployments/{workloadDeploymentName}",
  httpMethod: "PUT",
  responses: {
    201: {
      bodyMapper: Mappers.WorkloadDeploymentModel,
    },
    default: {
      bodyMapper: Mappers.ArmError,
    },
  },
  requestBody: Parameters.body3,
  queryParameters: [Parameters.apiVersion],
  urlParameters: [
    Parameters.$host,
    Parameters.subscriptionId,
    Parameters.resourceGroupName,
    Parameters.modernizeProjectName,
    Parameters.workloadDeploymentName,
  ],
  headerParameters: [Parameters.accept, Parameters.contentType],
  mediaType: "json",
  serializer,
};
const deleteOperationSpec: coreClient.OperationSpec = {
  path: "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Migrate/modernizeProjects/{modernizeProjectName}/workloadDeployments/{workloadDeploymentName}",
  httpMethod: "DELETE",
  responses: {
    200: {
      headersMapper: Mappers.WorkloadDeploymentDeleteHeaders,
    },
    201: {
      headersMapper: Mappers.WorkloadDeploymentDeleteHeaders,
    },
    202: {
      headersMapper: Mappers.WorkloadDeploymentDeleteHeaders,
    },
    204: {
      headersMapper: Mappers.WorkloadDeploymentDeleteHeaders,
    },
    default: {
      bodyMapper: Mappers.ArmError,
    },
  },
  queryParameters: [Parameters.apiVersion, Parameters.forceDelete],
  urlParameters: [
    Parameters.$host,
    Parameters.subscriptionId,
    Parameters.resourceGroupName,
    Parameters.modernizeProjectName,
    Parameters.workloadDeploymentName,
  ],
  headerParameters: [Parameters.accept],
  serializer,
};
const listOperationSpec: coreClient.OperationSpec = {
  path: "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Migrate/modernizeProjects/{modernizeProjectName}/workloadDeployments",
  httpMethod: "GET",
  responses: {
    200: {
      bodyMapper: Mappers.WorkloadDeploymentModelCollection,
    },
    default: {
      bodyMapper: Mappers.ArmError,
    },
  },
  queryParameters: [Parameters.apiVersion],
  urlParameters: [
    Parameters.$host,
    Parameters.subscriptionId,
    Parameters.resourceGroupName,
    Parameters.modernizeProjectName,
  ],
  headerParameters: [Parameters.accept],
  serializer,
};
const getSecretConfigurationsOperationSpec: coreClient.OperationSpec = {
  path: "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Migrate/modernizeProjects/{modernizeProjectName}/workloadDeployments/{workloadDeploymentName}/getSecrets",
  httpMethod: "POST",
  responses: {
    200: {
      bodyMapper: Mappers.WorkloadDeploymentModel,
    },
    default: {
      bodyMapper: Mappers.ArmError,
    },
  },
  queryParameters: [Parameters.apiVersion],
  urlParameters: [
    Parameters.$host,
    Parameters.subscriptionId,
    Parameters.resourceGroupName,
    Parameters.modernizeProjectName,
    Parameters.workloadDeploymentName,
  ],
  headerParameters: [Parameters.accept],
  serializer,
};
const buildContainerImageOperationSpec: coreClient.OperationSpec = {
  path: "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Migrate/modernizeProjects/{modernizeProjectName}/workloadDeployments/{workloadDeploymentName}/buildContainerImage",
  httpMethod: "POST",
  responses: {
    200: {
      headersMapper: Mappers.WorkloadDeploymentBuildContainerImageHeaders,
    },
    201: {
      headersMapper: Mappers.WorkloadDeploymentBuildContainerImageHeaders,
    },
    202: {
      headersMapper: Mappers.WorkloadDeploymentBuildContainerImageHeaders,
    },
    204: {
      headersMapper: Mappers.WorkloadDeploymentBuildContainerImageHeaders,
    },
    default: {
      bodyMapper: Mappers.ArmError,
    },
  },
  requestBody: Parameters.body4,
  queryParameters: [Parameters.apiVersion],
  urlParameters: [
    Parameters.$host,
    Parameters.subscriptionId,
    Parameters.resourceGroupName,
    Parameters.modernizeProjectName,
    Parameters.workloadDeploymentName,
  ],
  headerParameters: [Parameters.accept, Parameters.contentType],
  mediaType: "json",
  serializer,
};
const testMigrateOperationSpec: coreClient.OperationSpec = {
  path: "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Migrate/modernizeProjects/{modernizeProjectName}/workloadDeployments/{workloadDeploymentName}/testMigrate",
  httpMethod: "POST",
  responses: {
    200: {
      headersMapper: Mappers.WorkloadDeploymentTestMigrateHeaders,
    },
    201: {
      headersMapper: Mappers.WorkloadDeploymentTestMigrateHeaders,
    },
    202: {
      headersMapper: Mappers.WorkloadDeploymentTestMigrateHeaders,
    },
    204: {
      headersMapper: Mappers.WorkloadDeploymentTestMigrateHeaders,
    },
    default: {
      bodyMapper: Mappers.ArmError,
    },
  },
  requestBody: Parameters.body5,
  queryParameters: [Parameters.apiVersion],
  urlParameters: [
    Parameters.$host,
    Parameters.subscriptionId,
    Parameters.resourceGroupName,
    Parameters.modernizeProjectName,
    Parameters.workloadDeploymentName,
  ],
  headerParameters: [Parameters.accept, Parameters.contentType],
  mediaType: "json",
  serializer,
};
const testMigrateCleanupOperationSpec: coreClient.OperationSpec = {
  path: "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Migrate/modernizeProjects/{modernizeProjectName}/workloadDeployments/{workloadDeploymentName}/testMigrateCleanup",
  httpMethod: "POST",
  responses: {
    200: {
      headersMapper: Mappers.WorkloadDeploymentTestMigrateCleanupHeaders,
    },
    201: {
      headersMapper: Mappers.WorkloadDeploymentTestMigrateCleanupHeaders,
    },
    202: {
      headersMapper: Mappers.WorkloadDeploymentTestMigrateCleanupHeaders,
    },
    204: {
      headersMapper: Mappers.WorkloadDeploymentTestMigrateCleanupHeaders,
    },
    default: {
      bodyMapper: Mappers.ArmError,
    },
  },
  queryParameters: [Parameters.apiVersion],
  urlParameters: [
    Parameters.$host,
    Parameters.subscriptionId,
    Parameters.resourceGroupName,
    Parameters.modernizeProjectName,
    Parameters.workloadDeploymentName,
  ],
  headerParameters: [Parameters.accept],
  serializer,
};
const migrateOperationSpec: coreClient.OperationSpec = {
  path: "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Migrate/modernizeProjects/{modernizeProjectName}/workloadDeployments/{workloadDeploymentName}/migrate",
  httpMethod: "POST",
  responses: {
    200: {
      headersMapper: Mappers.WorkloadDeploymentMigrateHeaders,
    },
    201: {
      headersMapper: Mappers.WorkloadDeploymentMigrateHeaders,
    },
    202: {
      headersMapper: Mappers.WorkloadDeploymentMigrateHeaders,
    },
    204: {
      headersMapper: Mappers.WorkloadDeploymentMigrateHeaders,
    },
    default: {
      bodyMapper: Mappers.ArmError,
    },
  },
  queryParameters: [Parameters.apiVersion],
  urlParameters: [
    Parameters.$host,
    Parameters.subscriptionId,
    Parameters.resourceGroupName,
    Parameters.modernizeProjectName,
    Parameters.workloadDeploymentName,
  ],
  headerParameters: [Parameters.accept],
  serializer,
};
const listNextOperationSpec: coreClient.OperationSpec = {
  path: "{nextLink}",
  httpMethod: "GET",
  responses: {
    200: {
      bodyMapper: Mappers.WorkloadDeploymentModelCollection,
    },
    default: {
      bodyMapper: Mappers.ArmError,
    },
  },
  urlParameters: [
    Parameters.$host,
    Parameters.nextLink,
    Parameters.subscriptionId,
    Parameters.resourceGroupName,
    Parameters.modernizeProjectName,
  ],
  headerParameters: [Parameters.accept],
  serializer,
};
