/*
 * Copyright (c) Microsoft Corporation.
 * Licensed under the MIT License.
 *
 * Code generated by Microsoft (R) AutoRest Code Generator.
 * Changes may cause incorrect behavior and will be lost if the code is regenerated.
 */

import { PagedAsyncIterableIterator, PageSettings } from "@azure/core-paging";
import { setContinuationToken } from "../pagingHelper";
import { OutboundRules } from "../operationsInterfaces";
import * as coreClient from "@azure/core-client";
import * as Mappers from "../models/mappers";
import * as Parameters from "../models/parameters";
import { AzureMachineLearningServicesManagementClient } from "../azureMachineLearningServicesManagementClient";
import {
  SimplePollerLike,
  OperationState,
  createHttpPoller,
} from "@azure/core-lro";
import { createLroSpec } from "../lroImpl";
import {
  OutboundRuleBasicResource,
  ManagedNetworkSettingsBasicResource,
  OutboundRulesPostNextOptionalParams,
  OutboundRulesPostOptionalParams,
  OutboundRulesPostResponse,
  OutboundRulesPostNextResponse,
} from "../models";

/// <reference lib="esnext.asynciterable" />
/** Class containing OutboundRules operations. */
export class OutboundRulesImpl implements OutboundRules {
  private readonly client: AzureMachineLearningServicesManagementClient;

  /**
   * Initialize a new instance of the class OutboundRules class.
   * @param client Reference to the service client
   */
  constructor(client: AzureMachineLearningServicesManagementClient) {
    this.client = client;
  }

  /**
   * The POST API for updating the outbound rules of the managed network associated with the machine
   * learning workspace.
   * @param resourceGroupName The name of the resource group. The name is case insensitive.
   * @param workspaceName Azure Machine Learning Workspace Name
   * @param managedNetworkName Name of the managedNetwork associated with the workspace. Only 'default'
   *                           is supported.
   * @param body The Managed Network Settings object of the workspace.
   * @param options The options parameters.
   */
  public beginListPostAndWait(
    resourceGroupName: string,
    workspaceName: string,
    managedNetworkName: string,
    body: ManagedNetworkSettingsBasicResource,
    options?: OutboundRulesPostOptionalParams,
  ): PagedAsyncIterableIterator<OutboundRuleBasicResource> {
    const iter = this.postPagingAll(
      resourceGroupName,
      workspaceName,
      managedNetworkName,
      body,
      options,
    );
    return {
      next() {
        return iter.next();
      },
      [Symbol.asyncIterator]() {
        return this;
      },
      byPage: (settings?: PageSettings) => {
        if (settings?.maxPageSize) {
          throw new Error("maxPageSize is not supported by this operation.");
        }
        return this.postPagingPage(
          resourceGroupName,
          workspaceName,
          managedNetworkName,
          body,
          options,
          settings,
        );
      },
    };
  }

  private async *postPagingPage(
    resourceGroupName: string,
    workspaceName: string,
    managedNetworkName: string,
    body: ManagedNetworkSettingsBasicResource,
    options?: OutboundRulesPostOptionalParams,
    settings?: PageSettings,
  ): AsyncIterableIterator<OutboundRuleBasicResource[]> {
    let result: OutboundRulesPostResponse;
    let continuationToken = settings?.continuationToken;
    if (!continuationToken) {
      const poller = await this._post(
        resourceGroupName,
        workspaceName,
        managedNetworkName,
        body,
        options,
      );
      result = await poller.pollUntilDone();
      let page = result.value || [];
      continuationToken = result.nextLink;
      setContinuationToken(page, continuationToken);
      yield page;
    }
    while (continuationToken) {
      result = await this._postNext(
        resourceGroupName,
        workspaceName,
        managedNetworkName,
        body,
        continuationToken,
        options,
      );
      continuationToken = result.nextLink;
      let page = result.value || [];
      setContinuationToken(page, continuationToken);
      yield page;
    }
  }

  private async *postPagingAll(
    resourceGroupName: string,
    workspaceName: string,
    managedNetworkName: string,
    body: ManagedNetworkSettingsBasicResource,
    options?: OutboundRulesPostOptionalParams,
  ): AsyncIterableIterator<OutboundRuleBasicResource> {
    for await (const page of this.postPagingPage(
      resourceGroupName,
      workspaceName,
      managedNetworkName,
      body,
      options,
    )) {
      yield* page;
    }
  }

  /**
   * The POST API for updating the outbound rules of the managed network associated with the machine
   * learning workspace.
   * @param resourceGroupName The name of the resource group. The name is case insensitive.
   * @param workspaceName Azure Machine Learning Workspace Name
   * @param managedNetworkName Name of the managedNetwork associated with the workspace. Only 'default'
   *                           is supported.
   * @param body The Managed Network Settings object of the workspace.
   * @param options The options parameters.
   */
  private async _post(
    resourceGroupName: string,
    workspaceName: string,
    managedNetworkName: string,
    body: ManagedNetworkSettingsBasicResource,
    options?: OutboundRulesPostOptionalParams,
  ): Promise<
    SimplePollerLike<
      OperationState<OutboundRulesPostResponse>,
      OutboundRulesPostResponse
    >
  > {
    const directSendOperation = async (
      args: coreClient.OperationArguments,
      spec: coreClient.OperationSpec,
    ): Promise<OutboundRulesPostResponse> => {
      return this.client.sendOperationRequest(args, spec);
    };
    const sendOperationFn = async (
      args: coreClient.OperationArguments,
      spec: coreClient.OperationSpec,
    ) => {
      let currentRawResponse: coreClient.FullOperationResponse | undefined =
        undefined;
      const providedCallback = args.options?.onResponse;
      const callback: coreClient.RawResponseCallback = (
        rawResponse: coreClient.FullOperationResponse,
        flatResponse: unknown,
      ) => {
        currentRawResponse = rawResponse;
        providedCallback?.(rawResponse, flatResponse);
      };
      const updatedArgs = {
        ...args,
        options: {
          ...args.options,
          onResponse: callback,
        },
      };
      const flatResponse = await directSendOperation(updatedArgs, spec);
      return {
        flatResponse,
        rawResponse: {
          statusCode: currentRawResponse!.status,
          body: currentRawResponse!.parsedBody,
          headers: currentRawResponse!.headers.toJSON(),
        },
      };
    };

    const lro = createLroSpec({
      sendOperationFn,
      args: {
        resourceGroupName,
        workspaceName,
        managedNetworkName,
        body,
        options,
      },
      spec: postOperationSpec,
    });
    const poller = await createHttpPoller<
      OutboundRulesPostResponse,
      OperationState<OutboundRulesPostResponse>
    >(lro, {
      restoreFrom: options?.resumeFrom,
      intervalInMs: options?.updateIntervalInMs,
      resourceLocationConfig: "location",
    });
    await poller.poll();
    return poller;
  }

  /**
   * PostNext
   * @param resourceGroupName The name of the resource group. The name is case insensitive.
   * @param workspaceName Azure Machine Learning Workspace Name
   * @param managedNetworkName Name of the managedNetwork associated with the workspace. Only 'default'
   *                           is supported.
   * @param body The Managed Network Settings object of the workspace.
   * @param nextLink The nextLink from the previous successful call to the Post method.
   * @param options The options parameters.
   */
  private _postNext(
    resourceGroupName: string,
    workspaceName: string,
    managedNetworkName: string,
    body: ManagedNetworkSettingsBasicResource,
    nextLink: string,
    options?: OutboundRulesPostNextOptionalParams,
  ): Promise<OutboundRulesPostNextResponse> {
    return this.client.sendOperationRequest(
      {
        resourceGroupName,
        workspaceName,
        managedNetworkName,
        body,
        nextLink,
        options,
      },
      postNextOperationSpec,
    );
  }
}
// Operation Specifications
const serializer = coreClient.createSerializer(Mappers, /* isXml */ false);

const postOperationSpec: coreClient.OperationSpec = {
  path: "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.MachineLearningServices/workspaces/{workspaceName}/managedNetworks/{managedNetworkName}/batchOutboundRules",
  httpMethod: "POST",
  responses: {
    200: {
      bodyMapper: Mappers.OutboundRuleListResult,
    },
    201: {
      bodyMapper: Mappers.OutboundRuleListResult,
    },
    202: {
      bodyMapper: Mappers.OutboundRuleListResult,
    },
    204: {
      bodyMapper: Mappers.OutboundRuleListResult,
    },
    default: {
      bodyMapper: Mappers.ErrorResponse,
    },
  },
  requestBody: Parameters.body60,
  queryParameters: [Parameters.apiVersion],
  urlParameters: [
    Parameters.$host,
    Parameters.subscriptionId,
    Parameters.resourceGroupName,
    Parameters.workspaceName,
    Parameters.managedNetworkName,
  ],
  headerParameters: [Parameters.accept, Parameters.contentType],
  mediaType: "json",
  serializer,
};
const postNextOperationSpec: coreClient.OperationSpec = {
  path: "{nextLink}",
  httpMethod: "GET",
  responses: {
    200: {
      bodyMapper: Mappers.OutboundRuleListResult,
    },
    202: {
      headersMapper: Mappers.OutboundRulesPostNextHeaders,
    },
    default: {
      bodyMapper: Mappers.ErrorResponse,
    },
  },
  urlParameters: [
    Parameters.$host,
    Parameters.subscriptionId,
    Parameters.nextLink,
    Parameters.resourceGroupName,
    Parameters.workspaceName,
    Parameters.managedNetworkName,
  ],
  headerParameters: [Parameters.accept, Parameters.contentType],
  mediaType: "json",
  serializer,
};
