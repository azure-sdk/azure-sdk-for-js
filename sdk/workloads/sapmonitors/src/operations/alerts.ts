/*
 * Copyright (c) Microsoft Corporation.
 * Licensed under the MIT License.
 *
 * Code generated by Microsoft (R) AutoRest Code Generator.
 * Changes may cause incorrect behavior and will be lost if the code is regenerated.
 */

import { PagedAsyncIterableIterator, PageSettings } from "@azure/core-paging";
import { setContinuationToken } from "../pagingHelper";
import { Alerts } from "../operationsInterfaces";
import * as coreClient from "@azure/core-client";
import * as Mappers from "../models/mappers";
import * as Parameters from "../models/parameters";
import { WorkloadsClient } from "../workloadsClient";
import {
  SimplePollerLike,
  OperationState,
  createHttpPoller,
} from "@azure/core-lro";
import { createLroSpec } from "../lroImpl";
import {
  Alert,
  AlertsListByMonitorNextOptionalParams,
  AlertsListByMonitorOptionalParams,
  AlertsListByMonitorResponse,
  AlertsGetOptionalParams,
  AlertsGetResponse,
  AlertsCreateOptionalParams,
  AlertsCreateResponse,
  AlertsDeleteOptionalParams,
  AlertsDeleteResponse,
  AlertsListByMonitorNextResponse,
} from "../models";

/// <reference lib="esnext.asynciterable" />
/** Class containing Alerts operations. */
export class AlertsImpl implements Alerts {
  private readonly client: WorkloadsClient;

  /**
   * Initialize a new instance of the class Alerts class.
   * @param client Reference to the service client
   */
  constructor(client: WorkloadsClient) {
    this.client = client;
  }

  /**
   * Gets a list of Alert in the specified SAP monitor. The operations returns various properties of each
   * provider instances.
   * @param resourceGroupName The name of the resource group. The name is case insensitive.
   * @param monitorName Name of the SAP monitor resource.
   * @param options The options parameters.
   */
  public listByMonitor(
    resourceGroupName: string,
    monitorName: string,
    options?: AlertsListByMonitorOptionalParams,
  ): PagedAsyncIterableIterator<Alert> {
    const iter = this.listByMonitorPagingAll(
      resourceGroupName,
      monitorName,
      options,
    );
    return {
      next() {
        return iter.next();
      },
      [Symbol.asyncIterator]() {
        return this;
      },
      byPage: (settings?: PageSettings) => {
        if (settings?.maxPageSize) {
          throw new Error("maxPageSize is not supported by this operation.");
        }
        return this.listByMonitorPagingPage(
          resourceGroupName,
          monitorName,
          options,
          settings,
        );
      },
    };
  }

  private async *listByMonitorPagingPage(
    resourceGroupName: string,
    monitorName: string,
    options?: AlertsListByMonitorOptionalParams,
    settings?: PageSettings,
  ): AsyncIterableIterator<Alert[]> {
    let result: AlertsListByMonitorResponse;
    let continuationToken = settings?.continuationToken;
    if (!continuationToken) {
      result = await this._listByMonitor(
        resourceGroupName,
        monitorName,
        options,
      );
      let page = result.value || [];
      continuationToken = result.nextLink;
      setContinuationToken(page, continuationToken);
      yield page;
    }
    while (continuationToken) {
      result = await this._listByMonitorNext(
        resourceGroupName,
        monitorName,
        continuationToken,
        options,
      );
      continuationToken = result.nextLink;
      let page = result.value || [];
      setContinuationToken(page, continuationToken);
      yield page;
    }
  }

  private async *listByMonitorPagingAll(
    resourceGroupName: string,
    monitorName: string,
    options?: AlertsListByMonitorOptionalParams,
  ): AsyncIterableIterator<Alert> {
    for await (const page of this.listByMonitorPagingPage(
      resourceGroupName,
      monitorName,
      options,
    )) {
      yield* page;
    }
  }

  /**
   * Gets a list of Alert in the specified SAP monitor. The operations returns various properties of each
   * provider instances.
   * @param resourceGroupName The name of the resource group. The name is case insensitive.
   * @param monitorName Name of the SAP monitor resource.
   * @param options The options parameters.
   */
  private _listByMonitor(
    resourceGroupName: string,
    monitorName: string,
    options?: AlertsListByMonitorOptionalParams,
  ): Promise<AlertsListByMonitorResponse> {
    return this.client.sendOperationRequest(
      { resourceGroupName, monitorName, options },
      listByMonitorOperationSpec,
    );
  }

  /**
   * Gets properties of an Alert for the specified subscription, resource group, SAP monitor name, and
   * resource name.
   * @param resourceGroupName The name of the resource group. The name is case insensitive.
   * @param monitorName Name of the SAP monitor resource.
   * @param alert Name of the provider instance.
   * @param options The options parameters.
   */
  get(
    resourceGroupName: string,
    monitorName: string,
    alert: string,
    options?: AlertsGetOptionalParams,
  ): Promise<AlertsGetResponse> {
    return this.client.sendOperationRequest(
      { resourceGroupName, monitorName, alert, options },
      getOperationSpec,
    );
  }

  /**
   * Creates An Alert for the specified subscription, resource group, SAP monitor name, and resource
   * name.
   * @param resourceGroupName The name of the resource group. The name is case insensitive.
   * @param monitorName Name of the SAP monitor resource.
   * @param alert Name of the provider instance.
   * @param resource Resource create parameters.
   * @param options The options parameters.
   */
  async beginCreate(
    resourceGroupName: string,
    monitorName: string,
    alert: string,
    resource: Alert,
    options?: AlertsCreateOptionalParams,
  ): Promise<
    SimplePollerLike<OperationState<AlertsCreateResponse>, AlertsCreateResponse>
  > {
    const directSendOperation = async (
      args: coreClient.OperationArguments,
      spec: coreClient.OperationSpec,
    ): Promise<AlertsCreateResponse> => {
      return this.client.sendOperationRequest(args, spec);
    };
    const sendOperationFn = async (
      args: coreClient.OperationArguments,
      spec: coreClient.OperationSpec,
    ) => {
      let currentRawResponse: coreClient.FullOperationResponse | undefined =
        undefined;
      const providedCallback = args.options?.onResponse;
      const callback: coreClient.RawResponseCallback = (
        rawResponse: coreClient.FullOperationResponse,
        flatResponse: unknown,
      ) => {
        currentRawResponse = rawResponse;
        providedCallback?.(rawResponse, flatResponse);
      };
      const updatedArgs = {
        ...args,
        options: {
          ...args.options,
          onResponse: callback,
        },
      };
      const flatResponse = await directSendOperation(updatedArgs, spec);
      return {
        flatResponse,
        rawResponse: {
          statusCode: currentRawResponse!.status,
          body: currentRawResponse!.parsedBody,
          headers: currentRawResponse!.headers.toJSON(),
        },
      };
    };

    const lro = createLroSpec({
      sendOperationFn,
      args: { resourceGroupName, monitorName, alert, resource, options },
      spec: createOperationSpec,
    });
    const poller = await createHttpPoller<
      AlertsCreateResponse,
      OperationState<AlertsCreateResponse>
    >(lro, {
      restoreFrom: options?.resumeFrom,
      intervalInMs: options?.updateIntervalInMs,
      resourceLocationConfig: "azure-async-operation",
    });
    await poller.poll();
    return poller;
  }

  /**
   * Creates An Alert for the specified subscription, resource group, SAP monitor name, and resource
   * name.
   * @param resourceGroupName The name of the resource group. The name is case insensitive.
   * @param monitorName Name of the SAP monitor resource.
   * @param alert Name of the provider instance.
   * @param resource Resource create parameters.
   * @param options The options parameters.
   */
  async beginCreateAndWait(
    resourceGroupName: string,
    monitorName: string,
    alert: string,
    resource: Alert,
    options?: AlertsCreateOptionalParams,
  ): Promise<AlertsCreateResponse> {
    const poller = await this.beginCreate(
      resourceGroupName,
      monitorName,
      alert,
      resource,
      options,
    );
    return poller.pollUntilDone();
  }

  /**
   * Deletes a Alert for the specified subscription, resource group, SAP monitor name, and resource name.
   * @param resourceGroupName The name of the resource group. The name is case insensitive.
   * @param monitorName Name of the SAP monitor resource.
   * @param alert Name of the provider instance.
   * @param options The options parameters.
   */
  async beginDelete(
    resourceGroupName: string,
    monitorName: string,
    alert: string,
    options?: AlertsDeleteOptionalParams,
  ): Promise<
    SimplePollerLike<OperationState<AlertsDeleteResponse>, AlertsDeleteResponse>
  > {
    const directSendOperation = async (
      args: coreClient.OperationArguments,
      spec: coreClient.OperationSpec,
    ): Promise<AlertsDeleteResponse> => {
      return this.client.sendOperationRequest(args, spec);
    };
    const sendOperationFn = async (
      args: coreClient.OperationArguments,
      spec: coreClient.OperationSpec,
    ) => {
      let currentRawResponse: coreClient.FullOperationResponse | undefined =
        undefined;
      const providedCallback = args.options?.onResponse;
      const callback: coreClient.RawResponseCallback = (
        rawResponse: coreClient.FullOperationResponse,
        flatResponse: unknown,
      ) => {
        currentRawResponse = rawResponse;
        providedCallback?.(rawResponse, flatResponse);
      };
      const updatedArgs = {
        ...args,
        options: {
          ...args.options,
          onResponse: callback,
        },
      };
      const flatResponse = await directSendOperation(updatedArgs, spec);
      return {
        flatResponse,
        rawResponse: {
          statusCode: currentRawResponse!.status,
          body: currentRawResponse!.parsedBody,
          headers: currentRawResponse!.headers.toJSON(),
        },
      };
    };

    const lro = createLroSpec({
      sendOperationFn,
      args: { resourceGroupName, monitorName, alert, options },
      spec: deleteOperationSpec,
    });
    const poller = await createHttpPoller<
      AlertsDeleteResponse,
      OperationState<AlertsDeleteResponse>
    >(lro, {
      restoreFrom: options?.resumeFrom,
      intervalInMs: options?.updateIntervalInMs,
      resourceLocationConfig: "location",
    });
    await poller.poll();
    return poller;
  }

  /**
   * Deletes a Alert for the specified subscription, resource group, SAP monitor name, and resource name.
   * @param resourceGroupName The name of the resource group. The name is case insensitive.
   * @param monitorName Name of the SAP monitor resource.
   * @param alert Name of the provider instance.
   * @param options The options parameters.
   */
  async beginDeleteAndWait(
    resourceGroupName: string,
    monitorName: string,
    alert: string,
    options?: AlertsDeleteOptionalParams,
  ): Promise<AlertsDeleteResponse> {
    const poller = await this.beginDelete(
      resourceGroupName,
      monitorName,
      alert,
      options,
    );
    return poller.pollUntilDone();
  }

  /**
   * ListByMonitorNext
   * @param resourceGroupName The name of the resource group. The name is case insensitive.
   * @param monitorName Name of the SAP monitor resource.
   * @param nextLink The nextLink from the previous successful call to the ListByMonitor method.
   * @param options The options parameters.
   */
  private _listByMonitorNext(
    resourceGroupName: string,
    monitorName: string,
    nextLink: string,
    options?: AlertsListByMonitorNextOptionalParams,
  ): Promise<AlertsListByMonitorNextResponse> {
    return this.client.sendOperationRequest(
      { resourceGroupName, monitorName, nextLink, options },
      listByMonitorNextOperationSpec,
    );
  }
}
// Operation Specifications
const serializer = coreClient.createSerializer(Mappers, /* isXml */ false);

const listByMonitorOperationSpec: coreClient.OperationSpec = {
  path: "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Workloads/monitors/{monitorName}/alert",
  httpMethod: "GET",
  responses: {
    200: {
      bodyMapper: Mappers.AlertListResult,
    },
    default: {
      bodyMapper: Mappers.ErrorResponse,
    },
  },
  queryParameters: [Parameters.apiVersion],
  urlParameters: [
    Parameters.$host,
    Parameters.subscriptionId,
    Parameters.resourceGroupName,
    Parameters.monitorName,
  ],
  headerParameters: [Parameters.accept],
  serializer,
};
const getOperationSpec: coreClient.OperationSpec = {
  path: "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Workloads/monitors/{monitorName}/alert/{alert}",
  httpMethod: "GET",
  responses: {
    200: {
      bodyMapper: Mappers.Alert,
    },
    default: {
      bodyMapper: Mappers.ErrorResponse,
    },
  },
  queryParameters: [Parameters.apiVersion],
  urlParameters: [
    Parameters.$host,
    Parameters.subscriptionId,
    Parameters.resourceGroupName,
    Parameters.monitorName,
    Parameters.alert,
  ],
  headerParameters: [Parameters.accept],
  serializer,
};
const createOperationSpec: coreClient.OperationSpec = {
  path: "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Workloads/monitors/{monitorName}/alert/{alert}",
  httpMethod: "PUT",
  responses: {
    200: {
      bodyMapper: Mappers.Alert,
    },
    201: {
      bodyMapper: Mappers.Alert,
    },
    202: {
      bodyMapper: Mappers.Alert,
    },
    204: {
      bodyMapper: Mappers.Alert,
    },
    default: {
      bodyMapper: Mappers.ErrorResponse,
    },
  },
  requestBody: Parameters.resource1,
  queryParameters: [Parameters.apiVersion],
  urlParameters: [
    Parameters.$host,
    Parameters.subscriptionId,
    Parameters.resourceGroupName,
    Parameters.monitorName,
    Parameters.alert,
  ],
  headerParameters: [Parameters.accept, Parameters.contentType],
  mediaType: "json",
  serializer,
};
const deleteOperationSpec: coreClient.OperationSpec = {
  path: "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Workloads/monitors/{monitorName}/alert/{alert}",
  httpMethod: "DELETE",
  responses: {
    200: {
      headersMapper: Mappers.AlertsDeleteHeaders,
    },
    201: {
      headersMapper: Mappers.AlertsDeleteHeaders,
    },
    202: {
      headersMapper: Mappers.AlertsDeleteHeaders,
    },
    204: {
      headersMapper: Mappers.AlertsDeleteHeaders,
    },
    default: {
      bodyMapper: Mappers.ErrorResponse,
    },
  },
  queryParameters: [Parameters.apiVersion],
  urlParameters: [
    Parameters.$host,
    Parameters.subscriptionId,
    Parameters.resourceGroupName,
    Parameters.monitorName,
    Parameters.alert,
  ],
  headerParameters: [Parameters.accept],
  serializer,
};
const listByMonitorNextOperationSpec: coreClient.OperationSpec = {
  path: "{nextLink}",
  httpMethod: "GET",
  responses: {
    200: {
      bodyMapper: Mappers.AlertListResult,
    },
    default: {
      bodyMapper: Mappers.ErrorResponse,
    },
  },
  urlParameters: [
    Parameters.$host,
    Parameters.subscriptionId,
    Parameters.resourceGroupName,
    Parameters.monitorName,
    Parameters.nextLink,
  ],
  headerParameters: [Parameters.accept],
  serializer,
};
